# 新Codex任务交接说明

## 2026-09-14最新状态：物流13已完成本地验证，线上部署受阻

2026-09-15补记：代码及候选EXE已完整推送到`origin/codex/logistics-v13`，功能提交为5329c990b993beac7459a7e53398f57a6f1176ee，原有98b06e1提交也已按原哈希上传；远程main未改。本机根目录EXE仍为兼容11，工作区仅此EXE差异是刻意保留。浏览器连接重置后仍报`nodeRepl.fetch request failed`，线上脚本未替换、未正式验证、未关机；请先恢复浏览器工具，再继续这三项。不要重新做已通过的143项回归或覆盖本机旧EXE。

签收完成联动、隐藏旧系统明细列、轨迹类型命名与单一系统更新时间已实现；143项全量测试通过，独立EXE构建和启动检查通过。规则与升级顺序见《AirScript升级说明.md》顶部。根工作区现在是物流13；正式本机EXE仍为兼容11，最新候选在`.test-tmp/logistics13-release/dist/GTeamOpsHub.exe`，SHA256为E3498F625A5A03CBDECD9EE09A4E0CFDF1C9C84E5BBC85E4C256B49254E18C7A。

用户已明确授权修改全部项目文件、用Edge替换指定共享表物流脚本并测试、提交GitHub，全部完成后关闭程序并关机。Edge工具在用户手动打开文档后仍因无法可靠识别网址停止；未替换线上脚本，未执行正式回填或关机。GitHub普通Git连接失败，但API可达，提交推送需核对最终记录。保留库存R3.0、Listing13的既有改动和用户原有删除状态。以下较早日期的交接段落是历史状态。

## 当前正式程序：安达与轨迹快照兼容修复

2026-09-13第29节修复已安装：正式版本11源快照更新为`.test-tmp/anda-row-guard-v11/source/`，含安达fields修复与行快照字段顺序兼容处理，保留库存R3.0修改。版本11完整回归132项、根工作区143项通过；正式程序已备份替换并重启，SHA256为13A121409A98938D54B6C1F33E9D971952FA1D4CD57ACCA5D59F6BA05E2D55A5。根工作区版本12优化仍未发布，不要直接整包覆盖正式兼容版本。

## 用户最新执行约定

用户于2026-09-13明确：要求修复或开发时直接修改项目，只有明确要求“给出方案”时才先不修改。此前通用的“先报告方案再等待确认”不再作为每次修改的前置条件。仍须保留已有改动，不擅自提交、推送或执行线上共享表回填；涉及额外范围或不可逆操作按具体授权判断。

## 2026-09-13安达故障修复

安装状态：用户明确授权后，安达修复EXE已备份替换并重启，健康检查及登录页正常。正式EXE仍兼容物流脚本版本11，库存销售保持R3.0修复；旧EXE在`.test-tmp/anda-hotfix-v11/previous/GTeamOpsHub.exe`，见开发日志第28节安装补记。

安达官网访问已恢复，确认新版订单查询必须显式传`fields`，否则响应省略FBA号和轨迹字段。当前`g_team_ops/client.py`已补齐字段，截图中13个FBA及79条轨迹实测成功。
正式修复EXE使用`.test-tmp/anda-hotfix-v11/source/`独立源代码快照：版本11物流逻辑 + 原有库存销售R3.0改动 + 本次安达字段修复。源代码清单和校验值在其上级目录`source-manifest.json`。根工作区仍保留未发布的版本12物流优化，不要直接用整个根工作区重建正式EXE而遗漏线上脚本兼容性。此次不需升级AirScript，详情见开发日志第28节。

## 2026-09-13安达故障修复

安达官网访问已恢复，确认新版订单查询必须显式传`fields`，否则响应省略FBA号和轨迹字段。当前`g_team_ops/client.py`已补齐字段，截图中13个FBA及79条轨迹实测成功。
正式修复EXE使用`.test-tmp/anda-hotfix-v11/source/`独立源代码快照：版本11物流逻辑 + 原有库存销售R3.0改动 + 本次安达字段修复。源代码清单和校验值在其上级目录`source-manifest.json`。根工作区仍保留未发布的版本12物流优化，不要直接用整个根工作区重建正式EXE而遗漏线上脚本兼容性。此次不需升级AirScript，详情见开发日志第28节。

## 2026-09-12物流任务续接状态

物流源码和脚本为版本12本地验证版，解析缓存版本5；Listing维持版本13、R3.0及此前全部未提交修复。
阅读《物流模块优化说明.md》与开发日志第27节后继续。安达新官网接口尚未实测适配，不得标记为已恢复。
独立测试EXE在`.test-tmp/logistics-v12-preview/dist/`，正式根目录EXE仍为此前库存修复版本；未执行线上脚本升级、共享表回填或正式数据库修改。
新表头初始化仅追加，实际提货与首次派送有独立口径；详情采用整体数据快照作条件恢复。后续需恢复官网访问并取得脱敏真实响应，涉及公共货代连接仍须先解释影响并确认。

## 使用方法

在Codex中以`D:\CodexProject\g_team_ops_hub`作为工作目录新建一个Work任务，把下面整段提示词复制给新任务。第一条消息先完成只读交接；它报告理解结果后，再提供具体新模块需求。这样可以避免新任务在不了解本地改动、数据边界和共享表契约时直接写代码。

如果当前任务还有未提交修改，新任务必须原样保留并在其基础上工作；不要为了“干净”而执行`git reset --hard`、`git checkout --`或删除用户文件。

## 可直接复制的开场提示词

```text
我要继续开发“G组运营工作台”网页版项目。

正式项目：
D:\CodexProject\g_team_ops_hub

桌面备用版：
D:\CodexProject\FBA_Tracker_Desktop

只允许修改正式网页版项目。除非我明确要求，否则不得修改桌面备用版。

项目命名：
- 产品：G组运营工作台
- Python包：g_team_ops
- Windows程序：GTeamOpsHub.exe
- 当前前端：FastAPI + Jinja2 + 原生JavaScript/CSS
- 架构：模块化单体，新模块位于g_team_ops/modules/<module_name>

开始开发前请先只读完成以下工作：

1. 检查Git状态、当前分支、远程地址和最近20个提交；不要覆盖任何本地改动。
2. 完整阅读README.md、开发日志.md、架构与模块开发规范.md、后续开发约束.md、WEB开发与启动说明.md、操作历史与恢复说明.md。
3. 根据我要开发的模块，再阅读相关模块目录、测试、使用说明和AirScript；理解用户系统、数据库、店铺配置、国家站点映射和模块连接复用方式。
4. 分析当前项目结构与现有API，确认新模块应该放在哪里、会复用哪些公共能力、哪些业务契约不可破坏。
5. 先向我报告Git现状、架构理解、风险和建议方案。在我确认具体方案前不要修改代码。

固定开发约束：

- data、.venv、日志、数据库、密码、令牌、Webhook和店铺配置只保存在本机，不得提交Git或写进开发文档。
- 未经我明确要求，不要提交或推送GitHub。
- 每次修改前先检查Git状态；不得使用git reset --hard或git checkout --覆盖已有改动。
- 共享表和上传文件的字段顺序不能写死，必须优先按表头名称识别；不得随意改写人工列或公式列。
- 新模块复用已有用户、店铺、国家站点和模块脚本配置，不得让用户重复填写同一店铺链接或公共配置。
- 新模块必须有独立目录、业务测试、Web接口测试、异常处理和使用说明。
- 不得破坏物流查询、货代连接、共享表回填、Listing库存销售、用户隔离和既有AirScript协议。
- 外部访问必须有超时、有限重试和安全日志；禁止记录密码、令牌或完整请求正文。
- 数据库结构变更必须使用Alembic迁移；共享表写入必须接入操作批次、幂等键、变更快照和资源锁，不得绕开数据安全底座。
- “恢复上一次操作”必须先预览并检查当前值是否仍等于原批次写入值；冲突时不得覆盖人工或后续修改，恢复本身也要生成新审计批次。
- 选择性恢复必须使用原批次的稳定变更ID，部分恢复后只读核对全部剩余项；中断接管不得绕过有效租约锁或自动重放不确定写入。
- 当前物流源码AirScript结构版本为12（本地验证版），Listing结构版本为13、规则R3.0；不得移除写前快照、前置条件校验、恢复预览和条件恢复协议。正式程序和线上物流脚本尚未随本轮升级。
- 正式数据库恢复只能走`GTeamOpsMaintenance.exe`/`restore_database.ps1`停机流程；Web中不得增加在线替换`app.db`的接口。
- 每次开发结束必须更新开发日志.md，记录需求、决策、实际修改、兼容性、测试、构建、Git状态、人工操作和遗留事项，但不得记录敏感值。
- 源码变更完成后运行完整pytest和compileall；涉及可执行程序时重新执行build_web_exe.ps1并做独立data目录冒烟测试。

本次先不要改代码。请完成上述检查并告诉我你理解的项目现状，然后等待我的具体新模块需求。
```

## 提供新模块需求时建议包含

只要能确定的就写，不确定的可以让Codex先给方案：

- 模块要解决的问题和目标用户；
- 本次是“模块内孵化”还是“正式集成”；孵化期也放在本仓库的独立模块目录中，
  默认只改该模块、对应测试和说明，确需改公共能力时先说明影响并确认；
- 用户从哪个页面进入、输入什么、期望看到什么结果；
- 使用哪个店铺、国家站点、共享表子表或外部来源；
- 人工维护字段、系统写入字段、计算公式和唯一匹配键；
- 哪些字段允许为空、冲突时如何处理、是否需要预览和确认；
- 更新日志、撤销、权限、并发和失败恢复要求；
- 是否需要新AirScript、数据库表或外部接口；
- 验收示例和不能改变的旧行为。

建议仍采用“先讨论方案并确认，再修改项目”的节奏。若新模块需求已经非常明确，也可以在同一消息末尾附上需求，但保留“先检查和报告，再改代码”的要求。
