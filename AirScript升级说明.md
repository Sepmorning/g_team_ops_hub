# AirScript升级说明

本次升级增加了“从共享表读取待查询FBA”功能。要使用网页版的“一键查询并更新物流”，必须更新WPS文档中的AirScript脚本。

## 升级步骤

1. 打开对应店铺的WPS共享表。
2. 进入“效率 → 高级开发 → AirScript脚本编辑器”。
3. 打开现有的文档共享脚本。
4. 先自行复制旧代码留作备份。
5. 删除旧代码，将项目中 `airscripts\FBA物流自动回填.js` 的全部内容完整粘贴进去。
6. 保存脚本，并在编辑器中手动运行一次。默认执行验证，不会写入物流数据。
7. 确认日志显示执行完毕，然后回到网页版“我的店铺”测试该店铺的WPS连接。

通常不需要重新申请脚本令牌，也不需要更换Webhook；如果WPS后台重新生成了Webhook，则在“我的店铺”中更新对应店铺即可。

## 共享表要求

当前只处理名称为 `US-FBA` 的子表，第一行必须能识别以下三类表头：

- FBA：`FBA单号`、`FBA号`、`FBA编号` 或 `FBA`
- 完成状态：`是否完成`
- 路由：`货代最新路由信息`、`最新路由信息` 或 `路由`

同一类表头缺失或重复时，脚本会停止，不会猜测目标列。

## 一键查询规则

- `是否完成` 等于“是”：跳过该行。
- `是否完成` 为空、等于“否”或为其他内容：读取该行FBA并查询。
- 空白或不是以FBA开头的内容：忽略。
- 同一个FBA只查询一次，保留首次出现顺序。
- 程序查询时每批最多50条，但会自动处理后续批次，因此任务总量不限制为50条。
- 回填时每次Webhook最多写50条，程序同样会自动分批完成。

## 安全说明

脚本令牌只加密保存在当前电脑的本地数据库中。普通账号只能读取和修改自己的店铺配置；管理员也不会在网页中看到令牌明文。
