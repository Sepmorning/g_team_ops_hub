# AirScript升级说明

## Listing库存销售脚本：结构版本10

Listing脚本已从结构版本9升级到版本10。升级前先备份WPS工作簿，然后将项目内
`airscripts/Listing库存销售自动回填.js`完整替换到Listing共享脚本并保存。

版本10保留版本9的表头识别、评分文本格式、公式错误检查和审计恢复，并按最终确认修正：

- 复核`趋势差异率`并保留`(M1-M3)÷MAX(M3,1)`：M1和M3先换算成相同的30天速度。7/14/30日销量为14/25/35时，最近7日日均为2、较早16日日均为0.625，结果为220%；直接算40%会把7天总量与16天总量混比，不采用。
- `ListingRules`说明区改为“一种结果一行”，逐项写明所有销量状态、高/中/低预测可信度、异常原因、库存状态、广告状态、最终月销和建议补货量的精确触发条件。
- 预测可信度为高必须同时满足：30日销量达到B6、销量状态稳定、补货状态为空或正常补货、FBA可售大于0、广告状态正常或无广告；广告数据不足明确归为中可信度。
- `链接状态`改为完全人工记录，不再影响预测可信度、异常原因、最终月销、库存状态或建议补货量。
- 低可信度不再让`最终补货月销`留空；除清库存、停售、新品观察和暂缓补货外，最终月销自动等于系统建议月销，因此覆盖天数始终可查看。
- 只有低可信度对应的`最终补货月销`使用“蓝色，着色1，淡色60%”（#9DC3E6）；中可信度不再着色。初始化会把版本8旧条件格式从“中”迁移为“低”。
- 人工覆盖最终月销只对当前判断有效；下一次正式回填或再次初始化会恢复标准公式，不沿用旧人工值。
- 建议补货量按`ListingRules!B14`向上取整，默认5，因此13取15、1取5。
- `运营备注`改为可选表头；已有列继续保留，没有该列也能验证和回填。
- 根据实际金山文档兼容性和用户最终决定，彻底取消`ListingRules`保护方案。初始化不再调用`Protect`、不设置`Locked`或可编辑区域，规则表全部单元格保持可直接编辑。
- 若历史`ListingRules`仍无法解除，系统会把它保留为`ListingRules_旧保护`（重名时增加编号），再创建新的`ListingRules`并重装标准公式，不删除旧规则数据。
- “初始化 / 检查规则配置”仍是明确的恢复默认动作：重置参数、逐项说明、标准公式和低可信度提示色。执行前必须确认自定义规则已不再需要。

升级顺序必须是：先备份，再替换脚本，最后在网页选择店铺和国家并点击“初始化 / 检查规则配置”。首次初始化会迁移中文规则表或保留无法解锁的旧规则表，恢复版本10默认规则、详细说明和低可信度提示色；它不会替代WPS历史版本。完成后再次导入最新领星文件。

Listing版本10与物流版本11使用不同脚本连接和令牌。不要用Listing脚本替换物流脚本，
也不要把任何Webhook或令牌写入文档或Git。

## 物流脚本：结构版本11

本次升级将共享表回填扩展为“主表物流快照 + 国家站点轨迹明细”。网页版会读取安达、超鸿、易通的完整轨迹，统一解析预计时间、实际时间、异常、恢复、班次和POD，再同步到WPS。

新版物流脚本结构版本为 `11`。网页版会拒绝缺少写前快照、前置条件校验和条件恢复动作的旧脚本，避免在无法审计或无法保护人工新值时写入共享表。

## 升级前先备份

1. 在WPS中为当前工作簿创建副本。
2. 在AirScript编辑器中复制旧代码留作备份。
3. 不要删除或移动现有业务列；店铺、备注及其他自定义列可以原样保留。

## 主表要求

每个站点的主表建议命名为 `国家代码-FBA`，例如 `US-FBA`、`CA-FBA`。
网页版实际使用“我的店铺”中确认的精确子表名称。第一行加入以下20个标准表头：

```text
FBA号	货代	运输工具/班次	当前阶段	当前节点	最新轨迹时间	货代最新路由信息	当前异常	提货	预计出发	开船（机）时间	预计到达	到港	预计送达	提取派送	签收时间	POD状态	是否完成	数据状态	物流最后更新时间
```

这些列的顺序可以任意，也可以分散在店铺、备注等现有列之间。程序按第一行表头名称定位，不依赖列字母。

`运输方式`、`运输线路/渠道`、`目的仓`、`末端追踪号` 不再属于物流自动回填字段。它们可以从共享表删除；如果因其他模块需要而保留，新版脚本也会完全忽略，不读取、不清空、不写入。

兼容的旧名称包括：

- FBA号：`FBA单号`、`FBA编号`、`FBA`
- 货代：`物流商`、`货代公司`
- 货代最新路由信息：`最新物流信息`、`最新路由信息`、`路由`
- 开船（机）时间：`开船(机)时间`、`实际出发`
- 到港：`到港时间`、`实际到达`
- 物流最后更新时间：`物流更新时间`

同一含义不能同时出现两个表头。例如已有“路由”时，不要再增加“货代最新路由信息”，应直接保留“路由”或将它重命名。脚本发现缺失或重复含义时会停止，不会猜测列。

建议：

- 第一行不要合并单元格。
- FBA号列设为文本格式。
- 五个实际节点和三个预计节点可设为日期格式。
- 不要在系统物流列中放公式；自定义公式放在其他列。

## 明细表要求

每个站点的明细子表建议精确命名为 `国家代码-轨迹明细`，例如
`US-轨迹明细`、`CA-轨迹明细`。如果美国站点已经使用过旧版
`物流轨迹明细`，请直接将它重命名为 `US-轨迹明细`，不要新建空表，以保留已有事件。

第一行使用以下17个表头：

```text
事件编号	FBA号	货代	货代订单号	轨迹发生时间	标准阶段	标准节点	信息属性	物流轨迹原文	涉及计划	有效状态	异常状态	运输信息	官网原始状态	首次获取时间	最后确认时间	系统更新时间
```

明细表表头顺序同样可以任意，也允许增加其他列。旧表中的 `地点` 和 `附件/POD` 列可以删除。请勿手动修改系统生成的事件编号。

新版脚本会把 `事件编号`、`FBA号`、`货代订单号` 强制设为文本格式，防止长订单号显示为科学计数法或丢失精度；日期时间列会自动设置日期显示格式。

每条官网轨迹对应一行。事件编号相同的轨迹不会重复追加；船期或预计到达被后续信息替换时，已有明细的“有效状态”会更新为“已被更新”或“部分已更新”。

每次同步产生的新明细统一追加在所选站点的轨迹明细表当前最后一行之后，先按本次主表中的FBA行号排序，同一FBA再按轨迹发生时间从早到晚排序。已经存在的事件只更新解析字段，不移动、不重复追加。事件同步成功后，脚本会删除不再属于活跃集合的有效FBA明细行；活跃集合严格等于主表“是否完成”为空的FBA。查询失败但仍活跃的FBA旧明细保留，空白、说明或无效FBA行不会自动删除。

## 安装新版脚本

1. 打开对应店铺的WPS共享表。
2. 进入“效率 → 高级开发 → AirScript脚本编辑器”。
3. 打开当前文档共享脚本。
4. 将项目中 `airscripts\FBA物流自动回填.js` 的全部内容完整复制。
5. 替换WPS编辑器中的旧代码并保存。
6. 在编辑器中手动运行一次。默认仍只验证 `US-FBA` 和 `US-轨迹明细` 的全部表头，不写入物流数据。
7. 回到网页版“我的店铺”，打开“配置脚本与站点”，保存物流脚本Webhook和令牌。
8. 点击“扫描共享表”，确认各国家的Listing表、FBA主表和轨迹明细表映射。
9. 对准备使用的站点点击“测物流”；验证成功后，到“物流查询”选择店铺和国家执行一键更新。

若仍使用原文档和原共享脚本，通常不需要更换脚本令牌或Webhook。只有WPS重新生成Webhook时，才需要在“我的店铺”更新配置。

## 回填规则

- 只按表头名称写入上述系统物流列，不移动、不清空店铺、备注等其他列。
- 成功查询且在主表唯一匹配FBA后，只清除物流标准字段中由系统上次留下的“矢车菊蓝，着色1，浅色80%”高亮；不会整行清除，也不会处理其他业务列的手工填充。
- 本次真实发生变化且成功写入的主表业务单元格会填充“矢车菊蓝，着色1，浅色80%”。`物流最后更新时间`继续刷新但不标蓝，也不计入业务更新。
- 轨迹明细表只同步事件数据，不增加更新颜色；同步成功后只清理主表中已经完成或已移除FBA对应的整行，不整表清空。
- 网页“本次共享表更新明细”会列出FBA、单元格地址、表头、更新前和更新后值；人工值冲突及颜色格式失败会单独提示。
- 颜色设置失败不会撤销已经成功写入的数据，网页会显示“格式警告”；此时以网页列出的单元格更新明细为准。
- `货代` 只用于查询路由，系统不会自动改写。
- 单元格中的货代只唯一包含“安达”“超鸿”或“易通”时，先查指定货代。
- 只有指定货代明确返回“未找到”时，才查另外两家兜底。登录、网络等失败不会触发兜底。
- 兜底时只有“一家查询成功、其他家明确未找到”才回填；一家成功但另一家查询失败时标记“部分查询失败”并跳过回填。
- 查询失败、货代冲突、主表FBA重复或表中未找到时不回填该FBA。
- 预计时间只写入预计列，绝不会写入提货、开船（机）时间、到港、提取派送或签收时间。
- 提货、开船（机）时间、到港和签收时间为空时由系统填写；已有人工值且与官网解析结果不同时保留人工值，并在结果中报告“人工值冲突”。
- “提取派送”取最近一次实际“派送中”的日期；发生取消、改约并重新派送时，会更新为新的开始派送日期。没有“派送中”节点时才使用提柜、货物提取、递交末端承运商或海外仓入库兜底。
- “当前异常”可在异常恢复后自动清空；其他官网没有返回的字段不会用空值覆盖。
- 明细表追加新事件，同时允许更新已有事件的有效状态、异常状态和确认时间；随后安全清理非活跃FBA行。
- 已有明细的日期时间会先统一处理Excel日期序号和文本日期再比较，相同时间不会重复写入。
- 明细统计使用互斥口径：每条本次轨迹只会计入“新增”“实际更新”或“无变化”其中一项，不再把实际更新同时计入“已存在”。
- 每次一键任务会把主表日期列统一显示为 `yyyy-mm-dd`，自动修复以前显示为 `46187.00` 一类Excel日期序号的单元格。
- 已写成科学计数法的货代订单号会在对应FBA再次同步时按官网原值重新写成文本。
- 店铺“一键查询并更新物流”严格只读取 `是否完成` 为空的FBA。该列只要有任何内容（包括“是”或“否”）就完全跳过，即使 `物流最后更新时间` 为空也不会补查；手动输入FBA查询不受此规则限制。
- 一键任务总量不限50条。查询和回填会自动分批，富轨迹回填使用较小批次以控制WPS负载。

## 字段口径

- `提货`：货代实际收货、进仓或入仓日期。
- `开船（机）时间`：实际开船、起飞或发车日期。
- `到港`：实际到达目的港、机场或站点日期。
- `提取派送`：优先取最近一次实际“派送中”的日期；没有“派送中”时才取最早的实际提柜、货物提取、递交末端承运商或海外仓入库日期。
- `签收时间`：实际签收日期。
- `预计出发`、`预计到达`、`预计送达`：始终保留当前有效的最新计划。
- `当前阶段`：接收、起运准备、干线运输、目的地处理、末端配送、完成或其他。
- `信息属性`：实际、预计、变更、异常、恢复、附件或其组合。

## 版本11升级操作

版本11在版本10活跃FBA明细清理基础上新增只读快照、条件写入、恢复预览和条件恢复，
不需要增加或调整物流表头：

1. 备份当前WPS工作簿和旧AirScript。
2. 将项目中 `airscripts\FBA物流自动回填.js` 的全部内容复制到物流文档共享脚本，完整替换旧版并保存。
3. 在AirScript编辑器中手动运行一次默认验证，确认 `US-FBA` 和 `US-轨迹明细` 表头均通过。
4. 启动新版网页程序，在“我的店铺”中保存物流脚本连接并点击“扫描共享表”。
5. 核对每个国家对应的FBA主表和轨迹明细表；`FAB`等疑似拼写错误必须人工修正后再测试。
6. 在站点列表点击“测物流”，确认网页版连接到正确的两张子表。
7. 先选择一条已知会变化的FBA测试：确认主表只有本次实际更新单元格变蓝，旧系统蓝色被清除，轨迹明细表不着色。
8. 准备一条“是否完成”为“是”的测试FBA，确认同步后只清除它的明细；“是否完成”为空但查询失败的FBA旧明细必须保留。
9. 回到物流查询页面检查“本次共享表更新明细”，核对单元格地址、表头、新旧值和明细清理行数。
10. 登录“操作历史与恢复”，先执行恢复预览；若测试单元格没有被人工再次修改，可确认恢复并核对新生成的恢复审计批次。

恢复会按FBA号、事件编号和表头名称重新定位，不依赖旧行号或列号。当前值不再等于
原操作写入值时只报告冲突；不会覆盖人工新值、公式列或其他模块字段。

系统以主题色属性设置“着色1，浅色80%”，颜色会跟随当前工作簿主题。请不要把完全相同的主题色当作物流字段中的长期人工标记，否则下次成功查询该FBA时会被视为上次系统高亮并清除。

## 本地数据与安全

完整轨迹缓存保存在当前用户的本机数据库中，用于在官网最新轨迹未变化时减少重复详情请求。`data`、密码、登录令牌和AirScript令牌均只保存在本机，不应提交Git或发送给他人。
