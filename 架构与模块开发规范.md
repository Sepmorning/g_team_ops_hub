# 架构与模块开发规范

本文是后续新增模块和重构代码的强制约束。目标是在继续使用单个FastAPI程序、
单个EXE和本机数据库的前提下，让各业务功能可以独立开发、测试和维护。

## 1. 当前架构

项目采用“模块化单体”，不是微服务：

- `anda_tracker.web.factory`只负责创建应用、共享依赖和注册模块；
- `anda_tracker.web.context`提供当前用户、数据库、模板、CSRF和模块公共配置；
- `anda_tracker.modules`按业务能力划分账号、货代连接、店铺、物流和库存销售；
- `anda_tracker.web.app.create_app`是兼容入口，不得重新堆入业务路由；
- 既有业务服务、数据库和外部接口客户端暂时保留原位置，后续按阶段迁移；
- WPS AirScript仍是独立的单文件部署产物，不因Python目录重构改变脚本协议。

## 2. 新模块目录

后续新模块必须直接在当前仓库中开发，不得先在另一个项目中完成后整体复制。
标准结构如下：

```text
anda_tracker/modules/<module_name>/
├─ __init__.py
├─ router.py          页面和API入口
├─ service.py         业务用例和事务编排
├─ repository.py      本模块的数据读写
├─ models.py          本模块的数据结构
└─ integrations.py    仅当模块有专属外部接口时使用

tests/
├─ test_<module_name>.py
└─ test_<module_name>_web.py
```

简单模块可以暂时只有`router.py`和`service.py`，但不得把代码写回
`web/factory.py`、`web/app.py`或其他模块的路由文件。

## 3. 强制边界

### Router

- 只处理HTTP参数、登录权限、CSRF、文件大小和响应格式；
- 不直接执行SQL；
- 不直接实现库存、销量、物流阶段或回填计算；
- 不直接创建任意URL请求，外部访问必须交给服务或集成客户端；
- 已发布接口不得无迁移方案地改变URL、HTTP方法、状态码或JSON字段。

### Service

- 负责业务校验、流程编排和事务边界；
- 不依赖FastAPI的`Request`、`HTMLResponse`或`JSONResponse`；
- 通过Repository取得数据，通过明确的集成接口访问WPS或货代；
- 所有写入操作必须返回可测试的结果摘要，不允许静默忽略失败。

### Repository

- 所有用户业务数据必须带`profile_id`隔离；
- 店铺数据必须同时校验店铺归属，站点数据必须校验店铺与用户归属；
- 密码、令牌和脚本密钥继续加密保存，禁止记录到日志；
- 新表和字段必须通过版本化迁移创建，不得要求用户删除现有数据库；
- 不得在Repository中调用货代、WPS或其他外部网站。

### Integration

- 每个货代或外部平台使用独立适配器；
- 必须设置超时、有限重试和安全错误信息；
- 禁止关闭TLS证书校验，禁止把密码、令牌、验证码或完整请求正文写入日志；
- 表格列必须按表头名称识别，不得把共享表列号或顺序写死；
- 外部接口返回值必须先转换为内部模型，再进入业务服务。

## 4. 跨模块规则

- 模块不得导入另一个模块的`router.py`；
- 跨模块能力通过Service接口、共享模型或明确的Facade调用；
- 店铺、用户、国家站点和模块连接是公共基础能力，不在新模块中重复保存；
- 每个模块可以拥有独立AirScript连接，但连接仍统一配置在“我的店铺”；
- 新模块页面只选择已有店铺和站点，不重复要求用户输入共享表链接和公共令牌；
- 不为某个模块修改其他模块已经确认的表格字段、公式或回填规则。

## 5. 不可破坏的业务契约

任何重构和新模块都必须继续满足：

- 店铺一键物流更新只读取`是否完成`为空的FBA；
- 货代定向查询、未找到兜底、部分失败和冲突判断规则不变；
- 物流查询失败、未找到、重复FBA和人工值冲突时不得错误回填；
- 物流主表按表头名称定位并只标记真实更新单元格，轨迹明细不着色；
- Listing源文件和目标共享表均按表头名称定位，列顺序任意；
- Listing同日重试、上次/本次数据滚动、25%月销差异阈值和可选优惠价规则不变；
- 模块间不得改写不属于自己的人工维护列；
- `data`、日志、数据库、密码、令牌和店铺配置不得提交Git。

## 6. 新模块开发流程

1. 先写模块说明，明确输入、输出、表头、人工字段和系统字段；
2. 建立独立模块目录和测试文件；
3. 先实现纯计算和解析测试，再实现Repository与外部接口；
4. 最后增加Router并在`web.factory`注册；
5. 添加API契约测试，固定URL、方法、状态码和响应结构；
6. 运行完整测试、语法编译和EXE构建；
7. 对共享表执行写入前预览，并保留明确的更新摘要和异常提示；
8. 一个模块或一个重构阶段单独提交，禁止把无关功能混在同一提交。

## 7. 前端重构约束

当前前端是Jinja2和原生JavaScript。后续可以迁移Vue 3，但必须与后端业务重构
分开进行：

- 先固定API契约，再逐页替换界面；
- Vue不得直接访问数据库或拼接货代/WPS请求；
- 登录会话、CSRF和用户隔离继续由FastAPI负责；
- 前端构建结果需要能随PyInstaller打入EXE，用户运行EXE时不依赖Node.js；
- 同一阶段不同时重写物流算法、数据库结构和前端页面。

## 8. 验收门槛

每次结构调整或新增模块至少需要：

- 现有完整测试全部通过；
- `tests/test_modular_architecture.py`中的兼容入口和路由契约通过；
- 原数据库可以直接启动，不丢失用户、店铺、凭据或缓存；
- 物流、Listing和AirScript协议测试通过；
- EXE重新构建成功；
- Git状态中不包含`data`、数据库、日志、密码或令牌。
