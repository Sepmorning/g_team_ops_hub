# 三家货代查询接口审计

审计日期：2026-07-26

## 结论

当前项目并不是通过浏览器自动点击或解析页面文本查询，而是直接调用三家官网前端自身使用的结构化HTTP接口。对于尚未提供正式开放API文档和独立API密钥的货代，这是目前速度和稳定性最好的实现方式。

真正优于当前方式的长期方案，是由货代提供正式合作方API、API密钥、版本说明、调用配额和变更通知。如果货代能够提供，应新增官方适配器并逐步替换前端内部接口；在此之前不改用浏览器自动化或网页抓取。

## 安达

- 官网当前公开配置仍将API基址指向 `https://fms.yunwuyun.com`，前缀为 `/api`。
- 项目直接调用JSON登录接口和FBA订单批量查询接口。
- 查询条件使用官网支持的多值条件，一次可提交一批FBA。
- 优于页面自动化：无需打开浏览器、无需等待DOM、响应为结构化JSON。
- 风险：这是登录后前端接口，不是独立公开API合同；路径或字段可能随官网升级改变。
- 本次优化：系统外层每50条分批；登录会话被浏览器挤掉时，批量查询和完整轨迹读取都会自动重新登录一次并重试当前请求。
- 完整轨迹：批量列表命中后，使用官网 `getTraceList/{traceNo}` 结构化接口读取时间轴，并结合 `warehouseInTime`、`etd`、`atd`、`eta`、`ata`、`signReceiveTime` 等订单字段校准快照。

## 超鸿

- 官网 `track.js` 明确使用 `GET /api/traces/batch/<JSON单号数组>`。
- 当前项目调用的正是该批量接口，而不是官网一次一条的表单交互。
- 这是目前三家中速度最快的方式。
- 本次优化：每个URL最多携带50条，超过后自动拆分，避免URL过长并降低服务端压力。
- 完整轨迹：批量命中后，使用官网单号详情接口读取 `traces` 时间轴和POD信息；目的地、末端追踪号不属于物流模块自动维护字段。
- 风险：接口仍为HTTP和IP地址，缺少TLS；不要向它发送任何账号、密码或内部令牌。

## 易通

- 项目直接调用官网用户登录、验证码、用户状态和订单列表JSON接口。
- 图形验证码仍由用户人工输入，不识别、不破解、不绕过。
- 登录成功后复用加密保存的会话令牌；失效时才重新验证。
- 订单接口原生支持以换行分隔的一批FBA号。
- 完整轨迹：订单命中后，使用官网 `getWorkorderRouterActivities` 结构化接口读取进仓、装柜、出口放行、开船、到港、清关、提柜、拆柜、派送、签收及附件时间轴。
- 风险：接口使用HTTP且属于官网内部前端接口；登录负载中包含账号密码。2026-07-26无凭据测试表明对应HTTPS主机证书已过期，因此项目不能在保持证书校验的前提下安全切换到HTTPS，也不会使用 `verify=False` 绕过证书。应只在可信公司网络登录，并推动货代修复HTTPS或提供正式API。

## 当前查询调度

1. 用户输入总量不设上限。
2. 系统按50条切分外层批次，并顺序处理批次，避免瞬间高并发。
3. 打开物流页面只读取本机状态，不访问货代网站。手动查询优先复用当前用户最近10分钟的连接结果；缓存缺失或过期时才并行检查安达、超鸿、易通，并只调度最近检查成功的货代。用户可以点击“立即重新检查”强制绕过缓存。
4. 某家未连接不终止手动任务；已连接货代之间仍进行归属判定，两家以上查到时报告冲突。
5. 店铺一键任务读取共享表“货代”列，按安达、超鸿、易通分组，优先请求对应货代。
   仅当指定货代明确返回“未找到”时，才对该FBA查询另外两家兜底；查询失败不兜底。
   兜底中一家命中而另一家查询失败时标记“部分查询失败”并跳过回填；只有其他家
   都明确未找到时才采用唯一命中结果。
6. 一键任务开始时只验证共享表指定的货代；其他货代仅在出现“未找到”并执行
   兜底时按需登录或查询，不预先发送健康检查。
7. 不同货代分组并行执行，各组内部继续按安全批次顺序查询。
8. 货代为空、无法识别或同时包含多个货代名称时明确报告配置失败，不回退为三家全查。
9. 批量结果返回的最新时间和最新事件与本机缓存一致时，复用完整轨迹缓存；发生变化或没有缓存时才请求详情接口。
10. 完整轨迹统一解析为通用阶段、节点、预计/实际、异常/恢复和POD；预计时间不进入实际时间列。
11. 全部结果完成后交给AirScript。旧式路由回填每50条一批，包含完整轨迹的回填每10条一批，以限制WPS单次负载；用户总量不受限。

## 后续进一步优化

- 优先询问三家货代是否提供正式客户API、Webhook或轨迹订阅服务。
- 已按用户确认采用共享表“货代”列定向优先查询；只有指定货代未找到时才查询
  另外两家兜底并检查冲突，手动查询仍在本次已连接货代之间进行归属判定。
- 可以缓存已确认的FBA货代归属，并设置有效期；首次或定期仍跨三家核验。
- 增加接口健康检查和字段变化测试，官网升级时更早发现问题。
- 完整轨迹已进入本地用户隔离缓存，仅用于减少未变化详情的重复请求；批量最新状态仍实时查询，缓存不会冒充实时结果。
