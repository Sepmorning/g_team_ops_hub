# 三家货代查询接口审计

审计日期：2026-07-21

## 结论

当前项目并不是通过浏览器自动点击或解析页面文本查询，而是直接调用三家官网前端自身使用的结构化HTTP接口。对于尚未提供正式开放API文档和独立API密钥的货代，这是目前速度和稳定性最好的实现方式。

真正优于当前方式的长期方案，是由货代提供正式合作方API、API密钥、版本说明、调用配额和变更通知。如果货代能够提供，应新增官方适配器并逐步替换前端内部接口；在此之前不改用浏览器自动化或网页抓取。

## 安达

- 官网当前公开配置仍将API基址指向 `https://fms.yunwuyun.com`，前缀为 `/api`。
- 项目直接调用JSON登录接口和FBA订单批量查询接口。
- 查询条件使用官网支持的多值条件，一次可提交一批FBA。
- 优于页面自动化：无需打开浏览器、无需等待DOM、响应为结构化JSON。
- 风险：这是登录后前端接口，不是独立公开API合同；路径或字段可能随官网升级改变。
- 本次优化：系统外层每50条分批；登录会话被浏览器挤掉时自动重新登录一次并重试当前批次。

## 超鸿

- 官网 `track.js` 明确使用 `GET /api/traces/batch/<JSON单号数组>`。
- 当前项目调用的正是该批量接口，而不是官网一次一条的表单交互。
- 这是目前三家中速度最快的方式。
- 本次优化：每个URL最多携带50条，超过后自动拆分，避免URL过长并降低服务端压力。
- 风险：接口仍为HTTP和IP地址，缺少TLS；不要向它发送任何账号、密码或内部令牌。

## 易通

- 项目直接调用官网用户登录、验证码、用户状态和订单列表JSON接口。
- 图形验证码仍由用户人工输入，不识别、不破解、不绕过。
- 登录成功后复用加密保存的会话令牌；失效时才重新验证。
- 订单接口原生支持以换行分隔的一批FBA号。
- 风险：接口使用HTTP且属于官网内部前端接口；字段和路径可能升级。

## 当前查询调度

1. 用户输入总量不设上限。
2. 系统按50条切分外层批次，并顺序处理批次，避免瞬间高并发。
3. 每个批次内，安达、超鸿、易通并行查询。
4. 三家结果统一后进行货代归属判定。
5. 两家或以上查到同一FBA时报告冲突。
6. 某家查询失败但另一家查到时报告部分失败，不静默认定归属。
7. 全部结果完成后交给AirScript，每50条回填一次，用户总量不受限。

## 后续进一步优化

- 优先询问三家货代是否提供正式客户API、Webhook或轨迹订阅服务。
- 未来从WPS读取可信的“货代”列后，可以只查询指定货代，大幅减少请求；但这会放弃“每次都跨三家检查冲突”，需由管理员选择策略。
- 可以缓存已确认的FBA货代归属，并设置有效期；首次或定期仍跨三家核验。
- 增加接口健康检查和字段变化测试，官网升级时更早发现问题。
- 查询历史和最近成功结果可进入本地数据库，临时网络失败时显示上次结果但明确标记时间，不能冒充实时数据。
