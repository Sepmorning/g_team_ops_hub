# 项目代码审计报告

审计日期：2026-07-26

## 2026-08-09模块化架构调整

- FastAPI应用装配迁入`g_team_ops.web.factory`，原`g_team_ops.web.app`保留兼容入口，启动脚本和测试调用方式不变。
- 新增`g_team_ops.web.context`，集中当前用户、CSRF、数据库、模板、店铺站点配置等Web层公共依赖。
- 页面和API按账号、货代连接、店铺、物流、库存销售拆入`g_team_ops.modules`，应用工厂不再直接承载业务路由。
- 新增完整HTTP路由契约测试，防止结构移动时意外改变URL或HTTP方法。
- 本阶段没有改变SQLite表结构、外部接口地址、AirScript协议、页面模板或业务计算；`web.services`、`storage.py`和`listing.py`将在后续独立阶段继续按服务与仓储边界拆分。
- 后续新模块强制遵守`架构与模块开发规范.md`，不得重新向应用工厂或其他模块路由堆入业务代码。

## 2026-08-09数据安全底座审计

- 本机SQLite继续作为正式数据库；所有连接统一启用WAL、外键、10秒写等待和`FULL`同步，避免把更换数据库误当成共享表可恢复能力。
- 引入SQLAlchemy 2和Alembic版本化迁移，旧数据库按迁移版本升级；迁移前先通过SQLite在线备份生成一致性快照并校验完整性。
- 新增独立`operations`模块，提供按用户隔离的操作批次、逐项结果、字段变更快照、不可变审计事件、幂等键和带租约的数据库资源锁。
- 管理员可以在“操作历史与恢复”页面创建和验证本机整库备份；备份不支持网页下载，运行中的`app.db`禁止在线替换。
- 当前物流和Listing写入尚未接入写前快照与条件恢复协议，所以本阶段没有开放可能覆盖后续人工值的WPS恢复按钮。
- 完整测试为`103 passed, 2 skipped`；语法编译、EXE重新构建和独立`data`目录健康检查均通过。

## 2026-08-09共享表安全恢复第二阶段审计

- 物流与Listing写入已统一接入`SharedTableOperationManager`：先创建幂等批次和店铺工作簿锁，再读取并持久化写前快照，最后按前置条件写入和记录精确差异。
- 新增`0003_shared_table_snapshots`迁移。写前原值保存在`operation_snapshots`，即使外部写入失败或进程随后异常，原值也不只存在于内存。
- 物流AirScript为结构版本11，Listing AirScript已升级到结构版本10；两者均提供只读快照、目标回读、恢复预览和条件恢复动作，并继续按表头与FBA、事件编号或MSKU定位。Listing版本10继续检查公式错误行并使用`ListingRules`规避WPS中文跨表引用的`#NAME?`；初始化会重置旧人工月销、写入逐结果精确规则说明并仅为低可信度最终月销安装提示色。根据金山文档兼容性和用户最终决定，`ListingRules`不再设置工作表或单元格保护。
- 恢复只允许当前用户、同店铺、同模块最近一个可恢复批次。当前值仍等于原批次新值才反写旧值；人工或更晚修改作为冲突跳过。
- 物流轨迹明细新增、更新和清理均按稳定事件编号审计；Listing审计程序写入的原始字段和标准公式文本，人工基础列及人工覆盖的最终补货月销不被导入覆盖。
- 恢复本身生成新的`restore`批次和反向字段差异；分批中断时保留已完成部分并标记`rollback_partial`，不伪装为全部成功。
- 完整测试为`109 passed`，仅有既有Starlette TestClient/httpx弃用警告；Python编译、两份AirScript语法检查、EXE构建和独立目录迁移/健康冒烟均通过。

## 2026-08-13灾难恢复闭环第三阶段审计

- 新增`0004_database_restore_history`迁移，记录整库恢复来源、恢复前保险备份、执行者和结果；恢复历史可由管理员在操作历史页面查看。
- Web程序每天首次启动自动创建SQLite一致性备份并执行完整性检查；只保留最近14份`scheduled_daily`备份，不自动删除手工、迁移前或恢复前备份。
- 新增独立`GTeamOpsMaintenance.exe`与`restore_database.ps1`。正式恢复前再次验证来源备份、创建`pre_restore`保险备份、确认SQLite可独占访问并设置维护锁，Web程序不会在线替换`app.db`。
- 整库恢复使用SQLite备份API写回正式数据库，完成后自动迁移到当前版本并再次检查完整性；后处理失败时从恢复前保险备份回写原库。
- `uncertain`共享表批次新增只读核对入口。系统以持久化写前快照为基线重新读取WPS，只记录实际差异，不修改共享表；有变化转为`reconciled`并具备冲突保护恢复条件，无变化转为`reconciled_no_change`。
- 第三阶段没有更改物流或Listing AirScript协议版本、货代访问、表头与公式契约、用户店铺配置或国家站点映射。
- 完整测试为`116 passed`；Python编译、两份AirScript语法检查、Web与维护双EXE构建，以及隔离目录中的自动备份、维护预览、真实恢复、迁移和恢复记录冒烟均通过。

## 2026-08-15安全续作与选择性恢复第四阶段审计

- 运行中批次增加显式接管流程；仍有效的持久化租约锁会拒绝接管，避免原任务尚在运行时释放并发保护。
- 接管后的普通写入根据持久化快照转入`uncertain`或`interrupted`，恢复任务转入`rollback_partial`；不自动重放结果不确定的WPS写入。
- 恢复预览和执行支持按`operation_changes.id`选择项目，并再次校验变更归属；不把可变行号、列号或客户端数组序号当作跨请求标识。
- 部分恢复后只读检查原批次全部变化，只有没有待恢复、冲突和失败项时才标记`rolled_back`；未预期中断保留为可重新预览的`rollback_partial`。
- 管理员页面增加每日备份健康告警和逐份完整校验入口；健康摘要不替代SHA-256及SQLite完整性验证。

## 2026-07-26物流查询与更新专项审计

### 本轮已修复

1. 指定货代“未找到”后的兜底查询，原来在一家命中、另一家查询失败时仍会采用命中结果，存在未排除货代冲突就错误回填的风险。现在标记为“部分查询失败”并跳过回填；全部兜底货代完成且只有一家命中时才回填。
2. 安达批量订单查询支持会话失效后自动重登，但随后读取完整轨迹时没有同样保护。现在完整轨迹请求遇到认证失效也会自动重登并只重试一次。
3. `GET /api/carriers/status?live=1` 会执行登录、网络请求，甚至在明确认证失效时删除易通旧会话，违反GET只读语义。现在 `GET /api/carriers/status` 只读取配置/缓存快照，实时验证统一为 `POST /api/carriers/validation`。
4. 安达客户端的GET/POST状态码、重试和JSON解析逻辑重复；超鸿批量与详情请求也重复同一套网络处理。现在分别收敛为共享请求方法，减少以后修改一处遗漏另一处的风险。
5. 删除无人调用的 `AirScriptClient.list_pending_fbas`，以及已取消自动维护却仍残留在内部模型中的运输方式、线路/渠道、目的仓、末端追踪号和地点字段；轨迹缓存结构升级为版本4。
6. 完整轨迹的可预期货代错误和数据格式错误不再输出无意义堆栈；未预期异常仍保留堆栈。共享表同步失败增加分类警告日志。
7. 日志不再直接写入FBA号；继续禁止记录密码、令牌、验证码和请求正文。
8. 清理测试中的未使用局部变量，并新增兜底不确定、安达详情重登和货代验证接口语义的回归测试。
9. 定向查询原来只校验货代返回结果数量，未校验返回FBA集合；上游错返同样数量的其他FBA时可能在后续映射中崩溃。现在按请求集合归一化结果，漏返、错返会稳定标记为接口响应失败。

### 克隆可用性

- `data/`、`.venv/`、日志、`.env*`、密钥和证书文件均由 `.gitignore` 排除。
- Git已跟踪文件中没有本机数据库、密码、会话令牌或店铺配置。
- README补充了Windows全新克隆、系统管理员、货代账号、WPS AirScript和店铺配置的完整首次使用顺序。
- GitHub Actions会在Windows和Python 3.12上执行完整测试与语法编译。

## 已修复问题

### 潜在Bug

1. 单个货代抛出未预期异常时，可能导致整批三家货代查询中断。现在按货代隔离异常，其他货代继续返回，并将故障货代标记为失败。
2. 某货代漏返FBA结果时，原逻辑可能产生 `KeyError`。现在补充结构校验并返回明确失败结果。
3. 易通令牌验证遇到网络或服务端临时故障时会删除已保存令牌。现在只有明确的认证失败才删除令牌。
4. 首次初始化存在并发提交时创建多个管理员的极小窗口。现在通过SQLite即时事务保证只初始化一次。
5. 两个进程同时首次创建Web会话密钥时可能互相覆盖。现在使用排他创建并复用成功写入的密钥。
6. 验证码挑战没有过期时间。现在五分钟后自动失效，并在创建新挑战时清理旧数据。
7. 手动查询请求正文没有长度保护，极端大输入可能占用过多内存。现在限制为20万字符。
8. `sync_wps` 接受非布尔值时会被Python真值规则误判。现在只接受JSON布尔值。

### 重复和未使用代码

1. 删除已经停用的WPS开放平台OAuth客户端、绑定模型和整套测试；当前只保留AirScript方案。
2. 删除旧的单用户AirScript保存、读取、删除接口；统一使用多店铺模型。
3. 删除旧的 `/connections` 页面、模板和 `/api/connections/*` 接口。
4. 删除网页版中的PyQt界面、主题、桌面入口、桌面构建脚本和PyQt依赖。
5. 删除只剩测试调用的旧总量限制函数、异常别名和兼容方法。
6. 消除货代状态读取时重复访问数据库的逻辑。

### 日志和异常处理

1. 新增 `data/logs/web.log`，单文件5MB、保留5份滚动日志。
2. 记录登录、查询开始/完成、货代连接验证、AirScript回填摘要和未预期异常。
3. 日志不记录密码、令牌、验证码、请求正文或FBA明细。
4. 用户可预期异常继续显示安全的中文错误；未预期异常返回统一提示并写入堆栈日志。

### 接口命名

当前业务接口统一为资源域：

- `/api/tracking/query`
- `/api/carriers/status`
- `/api/carriers/validation`
- `/api/carriers/anda`
- `/api/carriers/yitong/captcha-challenges`
- `/api/carriers/yitong/session`
- `/api/shops`
- `/api/shops/{shop_id}/validation`
- `/api/shops/{shop_id}/tracking-sync`

## 桌面版分离

- 网页版：`D:\CodexProject\g_team_ops_hub`
- 桌面版：`D:\CodexProject\FBA_Tracker_Desktop`

两套程序各自拥有：

- 独立源码
- 独立 `.venv`
- 独立 `data/app.db`
- 独立设置文件
- 独立构建脚本
- 独立EXE

分离时复制了数据库快照；分离后的新增、修改和删除不会同步到另一套程序。

## 当前保留风险

1. 三家货代使用其网页前端调用的接口，并非货代承诺长期稳定的正式开放API。网站改版后仍可能需要更新字段或地址。
2. 当前后端仍定位为单机、单进程使用。共享资源锁已经持久化，但验证码和部分短时验证状态仍在进程内；以后部署多进程或多服务器时仍需要Redis和正式任务队列。
3. 物流和Listing共享表写入已支持锁过期后的安全接管、状态不确定只读核对及逐项继续恢复，但货代查询等长任务仍没有持久化后台队列和自动断点续作；接管与取舍需要人工确认。
4. 数据库已具备停机维护恢复、恢复前保险备份和恢复记录；仍禁止在线替换运行中的`app.db`。维护程序异常退出或系统断电仍需依据`pre_restore`备份人工核对。
5. DPAPI密文与当前Windows用户绑定。复制数据库到其他电脑或Windows账号后通常无法解密。
6. 测试环境存在Starlette TestClient对当前 `httpx` 兼容层的弃用警告，不影响运行；未来升级FastAPI测试依赖时应一并消除。
7. 超鸿官网当前只提供项目正在使用的HTTP明文接口；易通登录和业务接口同样使用HTTP，且无凭据测试发现其HTTPS证书已过期。两者都无法由客户端单方面安全升级；在不可信网络中存在被监听或篡改的上游风险。易通登录应只在可信公司网络进行，货代提供有效HTTPS或正式开放API后应优先迁移。
8. 系统级恢复依赖WPS中安装物流版本11和Listing版本10脚本；用户仍需手工完整替换旧脚本。冲突项只报告不覆盖，必要时继续使用WPS历史版本做文件级兜底。
