# 项目代码审计报告

审计日期：2026-07-26

## 2026-08-09模块化架构调整

- FastAPI应用装配迁入`g_team_ops.web.factory`，原`g_team_ops.web.app`保留兼容入口，启动脚本和测试调用方式不变。
- 新增`g_team_ops.web.context`，集中当前用户、CSRF、数据库、模板、店铺站点配置等Web层公共依赖。
- 页面和API按账号、货代连接、店铺、物流、库存销售拆入`g_team_ops.modules`，应用工厂不再直接承载业务路由。
- 新增完整HTTP路由契约测试，防止结构移动时意外改变URL或HTTP方法。
- 本阶段没有改变SQLite表结构、外部接口地址、AirScript协议、页面模板或业务计算；`web.services`、`storage.py`和`listing.py`将在后续独立阶段继续按服务与仓储边界拆分。
- 后续新模块强制遵守`架构与模块开发规范.md`，不得重新向应用工厂或其他模块路由堆入业务代码。

## 2026-07-26物流查询与更新专项审计

### 本轮已修复

1. 指定货代“未找到”后的兜底查询，原来在一家命中、另一家查询失败时仍会采用命中结果，存在未排除货代冲突就错误回填的风险。现在标记为“部分查询失败”并跳过回填；全部兜底货代完成且只有一家命中时才回填。
2. 安达批量订单查询支持会话失效后自动重登，但随后读取完整轨迹时没有同样保护。现在完整轨迹请求遇到认证失效也会自动重登并只重试一次。
3. `GET /api/carriers/status?live=1` 会执行登录、网络请求，甚至在明确认证失效时删除易通旧会话，违反GET只读语义。现在 `GET /api/carriers/status` 只读取配置/缓存快照，实时验证统一为 `POST /api/carriers/validation`。
4. 安达客户端的GET/POST状态码、重试和JSON解析逻辑重复；超鸿批量与详情请求也重复同一套网络处理。现在分别收敛为共享请求方法，减少以后修改一处遗漏另一处的风险。
5. 删除无人调用的 `AirScriptClient.list_pending_fbas`，以及已取消自动维护却仍残留在内部模型中的运输方式、线路/渠道、目的仓、末端追踪号和地点字段；轨迹缓存结构升级为版本4。
6. 完整轨迹的可预期货代错误和数据格式错误不再输出无意义堆栈；未预期异常仍保留堆栈。共享表同步失败增加分类警告日志。
7. 日志不再直接写入FBA号；继续禁止记录密码、令牌、验证码和请求正文。
8. 清理测试中的未使用局部变量，并新增兜底不确定、安达详情重登和货代验证接口语义的回归测试。
9. 定向查询原来只校验货代返回结果数量，未校验返回FBA集合；上游错返同样数量的其他FBA时可能在后续映射中崩溃。现在按请求集合归一化结果，漏返、错返会稳定标记为接口响应失败。

### 克隆可用性

- `data/`、`.venv/`、日志、`.env*`、密钥和证书文件均由 `.gitignore` 排除。
- Git已跟踪文件中没有本机数据库、密码、会话令牌或店铺配置。
- README补充了Windows全新克隆、系统管理员、货代账号、WPS AirScript和店铺配置的完整首次使用顺序。
- GitHub Actions会在Windows和Python 3.12上执行完整测试与语法编译。

## 已修复问题

### 潜在Bug

1. 单个货代抛出未预期异常时，可能导致整批三家货代查询中断。现在按货代隔离异常，其他货代继续返回，并将故障货代标记为失败。
2. 某货代漏返FBA结果时，原逻辑可能产生 `KeyError`。现在补充结构校验并返回明确失败结果。
3. 易通令牌验证遇到网络或服务端临时故障时会删除已保存令牌。现在只有明确的认证失败才删除令牌。
4. 首次初始化存在并发提交时创建多个管理员的极小窗口。现在通过SQLite即时事务保证只初始化一次。
5. 两个进程同时首次创建Web会话密钥时可能互相覆盖。现在使用排他创建并复用成功写入的密钥。
6. 验证码挑战没有过期时间。现在五分钟后自动失效，并在创建新挑战时清理旧数据。
7. 手动查询请求正文没有长度保护，极端大输入可能占用过多内存。现在限制为20万字符。
8. `sync_wps` 接受非布尔值时会被Python真值规则误判。现在只接受JSON布尔值。

### 重复和未使用代码

1. 删除已经停用的WPS开放平台OAuth客户端、绑定模型和整套测试；当前只保留AirScript方案。
2. 删除旧的单用户AirScript保存、读取、删除接口；统一使用多店铺模型。
3. 删除旧的 `/connections` 页面、模板和 `/api/connections/*` 接口。
4. 删除网页版中的PyQt界面、主题、桌面入口、桌面构建脚本和PyQt依赖。
5. 删除只剩测试调用的旧总量限制函数、异常别名和兼容方法。
6. 消除货代状态读取时重复访问数据库的逻辑。

### 日志和异常处理

1. 新增 `data/logs/web.log`，单文件5MB、保留5份滚动日志。
2. 记录登录、查询开始/完成、货代连接验证、AirScript回填摘要和未预期异常。
3. 日志不记录密码、令牌、验证码、请求正文或FBA明细。
4. 用户可预期异常继续显示安全的中文错误；未预期异常返回统一提示并写入堆栈日志。

### 接口命名

当前业务接口统一为资源域：

- `/api/tracking/query`
- `/api/carriers/status`
- `/api/carriers/validation`
- `/api/carriers/anda`
- `/api/carriers/yitong/captcha-challenges`
- `/api/carriers/yitong/session`
- `/api/shops`
- `/api/shops/{shop_id}/validation`
- `/api/shops/{shop_id}/tracking-sync`

## 桌面版分离

- 网页版：`D:\CodexProject\g_team_ops_hub`
- 桌面版：`D:\CodexProject\FBA_Tracker_Desktop`

两套程序各自拥有：

- 独立源码
- 独立 `.venv`
- 独立 `data/app.db`
- 独立设置文件
- 独立构建脚本
- 独立EXE

分离时复制了数据库快照；分离后的新增、修改和删除不会同步到另一套程序。

## 当前保留风险

1. 三家货代使用其网页前端调用的接口，并非货代承诺长期稳定的正式开放API。网站改版后仍可能需要更新字段或地址。
2. 当前后端定位为单机、单进程使用。用户锁和验证码状态保存在进程内；以后部署多进程或多服务器时需要Redis、数据库任务锁和正式任务队列。
3. 长时间批量任务没有持久化任务表。程序中途退出后任务不能从断点恢复，后续应增加任务与明细表。
4. 数据库升级目前采用程序内兼容迁移。表结构继续增多后建议引入Alembic等版本化迁移机制。
5. DPAPI密文与当前Windows用户绑定。复制数据库到其他电脑或Windows账号后通常无法解密。
6. 测试环境存在Starlette TestClient对当前 `httpx` 兼容层的弃用警告，不影响运行；未来升级FastAPI测试依赖时应一并消除。
7. 超鸿官网当前只提供项目正在使用的HTTP明文接口；易通登录和业务接口同样使用HTTP，且无凭据测试发现其HTTPS证书已过期。两者都无法由客户端单方面安全升级；在不可信网络中存在被监听或篡改的上游风险。易通登录应只在可信公司网络进行，货代提供有效HTTPS或正式开放API后应优先迁移。
