# 物流模块优化说明（2026-09-12）

## 2026-09-17最新规则：物流14物理精简轨迹明细

物流结构版本14将轨迹明细从“隐藏旧列”改为用户预览确认后的物理迁移。主表22列和人工业务列不变；轨迹明细严格保留FBA号、货代、轨迹发生时间、标准阶段、标准节点、轨迹类型、物流轨迹原文、涉及计划、有效状态、异常状态、运输信息、系统更新时间12列，其余明细列及数据删除。表头整理不可由程序恢复，执行前必须保留WPS历史版本或工作簿副本。

事件去重不再依赖可见事件编号，而使用FBA、货代、规范化轨迹时间和原文组合键。旧表若存在不同事件编号但四项完全相同的记录，预览会报告重复行并停止，不自动删除数据。轨迹明细已有数据但缺少FBA号、货代、轨迹发生时间、物流轨迹原文任一定位列时拒绝整理。旧17列整表历史恢复时只投影12个保留字段，投影后若组合自然键重复则拒绝恢复；被删列无法恢复。表头预览使用表格内容双哈希校验并发变化，内容变化会拦截，不传输整张明细表。

截图中的`不支持的操作：headers_preview`确认是店铺Webhook仍运行旧物流脚本。程序现在给出物流14脚本精确路径和替换提示；用户需自行完整替换并保存`airscripts\FBA物流自动回填.js`后重新验证。Listing继续使用独立结构13脚本。以下版本13及更早段落均为历史记录。

## 2026-09-15交付补记

用户接管线上脚本替换。项目源码及根目录EXE均已更新为物流13，旧EXE保存在`.test-tmp/logistics13-release/previous-formal/`。143项本地测试及EXE启动检查已通过；未执行线上回填或声称完成共享表实测。使用前由用户完整替换物流13脚本，步骤和规则见《AirScript升级说明.md》顶部。下方“正式EXE仍为11”“等待浏览器恢复”等段落仅记录此前状态。

## 2026-09-14最新规则

当前源码为物流结构13，全量143项本地测试及独立EXE启动检查通过。线上替换受Edge网址安全检查阻塞，正式本机EXE暂保持兼容11；当前规则以《AirScript升级说明.md》顶部为准，下文为9月12日至13日的历史记录。

签收时间非空不覆盖，先补齐是否完成为“是”并跳过查询；空值只填系统确认的实际签收日期，再完成并清理明细。预计、预约、POD不构成实际签收。签收/完成写入均留审计快照，人工修改后条件恢复不会覆盖新值。

整理表头隐藏5个旧系统明细列，保留原数据和事件编号用于恢复与去重；“信息属性”改称“轨迹类型”。运营仅查看12列，其中只保留一个可见系统更新时间，重复获取不刷新它。未完成轨迹依主表FBA顺序排列，查询失败保留旧轨迹。

## 2026-09-13安达查询修复补充

此前安达访问阻塞已恢复，已取得官网脚本及真实接口返回。新版订单列表按请求中的`fields`选择返回列；旧请求没有声明字段，订单虽然命中却没有`fbaCode`，旧程序将其误报为未找到。补充物流必需字段后，用户截图中的13个安达FBA均正确匹配，完整轨迹共79条。
根目录源码已修复`g_team_ops/client.py`；正式修复程序从独立版本11源代码副本构建，仅增加安达字段选择，保留当前库存销售R3.0修复，**不要求升级线上AirScript**。版本12的表头、明细整理等优化仍保持独立待发布，不能与此次版本11修复混淆。下文“安达尚未适配”描述的是9月12日历史状态，以本节和开发日志第28节为准。

## 2026-09-13安达查询修复补充

此前安达访问阻塞已恢复，已取得官网脚本及真实接口返回。新版订单列表按请求中的`fields`选择返回列；旧请求没有声明字段，订单虽然命中却没有`fbaCode`，旧程序将其误报为未找到。补充物流必需字段后，用户截图中的13个安达FBA均正确匹配，完整轨迹共79条。
根目录源码已修复`g_team_ops/client.py`；正式修复程序从独立版本11源代码副本构建，仅增加安达字段选择，保留当前库存销售R3.0修复，**不要求升级线上AirScript**。版本12的表头、明细整理等优化仍保持独立待发布，不能与此次版本11修复混淆。下文“安达尚未适配”描述的是9月12日历史状态，以本节和开发日志第28节为准。

## 交付状态

本轮是物流模块内优化，主表22列保持不变。源码和本地回归已完成一轮验证，独立测试EXE置于`.test-tmp/logistics-v12-preview/dist/`，不替换正式目录EXE。
安达新官网接口仍未完成实测适配：浏览器访问曾被管理员策略验证阻止，随后浏览器工具启动失败；未绕过限制，未猜测接口或声称查询恢复。三家历史响应测试不代表当前线上接口可用。

## 表头与时间

仍使用：店铺、FBA号、货代、运输工具/班次、当前阶段、当前节点、最新轨迹时间、货代最新路由信息、当前异常、提货、预计出发、开船（机）时间、预计到达、到港、预计送达、提取派送、签收时间、POD状态、是否完成、数据状态、物流最后更新时间、备注。

预计出发/到达/送达仅提取明确计划；完成实际运输也不会把实际日期补入预计列。计划更新使旧计划显示已被更新；明确取消后清空对应预计列并提示待更新。接口单纯缺字段则保留原表值。
提货只认实际提货，不用入仓日期替代。提取派送保留第一次实际派送；提柜、预约、海外仓入库不是派送。实际日期已有人填值且与本次解析不同，保留并提示冲突。旧系统按不同口径填写的提货/派送日期需人工核对，不做批量修正。
AMZ签收登记日期为人工维护的亚马逊后台登记日期，不属于物流自动写入列。计费重、费用、价格、备注和其他业务列不自动填写。

## 异常、完成与详情

当前异常按查验、班次、派送、货损、丢失等分类保留；海关放行不能清除丢失等其他异常。恢复或后续同类记录更新后，旧轨迹保留并标注状态。仍需谨慎核对货代自由文本，无法识别的新措辞应补充脱敏案例和测试，不能凭词语猜实际日期。
是否完成继续由用户维护，任何非空内容都停止一键查询，填写“否”也会跳过。货代签收/POD不会自动更改它。手动查询沿用原有入口和范围。
轨迹详情只保留主表活跃货件，完成或从主表移除的FBA会在下次任务清理。未完成但查询失败的FBA保留旧详情；排序按主表顺序，组内按时间。非法FBA说明行保留。完整轨迹为空或失败时不写入残缺快照；相同最新路由的缓存6小时后重新读取。

## 表头补齐与操作保护

页面先预览各子表缺列，再由用户确认追加。已有列名、列顺序、内容保留；已用区域右侧追加，不占用无表头但有数据的列。别名冲突或超过100列拒绝处理。初始化只设置系统日期显示和路由换行，不更改人工业务数据；记录操作历史，不支持通过恢复删除新增表头。
沿用账号隔离、CSRF、店铺配置与跨模块工作簿锁。自动查询前后的FBA顺序、货代、完成状态变化会阻止写入。多批写入连续校验明细快照，失败不盲目重试。外部WPS无法提供跨多次请求事务，断网时可能部分写入，应先核对操作历史。
主表仍按单元格条件恢复。新版明细整理使用整体数据快照，恢复要求当前明细等于操作后的明细；人工更改或表头改变会阻止覆盖。含公式或合并单元格、超出100列或10万个单元格的明细拒绝整理。保存自定义值及数字格式；批注、字体、填充等其他样式不在快照恢复范围内。

## 后续验收

需要恢复可访问货代官网的环境，逐家核对真实字段、安达新登录与查询协议，以及三家样本FBA完整轨迹。若修复涉及公共货代连接/账号能力，先列出文件和兼容影响再征得确认。然后在共享表副本验证脚本版本12、缺列表头、完成清理、异常恢复及中断恢复。当前不应把安达列为“修复完成”。
本轮未创建定时任务或发送邮件/微信；今后提醒应基于成功查询后的业务变化，失败和仅查询时间刷新不冒充物流更新。
