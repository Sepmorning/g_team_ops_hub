# 界面与多用户架构

## 当前选择

当前版本继续采用 PyQt5 桌面程序，使用 SQLite 保存系统数据。已经稳定的安达、超鸿、易通和 AirScript 业务服务保持独立，不与界面代码绑定。

现在采用桌面架构的原因：

- 当前明确只在一台 Windows 电脑上继续开发和使用；
- 可以直接复用已经验收的物流查询模块；
- 易通验证码和本机加密更容易可靠处理；
- 不需要提前维护服务器、域名、HTTPS和部署环境。

如果以后需要多台电脑同时登录，应迁移为“客户端/浏览器 + 后端服务 + PostgreSQL”。届时可以复用现有货代查询服务、输入解析、结果模型和 AirScript 客户端；需要替换的主要是界面层、本机 DPAPI 密钥管理和 SQLite 数据访问层。

## 页面框架

- 登录页：应用账号登录。
- 概览：后续模块入口和状态摘要。
- 物流查询：保留现有三家货代批量查询与共享表同步。
- 我的连接：当前用户自己的货代账号和 AirScript 设置。
- 账号管理：仅管理员可见。

后续业务模块应作为新的独立页面加入左侧导航，不要继续把所有功能堆在物流查询页面中。

## 权限

管理员可以创建管理员或普通用户、启用或停用其他用户、重置临时登录密码，并查看账号状态和最后登录时间。

管理员不能通过界面查看其他用户的货代密码、货代会话、WPS密钥、AirScript脚本令牌、共享表链接或其他业务配置。管理员重置密码后，用户首次登录必须自行修改临时密码。

## 数据隔离

每个用户拥有随机生成的 `profile_id`。货代账号、货代会话、WPS配置和AirScript配置均通过 `profile_id` 隔离。登录成功后只创建绑定当前用户的数据库访问对象。

应用登录密码使用 PBKDF2-SHA256 和独立随机盐保存为不可逆哈希。货代密码、会话和脚本令牌继续使用 Windows DPAPI 保存为密文，敏感值不会自动回显。

首次升级会先在 `data/backups` 创建旧数据库一致性备份，再将原来 `default` 配置归属到首个管理员账号。

