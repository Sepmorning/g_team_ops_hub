# 界面与多用户架构

## 当前技术选择

正式项目是FastAPI网页版，本机运行`FbaTrackerWeb.exe`后通过浏览器访问。
PyQt桌面验证版已经独立到`D:\CodexProject\FBA_Tracker_Desktop`，不属于本仓库，
网页版开发不得读写或修改桌面版。

当前网页版采用：

- FastAPI后端；
- Jinja2服务端模板和原生JavaScript；
- SQLite本机数据库；
- Windows DPAPI加密货代密码、会话和AirScript令牌；
- 模块化单体目录，按账号、货代连接、店铺、物流和库存销售划分路由；
- 单进程、仅监听`127.0.0.1`的本机运行方式。

应用创建入口位于`anda_tracker.web.factory`，`anda_tracker.web.app`仅保留兼容
导出。后续模块开发规范见[架构与模块开发规范.md](架构与模块开发规范.md)。

## 当前页面

- 登录与初始化：系统账号和首次管理员；
- 工作台概览：模块入口；
- 物流查询：三家货代查询和共享表同步；
- 货代连接：当前用户自己的账号、验证码和连接状态；
- 我的店铺：多店铺共享表、模块脚本和国家站点映射；
- 库存销售表格：领星文件预览与Listing共享表更新；
- 账号管理：仅管理员可见。

新模块必须作为`anda_tracker.modules`下的独立业务模块加入导航和应用工厂，
不得继续向某个已有页面或`web.factory`集中堆放业务代码。

## 前端升级方向

当前没有使用Vue、React等前端框架。现有页面已经包含上传预览、复杂表格、店铺与
站点联动和长任务状态，后续整体优化界面时可以采用Vue 3和Vite。

迁移顺序必须是：

1. 先稳定模块化后端和API契约；
2. 将模板内联JavaScript按页面迁入独立静态文件；
3. 再逐页迁移Vue，优先工作台、我的店铺、库存销售和物流查询；
4. 保留原API和登录会话，避免同时修改业务算法；
5. 将Vite构建产物加入PyInstaller，运行EXE的用户不需要安装Node.js。

## 用户与数据隔离

每个用户拥有独立`profile_id`。货代账号、货代会话、店铺、模块连接、国家站点和
轨迹缓存均按该ID隔离。管理员可以管理系统账号，但不能查看其他用户的业务密码、
令牌或店铺配置。

系统登录密码继续使用PBKDF2-SHA256不可逆哈希；业务密钥使用DPAPI密文。
DPAPI与当前Windows用户绑定，因此本机数据库不能直接作为跨电脑部署的数据包。

## 未来服务器部署

当前本机模式继续使用SQLite最简单可靠。确定需要多人同时访问服务器后，再进行：

1. 以Repository接口为边界把SQLite迁移到PostgreSQL；
2. 使用Alembic或等价工具管理数据库版本；
3. 将进程内验证码、任务锁和状态缓存迁移到Redis；
4. 增加持久化任务、更新日志、数据库备份和回退记录；
5. 使用域名、HTTPS、反向代理和服务器密钥管理；
6. 为货代设置全局队列和限速，避免所有用户共享出口IP时并发冲击上游。

在完成这些能力前，不得把监听地址直接改为`0.0.0.0`供外网或公司多人访问。
