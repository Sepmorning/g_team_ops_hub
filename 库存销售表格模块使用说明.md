# 库存销售表格模块使用说明

## 1. 当前版本与边界

Listing AirScript结构版本13、计算模型`三窗口预测-v3`、默认规则版本R3.0。
物流AirScript仍为11，物流算法、数据库结构、账号、用户隔离、店铺映射和恢复接口不变。
本次仅新增“月销计算方案”这一可读业务列，初始化缺少时追加到已用区域右侧。参数与说明集中在现有`ListingRules`；同一工作簿的Listing子表共享规则。
候选权重未经真实后续销量验证，系统建议是近期销售水平外推，不是准确率承诺。

上传仅支持领星`.xlsx`，只读首个工作表，在前20行按名称寻找表头；20MB文件、100MB解压、20,000行安全限制不变。
文件及解析数据只在内存中保留20分钟预览，每位用户新预览替换旧预览。预览检查源文件和目标规则，不是逐格写入差异预览。
真正确认执行后才获取并持久化写前快照，然后校验前置条件、分批写入和回读记录实际差异。

## 2. 源文件与共享表字段

领星文件必需20个字段，顺序不限：

```text
MSKU ASIN 价格 FBA可售 FBA待调仓 FBA预留 FBA计划入库 FBA标发在途 7日销量 14日销量 30日销量 昨日广告费 7日广告费 14日广告费 30日广告费 7日销售额 14日销售额 30日销售额 Rating总数 评分
```

- `预留=FBA待调仓+FBA预留`；`在途=FBA计划入库+FBA标发在途`；不计FBA入库中、不可售。
- 优惠价为双端可选；两端存在才同步，源空值清除旧优惠价。
- 库存和价格缺失不覆盖旧值；周期销量、广告费、销售额缺失清空旧周期值。
- 负销量和累计倒挂保留原始数值并警告，由共享表显示数据异常、不预测；其他非数字或负价格/库存仍报告无效行。
- 文件内有效解析行MSKU重复时全部排除；目标MSKU重复、ASIN冲突、旧日期不覆盖，不自动新增商品行。

共享表按标准表头名称定位，任意列顺序；必需字段仍以Python `TARGET_HEADERS`及脚本`FIELD_DEFINITIONS`为准。
`优惠价`、`运营备注`、`产品属性`是可选列；缺少产品属性不新增列，按普通走势计算。有标准表头重复时停止。

人工维护列：MSKU、品名、ASIN、链接状态、产品属性、补货状态以及维护情况、前台情况、促销情况、广告情况、待操作、备注等自定义列。
系统只写原始指标、对应上次值及6个数字/文本结果列与8个下游公式列，不重排列，不覆盖人工列。

## 3. 业务状态

| 输入 | 系统建议月销 | 最终月销 / 覆盖 | 建议补货 |
|---|---|---|---|
| 链接留白（兼容在售）、补货留白 | 正常计算 | 默认系统值 | 正常计算 |
| 补货=停补 | 正常计算 | 保留需求和覆盖 | 留白 |
| 链接=即将下架 | 正常计算 | 保留需求和覆盖 | 留白 |
| 链接=停售、已删除、已删除-原因 | 0 | 最终月销0、覆盖留空 | 留白 |
| 链接=冻结 | 留空 | 暂停 | 留空 |
| 未知非空链接状态 | 留空 | 待核对 | 留空 |
| 未知非空补货状态 | 保留需求估算 | 待核对 | 留空 |

旧补货状态`正常补货`兼容留白；`清库存/停售`兼容停补；`新品观察/暂缓补货`保留暂停自动补货语义。
冻结优先于人工停补；停售/删除优先于普通数据规则。人工填写最终月销不能绕过补货禁用条件。
库存状态先显示退出销售、冻结待处理、停补，再判断普通库存情况，不催促已停补商品。

## 4. 月销算法与规则参数

```text
M1 = 7日销量 / 7 * 30
M2 = (14日销量 - 7日销量) / 7 * 30
M3 = (30日销量 - 14日销量) / 16 * 30
q = MIN(1, 30日销量 / 平滑参数)
场景估算 = M1*w1 + M2*w2 + M3*w3
系统建议月销 = ROUND(30日销量*(1-q) + 场景估算*q, 0)
```

三个窗口互不重叠；平滑是加权平均，不是再加一份销量。计算过程不取整，仅最终四舍五入。

| 参数位置 | 默认值 | 作用 |
|---|---:|---|
| A2 | 三窗口预测-v2 | 模型标识，不可改；防止R1参数误用于R2公式 |
| B4 | R3.0 | 自定义参数后手工升级规则版本；预览变化会阻止执行 |
| B5 | 20 | 小销量平滑参数，必须>0 |
| B6 | 30 | 高可信度最低30日销量，不小于B8 |
| B7 | 90 | 总库存目标覆盖天数，必须>0 |
| B8 | 10 | 低可信度销量门槛；低于此值趋势差异率留空，但仍分类走势 |
| B9 | 20% | 增长门槛，必须>0 |
| B10 | 25% | 三窗口稳定最大差幅，必须>0 |
| B11 | -20% | 下降门槛，必须在-100%与0之间 |
| B12 | 10% | 最近两周实际成交均价变化提示，必须>0 |
| B13 | 25% | 高广告费率，0%到100% |
| B14 | 5 | 补货向上取整的正整数倍数 |
| B15 | 30% | 最近两周广告投入变化提示，0%到100%之间 |
| B17:D17 | 40%/30%/30% | 平稳型 |
| B18:D18 | 60%/30%/10% | 近期增强型 |
| B19:D19 | 70%/20%/10% | 近期减弱型 |
| B20 | 10 | 广告变化最低金额，源文件币种，须与B15同时达到才降为低；同工作簿共用，跨币种需确认适用性 |

每行权重在0到100%之间且合计100%。参数不再按10/30件直接换整套权重，但走势阈值附近仍可能切换类型。

按下表顺序只命中一个场景；增长/下降门槛使用B9、B11：

| 顺序 | 条件 | 销量状态 | 类型 |
|---|---|---|---|
| 1 | 任一窗口非数字/缺失/负数/累计倒挂 | 数据异常 | 不预测 |
| 2 | 30日=0 | 无销量 | 0 |
| 3 | M1=0，30日>0 | 近期无销量 | 减弱 |
| 4 | M1>0，M2=0 | 近期启动／恢复出单 | 增强 |
| 5 | 三窗口均>0，MAX<=(1+B10)*MIN | 稳定 | 平稳 |
| 6 | M1>(1+B9)*M2 | 若M1>(1+B9)*M3为增长，否则近期反弹 | 增强 |
| 7 | M1<(1+B11)*M2 | 若M2>(1+B9)*M3为放量后回落，否则下降 | 减弱 |
| 8 | (M1+M2)/2>(1+B9)*M3 | 增长后高位维持 | 增强 |
| 9 | (M1+M2)/2<(1+B11)*M3 | 下降后低位维持 | 减弱 |
| 10 | 其他有效窗口 | 波动观察 | 平稳 |

趋势差异率继续显示`(M1-M3)/MAX(M3,1)`，不作为额外乘数。低销量仍能识别起量，q控制偶然订单被过度外推。

## 5. 产品属性、价格广告与可信度

产品属性允许留白、常规品、夏季品、冬季品；未知非空值提示核对并暂停系统预测。
季节品出现非稳定走势时在异常原因提示持续性待复核，不额外增加近期权重、不按月份乘季节系数。
没有逐日销量、活动起止、断货天数和季末日期，不能精确预测活动结束后的需求或本季到货窗口；确定不再补货时使用现有停补。

最近7日成交均价为7日销售额/7日销量，前7日为(14日销售额-7日销售额)/(14日销量-7日销量)。
只有两个销量窗口均>0、销售额完整非负且前期销售额>0时可比；变化绝对值达到B12时降为低。不可比不默认为稳定。
广告费按最近7日与前7日比较；数据必须完整、非负、累计不倒挂。变化比例达到B15（或零基期出现投入），并且两周绝对差额达到B20时，降为低。
广告状态仍优先报告高广告费；普通涨跌按同样两周窗口判断，超过门槛才显示涨跌。
高广告费率本身不单独降低可信度，也不增加或减少月销；评分条件格式保留，不直接进入预测。

| 可信度 | 条件 |
|---|---|
| 不适用 | 停售/已删除 |
| 不可评估 | 冻结、未知链接/产品属性、销量异常 |
| 低 | 销量<B8；无销量/近期无销量/启动/反弹/回落/波动；或FBA可售=0且30日有销量；均价明显变化、实质广告变化、季节品非稳定 |
| 高 | 未命中以上；稳定、销量>=B6、库存完整非负且FBA可售>0、均价和广告窗口可比且无明显变化 |
| 中 | 其他有效预测 |

这些等级是运营规则，不是统计概率。停补不单独降低等级。只有低可信度最终月销保留蓝色提示，评分红字/黄底等人工条件格式不修改。

## 6. 最终月销与补货

最终月销直接填入与系统建议相同的数字，不含公式，用户可手工修改。下一次成功同步该行、初始化或重算会恢复为最新系统建议；未匹配/冲突/旧日期跳过行不重置。
已有非标准公式不自动静默覆盖：验证提示先初始化当前子表。

在库覆盖=(FBA可售+预留)/最终月销*30；含在途覆盖=(FBA可售+预留+在途)/最终月销*30。
最终月销为空/非数字/负数、库存缺失/非数字/负数或链接冻结时不产生相应建议；0需求或库存足够时建议补货量实际返回空字符串，覆盖留空。
先检查经营状态，再计算`ROUNDUP(MAX(0,最终月销/30*B7-总库存)/B14,0)*B14`。
不推测在途到货日期，不保证中间不断货；本次不修改物流模块。

## 7. 升级、预览、恢复

1. 保留WPS历史版本或工作簿副本。初始化是显式结构维护，会恢复规则参数与当前子表全部标准公式，不在日常导入的逐格恢复批次中。
2. 完整替换线上Listing脚本为本项目结构版本13；连接不变时无需修改令牌。
3. 通过库存页“初始化 / 检查规则配置”初始化当前子表；旧中文规则表会迁移，无法解锁旧保护表时归档保留。
4. 同工作簿其他Listing子表要分别初始化。R2规则已更新而子表仍是旧公式时，验证会拒绝回填，避免新参数与旧公式混用。
5. 原始指标、日期、人工列及评分条件格式保留；只追加缺少的“月销计算方案”。初始化会恢复人工最终月销及自定义公式。
6. 上传预览、确认回填；切换文件、日期、店铺、国家或初始化会使当前网页预览失效。同一预览重试复用幂等键。
7. 操作历史保存导入写前快照、公式文本和真实差异，按当前用户/店铺/模块最近可恢复批次进行条件恢复；冲突不覆盖。

日常导入按用户+店铺租约锁跨模块串行，每批50个MSKU；同日只修正当前值，新日期滚动旧值，旧日期跳过。
恢复和回读不因规则表后来无效而被阻止。不同电脑或不同用户指向同一工作簿，不等同于全局分布式锁。

## 8. 看懂月销与精简风险

“销量状态”展示走势；“月销计算方案”展示实际采用的规则表权重，顺序为M1/M2/M3。例如“平稳加权 40/30/30”“近期减弱 70/20/10＋低样本平滑”。退出销售和无销量显示归零，冻结或无效输入显示暂停预测。中间量仅在系统内部计算，不新增辅助列。

异常原因只显示最高优先级的预测风险：阻断输入、供给受限、无销量、样本少、恢复出单、均价变化、实质广告变化、季节变化、回落/反弹/波动。高、中、不适用留白；低和不可评估显示具体原因。停补、广告费率高等交由已有状态列展示。它是主因摘要，不是完整风险清单。

高可信度默认采用，无需逐行核算，但不是未来销量保证。中为参考采用，低需重点复核。预测输出统一为数字/文本，手动改销量、链接/补货状态、产品属性或规则后点击“重新计算当前子表”。最终月销可手改，下游覆盖/补货公式即时联动，停补、冻结等限制立即生效。异常原因和方案自动换行。工作台“查看计算详情”只读试算，展示每个MSKU三窗口折算值、权重、响应比例、加权结果、取整值和完整风险。

## 9. 重新计算当前子表

无需重新上传Listing。按钮保留现有规则，只重算预测结果并恢复最终月销为系统建议，不改原始指标、日期和历史列。写前提示会覆盖人工最终月销。每批50行，重复MSKU停止；规则或输入在读写间变化停止。使用现有用户/店铺锁、幂等键、写前快照、写后回读和条件恢复；部分失败保留可追溯状态。查看计算详情不写共享表，显示当前输入的试算值，不能冒充已保存结果。
