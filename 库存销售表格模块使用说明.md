# 库存销售表格模块使用说明

## 1. 模块边界

- Listing库存销售模块沿用“我的店铺”中的金山共享表链接。
- Listing模块使用独立的AirScript Webhook和脚本令牌，不调用、不修改物流查询脚本。
- 上传文件只在内存中保留20分钟预览，不写入项目目录或数据库。
- Webhook、脚本令牌、店铺与国家映射只保存在本机`data/app.db`；令牌使用Windows DPAPI加密。
- 共享表以MSKU为唯一匹配键。目标表MSKU重复、上传文件MSKU重复或ASIN冲突时不写入该MSKU。
- MSKU、品名、ASIN、Listing状态、价格、链接情况、库存情况、广告情况、运营备注均为人工字段，系统不会覆盖。`优惠价`为可选系统字段，只有领星文件和共享表都存在该表头时才同步。

## 2. 最终共享表表头

以下仅为便于阅读和新建表格的推荐完整顺序，其中 `优惠价` 可以整列省略，也可以插在共享表的任意列。现有示例的表头位于第2行，因此公式示例从第3行开始。程序会在前12行按表头名称动态定位每一列，不依赖列字母、不移动列、不写入公式。对于已有共享表，直接插入“优惠价”列即可，WPS会自行调整原公式引用；不需要按本说明手工右移公式列。

```text
MSKU	品名	ASIN	Listing状态	价格	优惠价	本次数据日期	上次数据日期	评分	评论数	上次评分	上次评论数	昨日广告费	上次昨日广告费	FBA可售	预留	在途	上次FBA可售	上次预留	上次在途	7日销量	14日销量	30日销量	上次7日销量	上次14日销量	上次30日销量	7日均销	14日均销	30日均销	7日折算月销	月销差异	月销差异率	销量趋势	系统建议月销	最终补货月销	在库覆盖月数	含在途覆盖月数	建议补货量	链接情况	库存情况	广告情况	运营备注	本次更新时间
```

### 字段定义

| 列 | 表头 | 维护方式 | 定义 |
|---|---|---|---|
| A | MSKU | 人工 | 唯一匹配键；同一子表内必须唯一 |
| B | 品名 | 人工 | 便于运营识别的产品名称 |
| C | ASIN | 人工 | 系统只用来校验MSKU是否匹配错误，不覆盖 |
| D | Listing状态 | 人工 | 如在售、停售、待清货等 |
| E | 价格 | 人工 | 领星文件虽有价格列，本模块不会覆盖 |
| F | 优惠价 | 可选系统 | 两份表都有该表头时同步；领星该MSKU为空会清除旧优惠价 |
| G | 本次数据日期 | 系统 | 本次导出数据所属日期 |
| H | 上次数据日期 | 系统 | 发生新日期更新时，由原本次日期滚动而来 |
| I:J | 评分、评论数 | 系统 | 领星“评分”和“Rating总数” |
| K:L | 上次评分、上次评论数 | 系统 | 新日期更新时保存原I:J |
| M | 昨日广告费 | 系统 | 领星“昨日广告费” |
| N | 上次昨日广告费 | 系统 | 新日期更新时保存原M |
| O | FBA可售 | 系统 | 领星“FBA可售” |
| P | 预留 | 系统 | 领星“FBA待调仓” + “FBA预留” |
| Q | 在途 | 系统 | 领星“FBA计划入库” + “FBA标发在途” |
| R:T | 上次FBA可售、上次预留、上次在途 | 系统 | 新日期更新时保存原O:Q |
| U:W | 7日、14日、30日销量 | 系统 | 领星对应周期销量 |
| X:Z | 上次7日、14日、30日销量 | 系统 | 新日期更新时保存原U:W |
| AA:AC | 7日、14日、30日均销 | 公式 | 周期销量除以对应天数 |
| AD | 7日折算月销 | 公式 | 7日销量 × 4 |
| AE | 月销差异 | 公式 | 7日折算月销 - 30日销量 |
| AF | 月销差异率 | 公式 | ABS(月销差异) / 30日销量 |
| AG | 销量趋势 | 公式 | 以25%为“差距较大”阈值进行提示 |
| AH | 系统建议月销 | 公式 | 两个口径都有值时取平均；单一口径为0时使用另一口径 |
| AI | 最终补货月销 | 系统后人工 | 每次系统更新先重置为系统建议月销；之后可人工修改 |
| AJ | 在库覆盖月数 | 公式 | (FBA可售 + 预留) / 最终补货月销 |
| AK | 含在途覆盖月数 | 公式 | (FBA可售 + 预留 + 在途) / 最终补货月销 |
| AL | 建议补货量 | 公式 | 补至3个月覆盖所需数量，最低为0 |
| AM:AP | 链接情况、库存情况、广告情况、运营备注 | 人工 | 运营判断与备注 |
| AQ | 本次更新时间 | 系统 | 实际完成共享表写入的时间 |

`FBA入库中`与`FBA不可售`只用于确认领星文件结构，不写入共享表，也不参与预留或在途计算。

## 3. Excel/WPS公式

以下公式只是按“新建空白表、采用第2节推荐顺序、F列为优惠价、第2行为表头、第3行为首条数据”的示例。把公式粘贴到第3行对应单元格，再向下填充到所有Listing行。`AI 最终补货月销`不能放公式，由程序在每次导入时写入AH的同等计算值，之后允许人工修改。已有共享表插入或移动“优惠价”时，应保留原公式并让WPS自动调整引用；程序只按表头写业务数据，不读取这些示例列字母，也不会改写公式。

| 单元格 | 表头 | 公式 |
|---|---|---|
| AA3 | 7日均销 | `=IF(U3="","",U3/7)` |
| AB3 | 14日均销 | `=IF(V3="","",V3/14)` |
| AC3 | 30日均销 | `=IF(W3="","",W3/30)` |
| AD3 | 7日折算月销 | `=IF(U3="","",U3*4)` |
| AE3 | 月销差异 | `=IF(OR(AD3="",W3=""),"",AD3-W3)` |
| AF3 | 月销差异率 | `=IF(OR(AD3="",W3=""),"",IF(W3=0,IF(AD3=0,0,""),ABS(AE3)/W3))` |
| AG3 | 销量趋势 | `=IF(OR(AD3="",W3=""),"数据不足",IF(AND(AD3=0,W3=0),"暂无销量",IF(AND(W3=0,AD3>0),"新品/销量启动",IF(AF3<=25%,"基本稳定",IF(AD3>W3,"近期加速","近期放缓")))))` |
| AH3 | 系统建议月销 | `=IF(OR(AD3="",W3=""),"",IF(AND(AD3=0,W3=0),0,IF(W3=0,ROUND(AD3,0),IF(AD3=0,ROUND(W3,0),ROUND((AD3+W3)/2,0)))))` |
| AJ3 | 在库覆盖月数 | `=IF(OR(AI3="",AI3=0),"",(O3+P3)/AI3)` |
| AK3 | 含在途覆盖月数 | `=IF(OR(AI3="",AI3=0),"",(O3+P3+Q3)/AI3)` |
| AL3 | 建议补货量 | `=IF(OR(AI3="",AI3=0),"",MAX(0,ROUNDUP(AI3*3-O3-P3-Q3,0)))` |

推荐格式：

- F、M:N可设为金额或保留2位小数；G:H设为日期`yyyy-mm-dd`，AQ设为文本或日期时间。
- AA:AC保留2位小数，AF设为百分比，AJ:AK保留1位小数。
- 可对AF大于25%、AG为“近期加速/近期放缓”设置条件格式。
- 可分别折叠K:L、N、R:T、X:Z这些“上次值”列，日常只看本次值和独立分析列。

## 4. 数据滚动规则

1. **新日期**：只对本次文件提供的滚动字段执行“本次值 → 上次值”，再写入新本次值；G更新为新日期，H保存原G。优惠价不维护上次值。
2. **同一日期重新上传**：修正本次值，不再次滚动上次值。可用于更正当天重新导出的文件。
3. **更早日期**：该MSKU跳过，防止历史文件覆盖新数据。
4. **空白与0不同**：普通领星字段为空时不覆盖共享表现值；数字0是有效数据。优惠价例外：两份表都有优惠价列时，领星该MSKU为空表示优惠结束，会清除旧优惠价。
5. 每次成功导入都把AI重置为程序计算的系统建议月销。人工可在导入完成后修改AI，AJ:AL会立即随公式重算；下一次导入时AI再次被系统重置。
6. 文件中新增的未知表头会被忽略；当前17个领星标准表头必须全部存在，`优惠价`为第18个可选表头，顺序可以改变。

## 5. 首次启用需要操作的地方

1. 先备份金山共享表。
2. 在每个Listing子表（如`纯粹-美国`、`纯粹-加拿大`）替换为本说明第2节表头，MSKU必须唯一。
3. 保留并检查人工字段数据；把第3节公式填入首条数据行并向下填充。
4. 在同一个金山共享表中新建一份**独立共享脚本**，不要替换现有物流脚本。
5. 把项目文件`airscripts/Listing库存销售自动回填.js`的全部内容复制到新脚本并保存，然后复制该脚本自己的Webhook和令牌。
6. 启动新版网页源码；如果平时使用`FbaTrackerWeb.exe`，需要先重新执行`build_web_exe.ps1`，旧EXE不包含本模块。
7. 进入“我的店铺”，打开对应店铺的“配置脚本与站点”，一次性保存Listing脚本Webhook和令牌。
8. 点击“扫描共享表”。系统会识别`Listing前缀-国家`、`国家代码-FBA`和`国家代码-轨迹明细`；店铺显示名称与前缀不同时确认一次，确认后永久保存，检查结果仍可手工修正。
9. 在站点列表点击“测Listing”，确认系统按名称识别42个必需表头；添加优惠价后会额外识别该可选表头。
10. 进入“库存销售表格”，只选择店铺和国家，上传领星`.xlsx`，确认数据日期、优惠价识别状态、聚合库存、销量和建议月销，再点击“确认更新共享表”。
11. 更新完成后查看未找到MSKU、重复MSKU、ASIN冲突和旧日期跳过清单，并人工复核AG、AH、AI及补货结果。

## 6. 常见停止原因

- **缺少表头**：按本说明复制完整表头；程序不会依赖列号猜测。
- **重复含义表头**：例如同时存在`MSKU`和`SKU`，为避免写错列会停止。
- **表内MSKU重复**：先在对应Listing子表去重。
- **ASIN冲突**：同一MSKU的人工ASIN与上传ASIN不同，先核对映射。
- **旧日期跳过**：上传日期早于该行当前日期。
- **脚本版本过旧**：重新复制项目内最新Listing脚本。
- **无法扫描国家站点或优惠价不更新**：Listing脚本需要升级到结构版本3；旧版脚本不支持可选优惠价。
- **文件内FBM行FBA字段为空**：空白库存不会清空共享表；销量、广告等非空字段仍可更新。
