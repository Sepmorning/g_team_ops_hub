# 库存销售表格模块使用说明

## 1. 模块边界

- 模块复用“我的店铺”中的用户、店铺、国家站点、共享表链接与Listing脚本连接，不重复填写公共配置。
- Listing脚本与物流脚本相互独立；升级Listing脚本不会修改物流版本11协议。
- 上传的领星文件只在内存中保留20分钟预览，不写入项目目录或数据库。
- 回填始终按表头名称和MSKU定位，不依赖列字母或固定列顺序；目标表MSKU重复、文件内MSKU重复、ASIN冲突或旧日期数据均不会覆盖。
- 每次正式回填进入“操作历史”，包含写前快照、写前条件校验、写后回读、并发租约和条件恢复。WPS历史版本仍建议作为文件级第二重保险。
- 本版本的销量预测、状态、覆盖天数和建议补货量均由WPS公式计算。系统只写原始数据并维护标准公式，调整业务参数不需要修改Python代码。

## 2. 领星文件要求

支持领星Listing `.xlsx` 全字段导出，列顺序可以变化。以下20个实际使用字段必须存在：

```text
MSKU	ASIN	价格	FBA可售	FBA待调仓	FBA预留	FBA计划入库	FBA标发在途	7日销量	14日销量	30日销量	昨日广告费	7日广告费	14日广告费	30日广告费	7日销售额	14日销售额	30日销售额	Rating总数	评分
```

`优惠价`为可选字段。领星文件和共享表同时存在该表头时才同步；领星单元格为空表示优惠结束，会清除共享表旧优惠价。

口径固定如下：

- `预留 = FBA待调仓 + FBA预留`。
- `在途 = FBA计划入库 + FBA标发在途`。
- `FBA入库中`、`FBA不可售`、当前有效库存、已发在途及其他全字段导出列不参与本公司补货口径，也不是必需字段。
- 完整导出中的已知非使用列会静默忽略；真正未知的新列只在预览中提示，不会写入共享表。
- 若出现`7日销量 > 14日销量`、`14日销量 > 30日销量`、负销量或窗口缺失，原始数据仍可预览和回填，但共享表会显示“数据异常”，不会自动给出补货月销。

## 3. Listing子表表头

以下是推荐顺序。程序允许任意调整顺序、插入人工列或保留其他业务列，但同一标准字段不能重复。`优惠价`和`运营备注`都可整列省略，也可放在任意位置。

```text
MSKU	品名	ASIN	补货状态	价格	优惠价	本次数据日期	上次数据日期	评分/评论数	上次评分/评论数	昨日广告费	上次昨日广告费	7日广告费	14日广告费	30日广告费	30日广告费率	FBA可售	预留	在途	上次FBA可售	上次预留	上次在途	7日销量	14日销量	30日销量	上次7日销量	上次14日销量	上次30日销量	7日销售额	14日销售额	30日销售额	30日实际成交均价	趋势差异率	销量状态	预测可信度	异常原因	系统建议月销	最终补货月销	在库覆盖天数	含在途覆盖天数	建议补货量	链接状态	库存状态	广告状态	运营备注	本次更新时间
```

维护责任：

- 人工基础列：`MSKU`、`品名`、`ASIN`、`补货状态`、`链接状态`；`运营备注`为可选人工列。
- 系统原始列：价格、优惠价、日期、评分/评论数、广告费、库存、销量、销售额及对应上次值。
- 系统公式列：`30日广告费率`、`30日实际成交均价`、`趋势差异率`、`销量状态`、`预测可信度`、`异常原因`、`系统建议月销`、`最终补货月销`、两个覆盖天数、`建议补货量`、`库存状态`、`广告状态`。
- `广告状态`由系统公式维护；若需记录特殊广告安排，写入`运营备注`，不要覆盖公式列。
- `链接状态`完全由人工自由填写，例如高退标签、链接异常或促销中；它不参与可信度、异常原因、月销、库存和补货公式。
- `最终补货月销`默认是公式。人工输入静态数字只覆盖当前这次运营判断；下一次正式数据回填或再次点击规则初始化时会恢复标准公式，避免新销量数据沿用上一次人工月销。

旧表迁移时，`Listing状态`不能直接当作新的`补货状态`使用；应先备份并人工核对旧值，再按第6节枚举迁移。评分和评论数需合并为`评分/评论数`，覆盖月数列改为覆盖天数列。

## 4. `ListingRules`规则工作表

每个共享工作簿只需要一张标签名为`ListingRules`的规则工作表，同一工作簿内所有国家Listing子表共同引用；A1标题仍显示“G组运营工作台｜库存销售规则配置”。版本5曾使用中文标签`规则配置`，但真实WPS文件证明跨表公式会产生`#NAME?`。版本10点击“初始化 / 检查规则配置”时会自动把旧标签迁移为`ListingRules`，恢复系统默认参数和逐项规则说明，并为已存在MSKU的行重新安装标准公式。

| 单元格 | 参数 | 默认值 | 说明 |
|---|---|---:|---|
| B4 | 规则版本号 | R1.0 | 每次改变业务规则后人工升级，预览和操作历史会记录该版本 |
| B5 | 低销量分界 | 10 | 30日销量低于10使用低销量权重 |
| B6 | 高销量分界 | 30 | 30日销量达到30使用高销量权重 |
| B7 | 目标覆盖天数 | 90 | 正常补货的含在途目标 |
| B8 | 趋势判断最低销量 | 10 | 低于该值显示“低销量观察” |
| B9 / B10 | 增长 / 快速增长门槛 | 20% / 50% | 趋势状态阈值 |
| B11 / B12 | 下降 / 快速下降门槛 | -20% / -50% | 趋势状态阈值 |
| B13 | 高广告费率门槛 | 25% | 30日广告费 ÷ 30日销售额 |
| B14 | 补货取整倍数 | 5 | 建议补货量向上取整到该正整数的倍数 |
| B17:D17 | 低销量M1/M2/M3权重 | 30% / 25% / 45% | 合计必须100% |
| B18:D18 | 中销量M1/M2/M3权重 | 40% / 30% / 30% | 合计必须100% |
| B19:D19 | 高销量M1/M2/M3权重 | 50% / 30% / 20% | 合计必须100% |

低销量分界、高销量分界、目标覆盖天数和其他参数不适合写死在每行公式里。集中配置的好处是只改一次、所有店铺公式立即引用新值、可显示规则版本并减少漏改。额外工作表不会增加日常导入步骤。

版本10在A22:D71按“一种结果一行”写入精确说明：高/中/低预测可信度、每一种销量状态、异常原因、库存状态和广告状态都有独立条件，不再用笼统概述代替。数字门槛和B17:D19权重是实际计算输入；说明文字仅供审计阅读，不会被当作可执行公式。根据金山文档兼容性和最终决定，规则表不设置工作表保护、单元格锁定或可编辑区域，所有单元格可直接编辑。修改参数后必须升级B4规则版本号，预览后的版本并发校验仍会阻止旧规则继续执行。日常上传会继续引用修改后的参数，不会重置。只有主动点击“初始化 / 检查规则配置”才恢复系统默认参数、说明和标准公式，因此执行前必须确认不再需要自定义规则。

参数校验规则：低分界必须小于高分界，目标天数必须大于0，趋势阈值顺序必须合理，广告费率必须在0%到100%之间，补货倍数必须是正整数，三档权重必须各自合计100%。配置无效或任一Listing公式出现`#NAME?`等错误时，网页会停止正式回填并要求先修复。

## 5. 预测与补货公式口径

三个非重叠窗口先统一为30天速度：

```text
M1 = 7日销量 / 7 × 30
M2 = (14日销量 - 7日销量) / 7 × 30
M3 = (30日销量 - 14日销量) / 16 × 30
```

`系统建议月销`只使用一次M1/M2/M3，不会再与30日销量二次混合：

- 30日销量 < 10：`M1×30% + M2×25% + M3×45%`。
- 10 ≤ 30日销量 < 30：`M1×40% + M2×30% + M3×30%`。
- 30日销量 ≥ 30：`M1×50% + M2×30% + M3×20%`。

边界与权重均引用`ListingRules`，以上只是默认值。最终显示四舍五入到整数。

`趋势差异率 = (M1 - M3) / MAX(M3, 1)`，M1和M3都已经换算成相同的30天速度。例如7/14/30日销量为14/25/35时，M1=`14÷7×30=60`，M3=`(35-25)÷16×30=18.75`，趋势差异率为`(60-18.75)÷18.75=220%`。直接计算`(14-10)÷10=40%`会把7天总量与16天总量混合比较：稳定为每天1单时也会得到`(7-16)÷16=-56.25%`并被误判快速下降，因此不采用。只有30日销量达到趋势判断最低销量时才判断趋势；默认规则为：大于50%快速增长，大于20%增长，-20%至20%（含边界）稳定，小于-20%下降，小于-50%快速下降。低于门槛显示`低销量观察`，全零显示`无销量`，窗口倒挂显示`数据异常`；M3为0但M1大于0显示`近期启动`。

`30日实际成交均价 = 30日销售额 / 30日销量`；`30日广告费率 = 30日广告费 / 30日销售额`。实际成交均价当前不进入补货公式，它的作用是与当前价格/优惠价对照，发现历史折扣、Coupon、组合装变化或销售额口径异常。2026-08-22纯粹美国站测试中有3个SKU偏差超过5%，因此暂时保留为人工诊断指标。系统不擅自对促销、优惠券、降价或广告投入做乘数修正，异常链接由运营人工决定最终月销。

覆盖和补货使用日均需求计算，但主决策值仍显示为月销：

```text
在库覆盖天数 = (FBA可售 + 预留) / 最终补货月销 × 30
含在途覆盖天数 = (FBA可售 + 预留 + 在途) / 最终补货月销 × 30
理论缺口 = MAX(0, 最终补货月销 / 30 × 目标覆盖天数 - FBA可售 - 预留 - 在途)
建议补货量 = ROUNDUP(理论缺口 / 补货取整倍数, 0) × 补货取整倍数
```

默认补货取整倍数为5，因此13取15、5取5、1至4取5，理论缺口为0仍取0。若公司以后改变分仓倍数，直接修改`ListingRules!B14`并升级B4规则版本号。

## 6. 状态优先级与人工决策

`补货状态`建议设置下拉值：`正常补货`、`清库存`、`停售`、`新品观察`、`暂缓补货`。

- 清库存、停售：建议补货量为0。
- 新品观察、暂缓补货：不自动推荐，等待人工填写最终补货月销或调整状态。
- 正常补货或空白：按规则计算；快速增长、快速下降、近期启动、数据异常或缺货会降低可信度，但最终补货月销仍自动等于系统建议月销，以便继续计算覆盖天数和建议补货量。

`链接状态`不限定枚举，完全由人工自由填写；促销、高退标签、低评分等内容只用于人工阅读，不影响任何系统公式。

库存年龄不直接进入补货公式。若365天以上库存代表不再继续销售，应把`补货状态`设为`清库存`，由业务状态而不是隐藏系数决定不补货。

### 6.1 系统建议月销与最终补货月销

- `系统建议月销`按第5节三档权重计算；30日销量只选择档位，不会再次混入预测。
- `最终补货月销`默认等于系统建议月销。
- `补货状态=清库存/停售`时最终月销和建议补货量为0。
- `新品观察/暂缓补货`时最终月销留空，等待人工判断；预测可信度为低时仍自动填写系统建议月销。
- 只有预测可信度为低的`最终补货月销`使用条件格式填充“蓝色，着色1，淡色60%”（#9DC3E6），提醒运营复核；中可信度不着色，该提示色不改变数值。
- 运营人工在`最终补货月销`输入静态数字后，可立即驱动当前覆盖天数和补货量。下一次正式回填或规则初始化会恢复公式，不保留上一次人工判断。

### 6.2 异常原因

`异常原因`由WPS公式按条件拼接，可同时出现多项：销量窗口倒挂或缺失显示`销量窗口异常`；30日无销量显示`无销量`；低于趋势最低销量显示`低销量`；近期启动、快速增长或快速下降显示`趋势突变`；FBA可售为0但30日有销量显示`当前无FBA可售`；广告状态为高广告费显示`广告依赖高`；非正常补货状态会原样加入原因。`链接状态`不再参与。该列只解释风险，不自动修改预测值。

### 6.3 库存状态

`库存状态`完全由WPS公式计算：最终月销为空或0为`数据不足`；FBA可售加预留为0时为`缺货`；在库覆盖天数小于30为`库存紧张`；含在途覆盖天数低于目标天数为`需要补货`；目标天数至目标天数加30为`库存健康`；再高为`库存偏高`。

### 6.4 广告状态

`广告状态`完全由WPS公式计算。7日或30日广告数据缺失为`数据不足`；30日广告费为0为`无广告`；30日广告费率达到`ListingRules!B13`（默认25%）为`高广告费`。其余SKU比较最近7日广告日均与第8—30日广告日均：比值大于1.2为`广告费上涨`，小于0.8为`广告费下降`，否则为`正常`。广告状态用于诊断和可信度，不直接给预测乘系数。

### 6.5 预测可信度

可信度不是统计概率，而是按优先级执行的三级规则：

- `低`：满足任一项——销量状态为`数据异常/无销量/近期启动/快速增长/快速下降`；补货状态非空且不是`正常补货`；FBA可售为0且30日销量大于0。
- `中`：先确认未命中低，再满足任一项——30日销量低于`ListingRules!B6`；销量状态为`增长/下降`；广告状态为`数据不足/高广告费/广告费上涨/广告费下降`。
- `高`：必须同时满足——30日销量达到`ListingRules!B6`；销量状态为`稳定`；补货状态为空或`正常补货`；FBA可售大于0；广告状态为`正常`或`无广告`。任一条件不满足都不会填高。

`链接状态`不参与可信度。实际表中没有高并不表示“高”不可出现，只表示当时每行都至少命中了中或低条件。

## 7. 数据滚动、并发与恢复

1. 新日期：原本次日期和滚动字段先进入“上次”列，再写入新值。
2. 同日重传：只修正本次值，不再次滚动上次值。
3. 更早日期：跳过，防止历史文件覆盖新数据。
4. 空白与0不同。库存空白继续保留共享表现值，避免FBM行误清FBA数据；周期销量、广告费和销售额空白会清除旧周期值并触发数据异常，防止旧指标继续生成补货建议；优惠价空白表示优惠结束，也会清除旧值。
5. 预览后若规则版本改变，正式执行会停止并要求重新预览。
6. 写入前会核对共享表是否在预览快照后被人工修改；发生变化时停止，不覆盖新值。
7. 操作历史保存静态值和公式文本。恢复时只恢复仍等于本次系统写入值的单元格；人工后来修改的冲突项只报告、不覆盖。
8. “初始化 / 检查规则配置”是显式的结构维护动作，执行前仍应使用WPS历史版本或工作簿副本备份；日常Listing导入则进入系统操作历史。

## 8. 首次升级步骤

1. 在WPS中创建工作簿副本并确认历史版本可用。
2. 按第3节更新每个Listing子表表头，保留MSKU、品名、ASIN、备注和需要迁移的人工值；MSKU必须唯一。
3. 将项目内`airscripts/Listing库存销售自动回填.js`完整替换到WPS的Listing共享脚本并保存。当前结构版本为10。
4. Webhook和脚本令牌未变化时无需重新生成；如WPS生成了新值，只在“我的店铺”本机配置中更新，不写入文档或Git。
5. 在“我的店铺”扫描共享表，核对每个国家对应的Listing子表，然后点击“测Listing”。
6. 进入“库存销售表格”，选择店铺和国家，点击“初始化 / 检查规则配置”。首次会创建`ListingRules`；已有中文`规则配置`会自动改名；无法解锁的历史规则表会保留为`ListingRules_旧保护`；系统恢复默认参数、写入A22:D71逐项规则说明、重装公式并安装低可信度提示色，新的规则表保持未保护。
7. 检查B4:B14及B17:D19默认参数，按公司策略调整后升级B4规则版本号。
8. 上传领星全字段`.xlsx`，检查重复MSKU、销量窗口、规则版本、公式行和“下次回填将重置人工最终月销”行数，再确认更新。
9. 到“操作历史”确认批次可恢复，并抽查评分/评论数、广告费、销售额、最终月销、覆盖天数和建议补货量。

## 9. 常见停止原因

- 缺少或重复标准表头：按名称补齐；不要用相似列名让系统猜测。
- 缺少`ListingRules`、参数无效或公式错误：替换最新脚本后点击初始化并检查参数/权重。不要再把标签改回中文`规则配置`。
- 历史`ListingRules`仍被旧脚本保护：完整替换为版本10脚本后重新初始化。系统会尝试解除；仍无法解除时自动保留旧表并创建未保护的新`ListingRules`。
- Listing脚本版本过旧：完整替换为项目内结构版本10脚本。
- 表内MSKU重复、文件内MSKU重复或ASIN冲突：先核对Listing映射。
- 规则版本在预览后改变：重新上传并预览。
- 人工值冲突：在操作历史查看具体单元格，系统不会强制覆盖。
- 领星完整导出存在同MSKU多行：重复MSKU全部跳过，避免把不同Listing误合并；需先确认店铺、站点和MSKU口径。
