# 操作历史与恢复说明

## 1. 已完成的数据安全能力

系统使用独立的`g_team_ops.modules.operations`模块管理数据库备份、共享表写入审计、
恢复和并发保护。当前已实现：

- SQLAlchemy统一数据库Engine，SQLite启用WAL、外键、写等待和完整耐久模式；
- Alembic版本化迁移，现有`app.db`可直接升级；
- 数据库迁移前自动生成一致性备份并执行`integrity_check`；
- 管理员可创建和验证本机整库备份；
- 按当前用户隔离的操作批次、逐项结果、写前快照、实际字段差异和审计事件；
- 请求幂等键和跨物流、Listing模块共用的店铺工作簿资源锁；
- 物流版本11与Listing版本4 AirScript的只读快照、前置条件写入、恢复预览和条件恢复；
- 共享表“恢复上一次操作”，包括冲突保护、部分恢复结果和独立恢复审计批次。

数据库、备份、日志和恢复快照只保存在本机`data`目录，不提交Git。数据库内的脚本
令牌和业务密码仍使用当前Windows用户的DPAPI密文保存，备份不得发送给他人。

## 2. 两类恢复的边界

“操作历史与恢复”页面包含两类不同的保护：

1. **WPS共享表恢复**：普通用户可以预览和恢复自己最近一次符合条件的物流或Listing
   写入；只反向处理该批次实际改变的系统字段。
2. **本机数据库备份**：仅管理员可以创建和重新验证整库备份。正式整库恢复使用独立
   维护程序，必须先完全关闭Web服务；网页永远不会在线替换`app.db`。

WPS自己的历史版本是文件级兜底，系统恢复是字段级、可审计且带冲突检查的第二道保险；
两者互相补充，不能相互替代。

## 3. 一次共享表写入如何受保护

物流和Listing更新使用相同的安全流程：

1. 创建包含当前用户、店铺、国家、模块和幂等键的操作批次；
2. 获取`workbook:{profile_id}:{shop_id}`资源锁，同一店铺的不同模块也串行写入；
3. AirScript按表头名称及FBA、MSKU或事件编号读取目标当前值；
4. 原值先持久化到`operation_snapshots`，成功后才允许调用外部写入；
5. AirScript在写入前再次检查当前值是否仍等于快照，不一致就停止或报告冲突；
6. 写入后再次读取目标，系统只把真实发生的差异写入`operation_changes`；
7. 批次记录成功、部分成功、失败或状态不确定，并释放资源锁。

长时间访问货代或WPS时不会持续占用SQLite写事务。若写入后回读失败，批次标记为
`uncertain`，但写入前快照已经落库。用户可点击“核对实际结果”，系统只读WPS现值并
补记与快照不同的精确差异，不修改共享表。存在差异时转为`reconciled`并允许后续安全
恢复；没有差异时转为`reconciled_no_change`。

## 4. “恢复上一次操作”的规则

“上一次”是**当前用户、当前店铺、当前模块最近一个已完成且可恢复的写入批次**，
不是全系统最后一次点击。国家站点用于定位实际子表，但不能绕过同店铺同模块中更晚的
可恢复批次。

页面先执行只读恢复预览，结果分为：

- **可恢复**：当前值仍等于原批次写入的新值，可以恢复旧值；
- **已经恢复**：当前值已经等于旧值，不重复写入；
- **冲突**：当前值既不是原新值也不是原旧值，说明存在人工或后续修改，只报告不覆盖；
- **失败**：目标表、表头、FBA、MSKU或事件编号无法安全定位，不猜测写入位置。

用户确认后，系统按表头和业务唯一键重新定位并条件写入。恢复只处理原批次实际修改的
系统字段，不改人工列、公式列或其他模块字段。物流主表以FBA号定位，轨迹明细以稳定的
事件编号定位，Listing以MSKU定位；原行号和列号仅作审计参考。

恢复动作会新建`restore`操作批次，并记录反向发生的精确差异。预览中的每个安全项目都
带有本机数据库的稳定变更ID，用户可以只选择本次需要恢复的项目；后端会再次校验这些ID
确实属于当前用户和原批次，不信任客户端行号或列号。部分恢复后系统只读检查原批次全部
变更，全部回到旧值时状态为`rolled_back`；仍有待恢复、冲突、失败或检查结果未知时为
`rollback_partial`，可从原操作继续预览和选择。原操作历史不会修改或删除。

## 5. 使用步骤

1. 先把`airscripts/FBA物流自动回填.js`完整更新到物流文档共享脚本，结构版本必须为11；
2. 把`airscripts/Listing库存销售自动回填.js`完整更新到Listing共享脚本，结构版本必须为4；
3. 在“我的店铺”分别测试物流和Listing连接；
4. 正常执行一次物流或库存销售更新；
5. 进入“操作历史与恢复”，在最近可恢复批次点击“选择并恢复”；
6. 核对可恢复、已经恢复、冲突和失败数量，勾选本次要恢复的项目，再显式确认；
7. 执行后刷新历史，核对新生成的恢复批次和结果摘要；若显示部分恢复，点击“继续恢复”处理剩余项目。

旧AirScript会被新版后端拒绝，避免在缺少快照和条件恢复协议时继续写入。升级脚本不会
要求重新填写共享表链接；Webhook和令牌没有变化时也不需要重新生成。

## 6. 数据库选择与遗留能力

当前正式数据库继续使用本机SQLite。数据量增加本身不足以要求迁移SQL Server或MySQL；
当前单机、单进程使用下，事务、迁移、备份、审计和外部补偿比更换数据库品牌更关键。
以后明确需要多电脑、多进程或服务器部署时，再通过现有Engine和Repository边界评估
PostgreSQL、SQL Server或MySQL。

## 7. 自动备份与停机整库恢复

程序每天首次启动创建一份`scheduled_daily`一致性备份，并保留最近14份自动备份。
轮换只删除自动备份；`manual`、`migration`和`pre_restore`等备份不会自动删除。

停机恢复步骤：

1. 在网页“操作历史与恢复”确认目标备份ID并完成验证；
2. 完全关闭`GTeamOpsHub.exe`、`start_web.ps1`和其他占用8765端口的程序；
3. 在项目目录运行`./restore_database.ps1 list-backups`再次核对备份；
4. 运行`./restore_database.ps1 preview-restore --backup-id <备份ID>`；
5. 确认后运行`./restore_database.ps1 restore --backup-id <备份ID> --confirm "RESTORE DATABASE"`；
6. 看到`result: success`后重新启动Web程序，并在页面核对整库恢复历史。

维护程序会先创建`pre_restore`保险备份，然后确认数据库可独占访问并恢复目标备份。恢复后
自动升级到当前Alembic版本并再次执行完整性检查；验证或记录失败时使用保险备份还原。
维护锁阻止Web程序在恢复过程中启动。不要手工删除或覆盖`app.db`。

运行中任务若因程序或请求退出遗留状态，页面允许“接管中断任务”，但必须先确认原任务
已经停止并等待最长一小时的租约锁过期。有快照的普通写入会转入`uncertain`并先只读核对，
没有快照的写前中断转入`interrupted`，恢复任务转入`rollback_partial`并从原操作继续。
系统不会自动重放状态不确定的WPS写入。

仍未完成的是货代查询等长任务的持久化后台队列和真正自动断点续作；现阶段安全接管、
只读核对和选择性继续恢复均需要人工触发。

在这些能力完成并重新审计前，系统继续定位为可信电脑上的本机单进程工具，不直接作为
公网多用户服务部署。

## 8. 安全限制

- 操作事件和文本日志不得保存密码、令牌、验证码或完整请求正文；
- 恢复所需的FBA、MSKU、事件编号和值只保存在本机数据库并按`profile_id`隔离；
- 上传文件只记录必要元数据和统计，不保存文件正文；
- 正式数据库恢复和WPS补偿写入都需要显式确认，不自动执行；
- 任何新模块写共享表时都必须复用操作批次、写前快照、幂等键和店铺资源锁。
