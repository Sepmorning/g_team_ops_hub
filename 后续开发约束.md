# 后续开发约束

本文记录用户已经确认、但当前阶段暂不实施的行为，后续改造成前后端或多页面系统时必须保留。

## 1. 已完成货件的查询边界

- 安达官网可能不再返回以前已经完成的 FBA。
- 当前程序将这类结果显示为“未找到”是可以接受的。
- 暂不自行推断“已完成”，也不调整查询范围或状态映射。
- 等用户明确“已完成”的判断边界后，再单独设计和修改。

## 2. 页面切换后的重新登录

- 用户在浏览器登录同一安达账号时，可能使系统中原有安达会话失效（被挤下线）。
- 后续前后端系统从其他功能页面切回物流查询页面时，必须重新执行一次登录或会话有效性验证。
- 优先静默重新登录；失败时显示明确的网络、认证或接口错误，但不得因此清空已有查询结果。
- 此规则至少适用于安达；后续统一认证管理时，也应对其他需要登录的货代进行会话有效性验证。

## 3. 查询结果生命周期

- 查询结果属于当前程序运行会话。
- 切换到其他功能页面再返回查询页面时，原搜索条件和查询结果必须保留。
- 重新登录、登录失败、会话失效或页面重新激活，都不得自动清空结果。
- 只有整个桌面程序退出，或将来的前端应用会话明确结束时，才清除这些临时查询结果。
- 如果以后增加“清空结果”按钮，只能由用户主动点击触发。

## 4. 后续验收要点

1. 查询一批 FBA 并得到结果。
2. 切换到其他功能页面。
3. 在浏览器登录同一安达账号，使旧会话可能失效。
4. 返回物流查询页面，系统自动重新登录或验证会话。
5. 原查询结果仍完整显示。
6. 不关闭程序再次切换页面，结果仍保留。
7. 退出并重新启动整个程序后，上一运行会话的临时结果被清除。

## 5. WPS 多店铺与多国家扩展

- 当前一个共享表链接代表一个店铺，桌面版当前只保存并连接一个店铺。
- 一个工作簿可包含多个国家站点子表；当前版本只查找并更新名称精确为 `US-FBA` 的子表。
- 后续增加其他国家时，必须建立“国家/站点 → 子表名称”的配置或数据模型，不得假设第一个子表就是目标表，也不得把工作表 ID 写死。
- 当前已采用“`US-FBA` 主表物流快照 + `US-轨迹明细` 事件表”。两张表都必须按第一行表头名称动态映射，不得依赖固定列号；店铺、备注等额外列和任意列顺序必须继续兼容。
- `运输方式`、`运输线路/渠道`、`目的仓`、`末端追踪号` 由用户或其他模块维护，物流自动回填不得要求、修改或清空这些列。
- 明细表不保存 `地点` 和 `附件/POD` 列；POD是否存在只保留在主表 `POD状态`。
- 主表的预计时间与实际时间必须严格分开。提货、实际出发、实际到达和签收时间已有人工值且与官网解析值不同时不得覆盖，必须报告冲突。“提取派送”表示最近一次实际开始派送的日期，出现新的“派送中”节点时允许更新。
- 明细事件必须用稳定事件编号去重；新增事件只追加一次，已有事件允许更新计划有效状态、异常状态和确认时间。
- 新增货代时应先映射到通用阶段、节点、信息属性和主表快照字段，不得仅因某家货代的专有字段就随意增加主表列。无法通用的信息保留在“物流轨迹原文”“官网原始状态”或“运输信息”中。
- 后续多用户系统中，WPS 应用凭证、用户令牌、店铺共享链接和工作表绑定必须按用户/店铺隔离。
- 自动回填必须继续遵守：无变化不写、查询失败不写、货代冲突不写、表内重复 FBA 不写、未找到不写。
- 店铺一键任务以共享表“货代”列作为查询路由，只查询单元格中唯一识别出的安达、超鸿或易通；手动查询只调度本次实时检查成功的货代，并在这些货代之间继续检查冲突。
- 当前 WPS 传统 `.xlsx` 接口不返回作为字段定义的第一行，桌面版采用用户一次性配置 FBA 列和路由列字母；后续如果迁移到能返回字段结构的表格类型，可以恢复按表头自动映射。
