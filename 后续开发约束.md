# 后续开发约束

本文记录用户已经确认、但当前阶段暂不实施的行为，后续改造成前后端或多页面系统时必须保留。

## 1. 已完成货件的查询边界

- 安达官网可能不再返回以前已经完成的 FBA；将这类手动查询结果显示为“未找到”是可以接受的。
- 店铺“一键查询并更新物流”严格只读取共享表中 `是否完成` 为空的FBA。
- `是否完成` 只要有任何内容（包括“是”或“否”）就完全跳过，不得再根据 `物流最后更新时间` 是否为空自动补查。
- 用户手动输入FBA时仍允许查询，不受一键任务的完成状态筛选限制。

## 2. 页面切换后的连接缓存

- 用户在浏览器登录同一安达账号时，可能使系统中原有安达会话失效（被挤下线）。
- 从其他功能页面切回物流查询页面时只读取本机配置和最近状态，不得自动访问货代网站。
- 连接检查结果按用户缓存 10 分钟；执行查询或一键更新时优先复用缓存，只对缺失或过期状态联网检查。
- 用户主动点击“立即重新检查”时忽略缓存并验证全部货代；失败时显示明确的网络、认证或接口错误，但不得因此清空已有查询结果。
- 查询期间若货代返回会话失效，仍应按货代自身的重新认证机制处理，不得把缓存状态当作永久有效会话。

## 3. 查询结果生命周期

- 查询结果属于当前程序运行会话。
- 切换到其他功能页面再返回查询页面时，原搜索条件和查询结果必须保留。
- 重新登录、登录失败、会话失效或页面重新激活，都不得自动清空结果。
- 只有整个本机Web服务退出，或将来的前端应用会话明确结束时，才清除这些临时查询结果。
- 如果以后增加“清空结果”按钮，只能由用户主动点击触发。

## 4. 后续验收要点

1. 查询一批 FBA 并得到结果。
2. 切换到其他功能页面。
3. 在浏览器登录同一安达账号，使旧会话可能失效。
4. 返回物流查询页面，系统只恢复最近状态，不自动访问货代网站。
5. 缓存有效期内执行查询，不重复进行连接探测；缓存过期后执行查询时才重新验证。
6. 原查询结果仍完整显示。
7. 不关闭程序再次切换页面，结果仍保留。
8. 退出并重新启动整个程序后，上一运行会话的临时结果被清除。

## 5. WPS 多店铺与多国家扩展

- 当前网页版支持多个店铺；一个共享表链接代表一个店铺，每个店铺可以配置物流和Listing两套独立AirScript连接。
- 一个工作簿可包含多个国家站点子表；当前版本已经保存“国家/站点 → Listing/FBA/轨迹明细子表”映射，并由用户在任务页面明确选择。
- 后续增加国家或表格模块时必须继续使用站点映射，不得假设第一个子表就是目标表，也不得把工作表ID、列号或国家写死。
- 当前已采用“`US-FBA` 主表物流快照 + `US-轨迹明细` 事件表”。两张表都必须按第一行表头名称动态映射，不得依赖固定列号；店铺、备注等额外列和任意列顺序必须继续兼容。
- `运输方式`、`运输线路/渠道`、`目的仓`、`末端追踪号` 由用户或其他模块维护，物流自动回填不得要求、修改或清空这些列。
- 明细表不保存 `地点` 和 `附件/POD` 列；POD是否存在只保留在主表 `POD状态`。
- 主表的预计时间与实际时间必须严格分开。提货、实际出发、实际到达和签收时间已有人工值且与官网解析值不同时不得覆盖，必须报告冲突。“提取派送”表示最近一次实际开始派送的日期，出现新的“派送中”节点时允许更新。
- 明细事件必须用稳定事件编号去重；新增事件只追加一次，已有事件允许更新计划有效状态、异常状态和确认时间。事件同步成功后只删除主表“是否完成”非空或已移除FBA对应的明细行；仍为空的活跃FBA即使查询失败也必须保留旧明细，禁止先清空整张明细表。
- 明细日期比较必须兼容WPS返回的Excel日期序号与文本日期；“新增、实际更新、无变化”统计必须互斥，禁止同一事件同时计入更新和无变化。
- 新增货代时应先映射到通用阶段、节点、信息属性和主表快照字段，不得仅因某家货代的专有字段就随意增加主表列。无法通用的信息保留在“物流轨迹原文”“官网原始状态”或“运输信息”中。
- 后续多用户系统中，WPS 应用凭证、用户令牌、店铺共享链接和工作表绑定必须按用户/店铺隔离。
- 自动回填必须继续遵守：无变化不写、查询失败不写、货代冲突不写、表内重复 FBA 不写、未找到不写。
- 店铺一键任务以共享表“货代”列作为查询路由，只查询单元格中唯一识别出的安达、超鸿或易通；手动查询只调度最近 10 分钟内检查成功的货代，缓存缺失或过期时先检查，并在可用货代之间继续检查冲突。
- 当前物流和Listing AirScript均按表头名称返回字段映射；列顺序允许任意调整。后续模块不得退回到用户配置固定列字母的实现。

## 6. 模块化架构约束

- FastAPI应用工厂只负责公共依赖和模块注册，不得放置具体业务路由或业务计算。
- 后续新模块必须在`anda_tracker/modules/<module_name>`中独立开发，不得先在外部项目完成后整体复制进来。
- Router不得直接执行SQL、实现业务公式或拼接任意外部请求；业务流程进入Service，数据访问进入Repository，货代与WPS访问进入Integration。
- 模块不得导入另一个模块的Router；跨模块只能使用明确的Service、Facade或公共模型。
- 每个新模块必须配套业务测试、Web接口测试、异常处理和使用说明，并更新`架构与模块开发规范.md`中的公共规则（如有变化）。
- 后端架构重构、数据库迁移和前端Vue迁移必须分阶段执行，不得在同一次修改中同时重写三层。
