# G组运营工作台开发日志

## 1. 文档用途与维护规则

本文是项目长期交接记录，汇总历次Codex对话中的用户要求、方案确认、实际改动、验证结果和遗留事项。它不是原始聊天逐字稿，而是可以直接指导后续开发的结构化记录。

出于安全要求，下列内容不得写入Git：真实店铺共享链接、Webhook、AirScript令牌、货代密码、验证码、登录会话、数据库内容和用户配置。对话中出现过的这类值在本文中统一记为“本机敏感配置”，不保留原值。

以后每次开发必须在本文末尾追加一节，至少包括：

- 日期、用户需求和确认方案；
- 实际修改的模块、接口、数据库、AirScript、表头或公式；
- 兼容性与风险处理；
- 测试、语法编译、EXE构建和人工验证结果；
- Git分支、提交或“未提交”状态；
- 用户还需要执行的操作及未解决事项。

## 2. 固定项目边界

- 正式项目原路径为`D:\CodexProject\anda_fba_tracker`，2026-08-09起改名为`D:\CodexProject\g_team_ops_hub`。
- PyQt桌面备用版位于`D:\CodexProject\FBA_Tracker_Desktop`，除非用户明确要求，否则只读且不得修改。
- 本机`data`、`.venv`、日志、数据库、密码、令牌和店铺配置不得提交Git。
- 每次修改前先检查Git状态、当前分支和最近提交，不覆盖已有本地改动。
- 未经用户明确要求，不提交或推送；用户明确要求提交或推送时，必须先完成相应测试并报告结果。
- 共享表和领星文件按表头名称识别，不能把字段顺序或列字母写死。
- 新功能必须包含测试、异常处理、使用说明，并且不能破坏物流查询、货代连接、店铺配置和已有共享表回填。

## 3. 历史基线与阶段提交

| 日期 | 提交 | 阶段 |
|---|---|---|
| 2026-07-19 | `6959e55` | 初始FBA物流查询项目 |
| 2026-07-20 | `13c696c`、`a93871c`、`5bf237c` | WPS范围识别、EXE交付、AirScript自动回填 |
| 2026-07-23 | `0cf8d3c` | 桌面版分离，网页版稳定性完善 |
| 2026-07-26 | `e4e686c` | 物流轨迹解析和共享表更新基线 |
| 2026-07-26 | `0eacd63` | Listing库存销售表格模块 |
| 2026-08-03 | `aad9089` | 店铺配置集中化和多国家站点映射 |
| 2026-08-06 | `74ef1a6` | 可选优惠价同步和轨迹明细清理 |
| 2026-08-09 | `c563463` | 货代连接验证缓存 |
| 2026-08-09 | `6fb760e` | FastAPI模块化单体架构拆分 |

远程`origin/main`在本次改名前停留于`74ef1a6`；`c563463`位于本机`main`，`6fb760e`位于本机`codex/modular-architecture`。以后应以实际`git status`和`git log`为准，不依赖本文推断当前分支。

## 4. 物流查询与共享表回填决策

### 4.1 物流主表与轨迹明细

- 每个国家站点使用`国家代码-FBA`主表和`国家代码-轨迹明细`事件表，例如`US-FBA`、`US-轨迹明细`。
- 物流主表保存当前快照；轨迹明细保存预计、实际、变更、异常、恢复和POD等稳定事件。
- 主表与明细均按第一行表头名称定位，不依赖固定列号；人工列和自定义列不得被模块覆盖。
- 主表预计时间与实际时间分开。人工已有实际值与官网解析值冲突时不覆盖，必须报告冲突。
- 明细事件使用稳定事件编号去重；新增只追加一次，已有事件只在真实变化时更新。
- 明细日期比较兼容WPS Excel日期序号和文本日期；“新增、实际更新、无变化”统计互斥，避免再次出现“状态更新381、已存在381”一类重复计数。

### 4.2 一键更新筛选

- 用户发现共享表仅37个`是否完成`为空的FBA，却读取58个，原因是旧逻辑让查询失败或未找到且没有更新时间的已完成行反复进入待查询列表。
- 最终确认：店铺一键更新严格只查询`是否完成`为空的FBA；该列只要有任何内容，包括“是”或“否”，都完全跳过。
- 手动输入FBA仍允许查询，不受上述筛选限制。
- 查询失败、未找到、货代冲突、目标表重复FBA或人工值冲突时不回填业务值。

### 4.3 更新标记与更新清单

- 查询到FBA行后，只清除系统管理字段中上一次的系统高亮，不清除人工单元格自己的格式。
- 仅主表真实变化的单元格填充“矢车菊蓝，着色1，浅色80%”。
- 轨迹明细不着色。
- 网页同步返回更新单元格地址、表头、新旧值和统计，用户可明确知道本次改了哪里。

### 4.4 轨迹明细清理

- 用户最初提出每次清空整张轨迹明细，再写入本次未完成FBA的全部轨迹。
- 为避免查询失败时丢失已有轨迹，最终采用安全快照逻辑：同步成功后只删除主表中`是否完成`非空或已经移除FBA对应的明细行；仍为空的活跃FBA即使本次查询失败，也保留旧明细。
- 该方案能控制历史货件造成的膨胀，同时避免一次外部接口故障毁掉有效数据。

### 4.5 货代连接与外部访问

- 打开物流查询或货代连接页面只读取本机配置和最近状态，不自动登录或访问货代网站。
- 连接验证按用户缓存10分钟；执行任务时复用有效缓存，只检查缺失或过期的货代。
- 用户点击“立即重新检查”时忽略缓存。
- 安达查询尽量复用同次检查建立的短时内存会话，减少重复登录。
- 某家货代未连接不阻断其他货代查询；手动任务只调度本次检查成功的货代。
- 安全审计结论：程序没有后台爬虫、定时轮询或打开页面即批量请求的设计，但批量任务仍会按FBA访问货代接口；易通和超鸿上游存在HTTP接口风险，只应在可信网络使用。后续增加货代必须配置超时、有限重试、并发和速率边界，禁止记录密码、令牌和请求正文。
- 用户曾提出Codex对话经常重连约5次。该现象属于开发工具或网络连接问题，不是项目Web模块发起的货代请求；项目侧只实施了货代连接验证缓存，其他外部网络优化暂缓。

## 5. Listing库存销售表格模块

### 5.1 使用流程

- 用户选择已有店铺和国家站点，上传领星导出的`.xlsx`，系统先按表头解析和预览，再更新该站点的`店铺前缀-国家`Listing子表。
- 领星以后可以增加未知列；程序忽略未知表头，但按名称检查当前必需表头，不依赖顺序。
- MSKU是匹配键。源文件MSKU重复、共享表MSKU重复或ASIN冲突时，该MSKU不写入并在结果中报告。
- 领星`Rating总数`映射为共享表`评论数`。

### 5.2 库存聚合

- `FBA可售 = 领星FBA可售`。
- `预留 = FBA待调仓 + FBA预留`。
- `在途 = FBA计划入库 + FBA标发在途`。
- `FBA入库中`和`FBA不可售`用于确认源文件结构，但不写入共享表，也不参与上述聚合。

### 5.3 最终表头

推荐完整表头如下，实际列顺序可以任意；`优惠价`整列可省略或放在任意位置：

```text
MSKU 品名 ASIN Listing状态 价格 优惠价 本次数据日期 上次数据日期 评分 评论数 上次评分 上次评论数 昨日广告费 上次昨日广告费 FBA可售 预留 在途 上次FBA可售 上次预留 上次在途 7日销量 14日销量 30日销量 上次7日销量 上次14日销量 上次30日销量 7日均销 14日均销 30日均销 7日折算月销 月销差异 月销差异率 销量趋势 系统建议月销 最终补货月销 在库覆盖月数 含在途覆盖月数 建议补货量 链接情况 库存情况 广告情况 运营备注 本次更新时间
```

- 人工字段：MSKU、品名、ASIN、Listing状态、价格、链接情况、库存情况、广告情况、运营备注。
- 系统滚动字段：数据日期、评分/评论数、昨日广告费、FBA可售/预留/在途、7/14/30日销量及对应上次值。
- 公式字段：日均销量、7日折算月销、月销差异、月销差异率、销量趋势、系统建议月销、覆盖月数和建议补货量。
- `最终补货月销`不是永久公式：每次系统更新都重置为本次系统建议月销，用户复核后可人工修改；覆盖和建议补货公式立即跟随变化。不维护“旧系统建议月销”或“旧最终补货月销”。
- “月销差距较大”的差异率阈值为25%。系统建议月销在7日折算和30日销量都有值时取平均，只有一个口径有效时使用有效口径。
- 覆盖月数与补至3个月的公式、示例列字母见`库存销售表格模块使用说明.md`。程序本身不写死公式列，也不改写WPS公式。

### 5.4 日期和可选优惠价

- 新日期导入：原本次值滚动到上次值，再写入新本次值。
- 同日重传：只修正本次值，不再次滚动。
- 更早日期：跳过，防止旧文件覆盖新数据。
- 普通字段空白不覆盖，数值0是有效值。
- `优惠价`只有领星文件和共享表两端都存在该表头时才同步；任一端没有时不报错、不写入。两端都有且源值为空时，表示优惠结束，会清除旧优惠价。
- 插入或移动`优惠价`后，由WPS自动调整公式；程序仅按表头名称寻找目标列。

### 5.5 AirScript

- Listing和物流使用同一店铺共享表，但业务脚本分开，以减少权限和协议耦合。
- Listing使用独立Webhook和令牌；物流也使用自己的Webhook和令牌。
- 两套连接统一在“我的店铺”一次性配置，任务页不得重复要求用户填写店铺链接或令牌。

## 6. 店铺集中配置与多国家站点

- 一个用户可以配置多个店铺；一个店铺保存名称、共享表链接、物流脚本连接、Listing脚本连接和国家站点映射。
- 首次连接后扫描工作簿全部子表，解析每个国家的Listing、FBA主表和轨迹明细。
- Listing真实命名示例为`纯粹-美国`、`纯粹-加拿大`；物流示例为`US-FBA`、`CA-FBA`及对应轨迹明细。
- 店铺显示名称和Listing表前缀可能不同。旧“必须与店铺名完全一致”的规则会漏掉加拿大等站点，最终改为：扫描候选前缀，明确时自动采用；存在歧义时让用户确认一次并保存；保存后仍允许手工修改。
- 任务页只选择店铺和国家，系统检查对应模块脚本与子表是否就绪。
- 新模块必须复用用户、店铺、站点和模块连接基础数据，不重复创建另一套店铺配置。

## 7. 架构、前端、数据库与部署决策

- 用户担心模块越来越多后项目臃肿、界面难维护、数据库和回退能力不足，并考虑未来服务器部署或每人本地运行。
- 当前推荐路线是先把现有两个模块的小问题收口，再建立数据安全底座和模块化边界，然后开发新模块；Vue界面迁移应等API契约稳定后单独进行，不能与业务算法和数据库大改绑在一起。
- 2026-08-09已先把FastAPI改为模块化单体：`web.factory`只装配应用；账号、货代连接、店铺、物流和库存销售路由分别位于`modules`。
- 新模块应直接在本项目独立模块目录开发，不建议在外部项目完成后整体复制；外部原型可以验证纯算法，但最终代码必须从一开始遵守本项目用户隔离、店铺配置、日志、安全和测试规则。
- 当前前端仍是Jinja2模板、原生JavaScript和CSS，没有Vue。未来可以按页面逐步迁移Vue 3，FastAPI继续负责登录、CSRF、API和业务；PyInstaller中打包前端构建结果，终端用户不需要Node.js。
- 当前使用本机SQLite，适合单机个人工具。服务器多用户阶段应增加数据库迁移、审计日志、写前预览、操作批次、幂等键、乐观并发控制、WPS写入快照和补偿/回滚机制，再迁移PostgreSQL；不能仅把现有EXE直接暴露到公网。
- 在这些安全底座完成前，推荐每人本机运行、各自维护店铺配置和数据，避免共享SQLite和同步冲突。

## 8. 2026-08-09 模块化架构实施

用户确认按不改变业务行为的原则拆分架构，并要求把未来新模块规范写入文档、更新过时说明。

实际结果：

- 新增`g_team_ops.web.factory`应用工厂和`g_team_ops.web.context`公共Web上下文；
- `g_team_ops.web.app`保留兼容入口；
- 页面与API按账号、货代连接、店铺、物流和库存销售拆入独立路由；
- 新增`架构与模块开发规范.md`并更新README、启动说明、开发约束、接口审计、界面架构和代码审计报告；
- 当时完整测试结果为`96 passed, 2 skipped`；
- 当时EXE重新构建并通过独立数据目录健康检查；
- 提交为`6fb760e`，没有推送。

## 9. 2026-08-09 项目更名、临时目录清理和交接文档

用户要求：

1. 判断并清理`.build-web-exe-20260806`和`.exe-smoke-20260806`；
2. 项目改成G组相关名称，并同步目录、代码路径、程序和前端；
3. 把历次对话要求和改动写入长期开发文档，以后持续维护；
4. 提供新Codex任务可直接使用的交接提示词。

命名方案：

- 产品：`G组运营工作台`；
- 本机目录：`g_team_ops_hub`；
- Python包：`g_team_ops`；
- EXE：`GTeamOpsHub.exe`；
- 前端英文标识：`G TEAM OPS`；
- GitHub远程仓库暂时保留历史名称`FBA_Tracker`，克隆时显式指定本地目录名。

实际处理：

- 两个带日期目录分别是旧PyInstaller构建工作区和独立EXE冒烟测试副本，共约65.6 MB；正式项目不读取，已永久删除。空的`.build-web-exe`中断残留也一并删除。这些均为可重新生成的临时产物，没有删除正式`data`或配置。
- Python包和全部导入路径由`anda_tracker`迁移为`g_team_ops`。
- 程序文件和构建名称由`FbaTrackerWeb.exe`迁移为`GTeamOpsHub.exe`。
- FastAPI标题、页面标题、侧栏品牌、启动提示、会话Cookie名、日志命名和前端公共命名空间同步改为G组名称。
- 会话Cookie改名会让浏览器中的旧登录会话失效一次；用户重新登录即可，账号、数据库、店铺和货代配置不会丢失。
- FBA库存、FBA号、FBA子表和安达货代类等业务名称保留。
- README、启动说明、使用说明、审计和架构文档中的路径及程序名同步更新。
- 新增本文和`新任务交接说明.md`，并把日志强制维护规则写入README、架构规范和后续开发约束。

最终结果：

- 根目录已完整迁移到`D:\CodexProject\g_team_ops_hub`，旧`D:\CodexProject\anda_fba_tracker`已删除；正式`data`、`.venv`和完整`.git`随项目保留，桌面备用版未修改。
- 首次Windows目录移动在清理受沙箱保护的旧`.git`入口时中断。核对后确认完整仓库已在新目录，源码、测试和本机数据仍完整在旧目录且目标端不存在；随后逐项无覆盖移动，最后只删除经检查为0项的旧`.git`残留和空旧根目录，没有丢失或覆盖文件。
- 新路径完整测试为`97 passed, 2 skipped`，仅有第三方Starlette TestClient弃用警告；`compileall`通过。
- 新增产品身份测试，固定FastAPI标题和`/health`返回值均为“G组运营工作台”。
- 在新路径重新构建`GTeamOpsHub.exe`并用独立临时`data`目录通过`/health`冒烟测试；测试进程和临时目录已清理。
- 最终EXE大小为18,688,862字节，SHA-256为`439E203FA43C9772D85AC66B980578FD1C01B24EF0856DAEACF8C38D16B96510`。
- 当前分支仍为`codex/modular-architecture`。本轮用户未要求提交或推送，因此所有更名和文档改动保持未提交状态。

## 10. 2026-08-09 GitHub仓库改名与发布

用户追加要求：GitHub仓库也采用G组新名称，并把当前全部修改提交到GitHub。

- 使用本机Git凭据通过GitHub官方API将`Sepmorning/FBA_Tracker`改名为`Sepmorning/g_team_ops_hub`；仓库仍为公开仓库，默认分支仍为`main`。
- 本地`origin`更新为`https://github.com/Sepmorning/g_team_ops_hub.git`，README克隆命令同步使用新地址。
- 发布前再次检查Git差异、敏感文件、完整测试和EXE冒烟结果；暂存77个版本文件，没有`data`、`.venv`、数据库、日志或密钥。
- 项目更名提交为`29381ee`（`refactor: 将项目更名为G组运营工作台`）；本地`main`以快进方式包含`c563463`、`6fb760e`和`29381ee`。
- 首次直连GitHub的推送因443网络超时失败；确认PowerShell可经Windows默认代理访问后，仅在单次Git命令中复用该代理，没有写入Git或项目配置。
- 已成功把`main`从远端`74ef1a6`推送到`29381ee`。

## 11. 2026-08-09 数据安全底座第一阶段

### 11.1 需求与决策

- 用户希望项目继续规范化、模块化，但不在功能尚未完善时直接做公网多用户部署；当前仍按本机单进程产品建设，同时为以后迁移正式服务端数据库保留清晰边界。
- 超鸿和易通没有可用的正式HTTPS开放API，当前无法从客户端消除HTTP上游风险；继续限制在可信网络使用，保留超时、有限重试和脱敏日志。
- 用户确认需要WPS历史版本与系统恢复“双重保险”。系统的“上一次”定义为当前用户、当前店铺、当前模块最近一个已完成且可恢复的写入批次，不是全系统最后一次点击。
- 恢复必须先预览；只有当前单元格仍等于原批次写入的新值时才恢复旧值。后续人工修改、公式变化或其他批次修改均作为冲突跳过，恢复动作本身也生成新的审计批次。
- 当前继续使用SQLite。数据量增加本身不足以要求SQL Server或MySQL；先补齐事务、迁移、备份、审计和并发保护，等明确出现多电脑、多进程或服务器部署需求后再评估PostgreSQL、SQL Server或MySQL。

### 11.2 实际修改

- 新增`g_team_ops.db`，统一SQLite连接参数并引入SQLAlchemy 2 Engine；启用WAL、外键、10秒写等待和`FULL`同步。
- 新增Alembic迁移环境及`0001`旧结构基线、`0002`数据安全表。`auth.py`和`storage.py`改为统一执行版本化迁移，保留旧用户和店铺迁移前备份兼容行为。
- 数据库升级前通过SQLite在线备份生成一致性快照，校验`integrity_check`并记录SHA-256；运行中的正式`app.db`禁止在线覆盖，只允许先恢复到隔离文件。
- 新增独立`g_team_ops.modules.operations`模块，包含操作批次、逐项结果、字段变更快照、审计事件、幂等键、持久化资源锁和备份目录仓储。
- 新增“操作历史与恢复”页面。普通用户只能读取自己的批次；管理员可以创建、列出和重新验证本机整库备份，备份文件不能从网页下载。
- 构建脚本加入Alembic迁移资源，`requirements.txt`加入SQLAlchemy和Alembic；重新构建`GTeamOpsHub.exe`。
- 新增`tests/test_operations.py`以及路由和页面契约测试，覆盖旧库迁移保留、迁移前备份、用户隔离、幂等、变更哈希、资源锁、备份验证、隔离恢复和管理员权限。
- 新增`操作历史与恢复说明.md`，同步更新README、架构规范、开发约束、Web启动说明、代码审计和新任务交接说明。

### 11.3 兼容性与安全边界

- 没有修改物流查询、货代连接、店铺配置、国家站点映射、Listing计算、共享表字段识别或现有AirScript请求协议。
- 没有访问或修改桌面备用版；没有修改正式`data/app.db`。首次运行新版程序时，正式数据库才会自动迁移，并在迁移前把一致性备份保存在本机`data/backups`。
- 数据库备份包含本机数据库内已经加密的配置，仍不得提交、外传或跨Windows用户直接使用；`data/`继续由Git忽略。
- 当前只是恢复底座。物流与Listing真实写入尚未接入写前快照、条件应用和补偿动作，因此页面明确不提供WPS“恢复上一次操作”按钮。

### 11.4 验证、构建与Git

- 完整测试：`103 passed, 2 skipped`，仅保留既有Starlette TestClient/httpx弃用警告。
- `compileall`通过；由于现有源码缓存目录受Windows权限保护，验证时把字节码缓存重定向到隔离临时目录，没有删除或覆盖原缓存。
- `build_web_exe.ps1`执行成功。EXE大小为24,386,188字节，SHA-256为`AE6E484F0DBBF3406DFFF6BCA9EA550EF6CEB6B67736AE5B8D49FB62C6E7A2D2`。
- 新EXE在独立目录启动，`/health`返回正常并创建独立`data/app.db`；仅停止路径完全匹配测试副本的进程，隔离目录已经清理。
- 开始时分支为`main`，与`origin/main`同步且工作区干净；本轮结束保持未提交修改。用户未要求提交或推送，因此没有执行Git提交或GitHub推送。

### 11.5 人工操作与遗留事项

- 用户下次启动正式程序时无需手工执行SQL；Alembic会自动升级数据库并先生成迁移备份。管理员登录后可在“操作历史与恢复”查看并手工验证备份。
- 下一阶段需要升级物流和Listing两份AirScript，增加只读写前预览、带前置条件应用、恢复预览和恢复应用，再把现有两类共享表写入接入操作批次与工作簿资源锁。
- 后续再实现维护模式下的正式整库恢复、自动备份保留策略和中断任务续作；在这些功能完成并重新审计前，不把当前EXE直接暴露到公网。

## 12. 2026-08-09 共享表更新历史、审计、恢复与并发保护第二阶段

### 12.1 需求与确认方案

- 用户确认WPS历史版本与系统字段级恢复作为双重保险，并授权执行物流与Listing共享表“恢复上一次操作”。
- “上一次”继续限定为当前用户、当前店铺、当前模块最近一个可恢复写入批次；国家站点不能绕过同模块更晚操作。
- 恢复前必须只读预览。仅当前值仍等于原批次写入值时恢复旧值；人工修改、更晚批次修改、定位失败和公式变化不得覆盖。
- 当前继续使用本机SQLite，不迁移SQL Server或MySQL。先完成写前快照、审计、补偿和并发保护；多电脑、多进程或服务器部署需求明确后再评估数据库迁移。

### 12.2 实际修改

- 新增共享表安全写入管理器，把物流手动查询、物流一键更新、轨迹明细清理和Listing确认更新统一接入操作批次、请求幂等键与店铺工作簿资源锁。
- 写入流程改为：AirScript只读快照、后端持久化原值、锁心跳、带前置条件写入、写后回读、记录精确差异。跨物流和Listing的同一店铺写入使用同一个工作簿资源键串行。
- 新增Alembic迁移`0003_shared_table_snapshots`和`operation_snapshots`表。写前原值在外部写入开始前落库；操作完成后实际差异保存在`operation_changes`。
- 物流AirScript结构版本由10升级到11，增加`snapshot`、`snapshot_targets`、`inspect_changes`和`apply_changes`动作；物流主表按FBA号，轨迹明细按事件编号和表头定位。
- Listing AirScript结构版本由3升级到4，增加同等安全动作；按MSKU和表头定位，仅处理Listing系统写入字段，不改公式列或人工列。
- 新增恢复预览和确认API及“操作历史与恢复”页面交互。恢复动作创建独立审计批次，记录反向精确差异；已恢复项不重复写，冲突项跳过，分批中断保留已完成部分并标记部分恢复。
- 物流和Listing客户端对恢复分批结果进行聚合；中途请求失败时把已经确认写入的部分附加到异常，后端仍保存部分恢复审计。
- 更新README、AirScript升级说明、库存销售模块说明、Web启动说明、架构规范、开发约束、代码审计、操作历史说明和新任务交接说明。

### 12.3 兼容性与业务边界

- 未改变店铺共享链接、用户隔离、国家站点映射和两套模块连接的保存方式；用户不需要重复配置相同店铺链接。
- 共享表继续按表头名称识别，列顺序任意。恢复以FBA号、MSKU或事件编号重新定位，旧行号和列号只作审计参考。
- 物流“是否完成”筛选、货代定向查询和兜底规则、轨迹解析、人工实际时间冲突、明细事件语义和高亮规则保持不变。
- Listing同日重试、日期滚动、库存销售聚合、建议月销和可选优惠价规则保持不变；人工列和公式列不进入恢复集。
- 超鸿、易通HTTP上游风险没有改变；仍只建议在可信网络使用，并保留超时、有限重试和脱敏日志。
- 未访问或修改桌面备用版；未修改正式`data/app.db`，未写入真实店铺链接、Webhook、令牌、账号或业务数据。

### 12.4 测试、构建与Git

- 定向迁移、操作历史、恢复、物流AirScript和Listing Web测试通过：`23 passed`。
- 完整测试：`109 passed`；两份AirScript通过Node语法检查，Python`compileall`通过。仅保留既有Starlette TestClient/httpx弃用警告。
- `build_web_exe.ps1`执行成功。`GTeamOpsHub.exe`大小为24,410,856字节，SHA-256为`88F75FF6E1CAB02919DD148803AC1C74E05B72185D2D6D1F8D9E48D08833628A`。
- 新EXE在隔离目录启动，`/health`返回正常；新建数据库迁移到`0003_shared_table_snapshots`且`operation_snapshots`包含14列。只停止隔离测试副本并清理隔离目录。
- 开发前分支为`main`，保留第一阶段全部未提交修改；本轮没有覆盖任何本地改动。结束时仍在`main`，所有第一、二阶段修改均未提交。用户未要求提交或推送，因此没有执行Git提交或GitHub推送。

### 12.5 人工操作与遗留事项

- 用户必须把项目中的`airscripts/FBA物流自动回填.js`完整替换到WPS物流共享脚本，并保存为结构版本11；再把`airscripts/Listing库存销售自动回填.js`完整替换到Listing共享脚本，并保存为结构版本4。
- Web后端会拒绝旧脚本，防止缺少写前快照和条件恢复时继续写入。Webhook和令牌未变化时不需要重新生成，只需在“我的店铺”重新测试两套连接。
- 升级后建议先用测试FBA和MSKU完成一次真实写入、恢复预览和确认恢复，核对可恢复、已恢复、冲突、失败数量以及新恢复批次。
- 正式整库恢复仍需维护模式；自动备份保留策略、进程中断自动续作和`uncertain`批次人工补偿向导仍为遗留事项。
- 在公网多用户部署、Redis/任务队列、服务端密钥管理和新的安全审计完成前，继续按可信电脑上的本机单进程工具使用。

## 13. 2026-08-13 灾难恢复闭环第三阶段

### 13.1 需求与决策

- 用户要求继续完成下一阶段。本阶段承接上一阶段遗留的维护模式整库恢复、自动备份保留策略和`uncertain`批次核对，不扩大到公网部署或数据库品牌迁移。
- 正式数据库恢复不开放Web接口；必须完全停机后使用独立维护程序，避免运行中的Repository、连接池或请求访问被替换的数据库文件。
- 自动清理只允许处理系统每天生成的`scheduled_daily`备份，手工、迁移前和恢复前保险备份永久保留，除非以后由用户明确授权删除。
- `uncertain`核对必须只读WPS，以持久化写前快照和当前值计算实际差异；不能因为状态不明就自动反写或猜测结果。

### 13.2 实际修改

- 新增Alembic迁移`0004_database_restore_history`和`database_restores`表，保存整库恢复来源、来源哈希、恢复前保险备份、执行者、结果和时间。
- `DatabaseBackupService`新增每日自动备份和保留策略：按本机日期每天最多创建一份，保留最近14份自动备份，只轮换`scheduled_daily`记录及文件。
- 应用工厂启动时执行自动备份；失败会写安全日志但不记录数据库内容或本机密钥，也不因备份故障伪装成功。
- 新增`OfflineDatabaseRestoreService`、`g_team_ops.maintenance`、`maintenance_main.py`、`restore_database.ps1`和独立`GTeamOpsMaintenance.exe`构建流程。
- 停机恢复验证来源SHA-256和完整性，创建`pre_restore`备份，要求确认文字`RESTORE DATABASE`，确认SQLite可独占访问并通过维护锁阻止Web启动，再使用SQLite备份API写回正式库。恢复后自动迁移并检查完整性；后处理失败则从保险备份回写。
- 操作历史页面新增状态不确定批次“核对实际结果”按钮和两个API。核对只调用AirScript的`snapshot_targets`读取当前值，不调用写入动作；确认后把真实差异和逐项摘要原子写入原批次。
- 操作批次新增`reconciled`和`reconciled_no_change`状态。有实际变化的批次标记可恢复，之后仍按现有冲突保护规则执行恢复。
- 管理员操作历史页面显示停机整库恢复历史，并明确网页不会在线替换`app.db`。
- 构建脚本改为同时生成Web EXE和维护EXE；同步更新README、Web启动说明、操作历史说明、架构规范、开发约束、代码审计和新任务交接说明。

### 13.3 兼容性与安全边界

- 继续使用本机SQLite、SQLAlchemy和Alembic；没有安装或迁移SQL Server、MySQL或PostgreSQL。
- 没有访问或修改正式`data/app.db`，所有恢复测试均在隔离临时目录完成；没有修改桌面备用版。
- 没有改动物流版本11和Listing版本4 AirScript，既有FBA、事件编号、MSKU、表头、人工列、公式列、货代兜底和用户隔离契约保持不变。
- 超鸿与易通HTTP上游风险不属于本阶段可消除范围，既有可信网络、超时、有限重试和脱敏日志要求不变。
- `uncertain`核对只证明当前值与写前快照的差异，不能证明差异一定来自原任务；因此恢复时仍必须再次检查当前值，冲突不覆盖。

### 13.4 测试、构建与Git

- 新增并通过旧库迁移、每日备份幂等与保留、停机恢复及保险备份、维护锁、恢复历史、状态不确定批次有变化/无变化核对，以及Web核对接口测试。
- 完整测试：`116 passed`；Python`compileall`与两份AirScript的Node语法检查通过。仅保留既有Starlette TestClient/httpx弃用警告。
- `build_web_exe.ps1`同时构建两个程序成功。`GTeamOpsHub.exe`大小24,422,487字节，SHA-256为`DBD8B7C481AC45052480FAB1F15E4A847BB251357AC57F4ACF99E373A595753A`；`GTeamOpsMaintenance.exe`大小18,070,454字节，SHA-256为`DD0D9874FF36600F837439FBE4AC6AA7F04D52EA1F5547DC4D8CE7A294C2209A`。
- 双EXE在隔离目录通过真实流程冒烟：Web`/health`正常，新库迁移到`0004_database_restore_history`并自动生成一份每日备份；维护EXE成功列出、预览并恢复该备份，恢复后迁移版本仍为`0004`，恢复历史1条，同时保留`scheduled_daily`与`pre_restore`两类备份。
- 冒烟检查曾发现SQLite临时备份会留下空的`-wal`/`-shm`辅助文件；现已统一清理临时主文件及辅助文件并增加回归断言，最终隔离备份目录只保留正式备份文件。
- 冒烟结束只停止路径完全匹配的隔离进程并清理隔离目录；正式8765端口没有监听进程，正式`data`未使用。
- 开发开始时分支为`main`，与`origin/main`指向`c2ac55f`；保留前两阶段52项未提交工作区改动。未经用户要求，不提交、不暂存、不推送。

### 13.5 人工操作与遗留事项

- 日常使用无需手工备份：每天首次启动会生成自动备份；重要操作前仍建议管理员手工备份。
- 整库恢复前必须关闭Web程序，并依次执行`restore_database.ps1 list-backups`、`preview-restore`和带确认文字的`restore`；只有看到`result: success`后才重新启动。
- 若维护程序因断电等原因中断，不要直接删除维护锁或反复启动；先保留`data/backups`中的`pre_restore`备份并人工核对数据库完整性。
- 长任务自动断点续作和`uncertain`逐项选择性确认仍未实现；它们不影响本阶段只读核对与整库恢复安全边界。

## 14. 2026-08-15 安全续作、选择性恢复与备份健康第四阶段

### 14.1 需求与决策

- 用户确认继续第四阶段，并要求完成后提交、推送GitHub及关闭电脑。本阶段承接第三阶段遗留的中断任务安全处理、逐项恢复和备份健康提示，不扩大到公网多用户部署或数据库品牌迁移。
- 当前单机、单进程规模继续使用SQLite、SQLAlchemy和Alembic。更换SQL Server或MySQL不能替代外部写入补偿、审计、并发锁和停机恢复，现阶段迁移收益不足。
- 状态不确定的货代或WPS调用禁止自动重放。只有用户确认原任务已停止且持久化租约已经过期，系统才允许接管并转入只读核对或条件恢复流程。
- 逐项恢复以`operation_changes.id`作为跨请求稳定标识，不使用可能变化的行号、列号或客户端数组位置；部分恢复后必须重新只读检查整个原批次。

### 14.2 实际修改

- 操作状态新增`interrupted`，Repository新增中断接管：有效租约锁会拒绝接管；普通写入有快照时转为`uncertain`、无快照时转为`interrupted`，恢复任务转为`rollback_partial`，并记录接管审计事件。
- 恢复预览和执行API支持`change_ids`。后端校验ID非空、无重复、数量上限及当前用户原批次归属，再把AirScript本次局部索引映射回稳定变更ID和字段元数据。
- 操作历史页面新增逐项勾选恢复、部分恢复“继续恢复”和运行中任务接管交互。冲突与失败项不进入安全勾选列表，未预期恢复中断保留为可再次预览的`rollback_partial`。
- 部分选择恢复后只读检查原批次全部变化；没有待恢复、冲突或失败才标记`rolled_back`，否则保留剩余项目和检查结果，不伪装为全部成功。
- 管理员页面新增每日自动备份健康提示与逐份“完整校验”入口，并新增`GET /api/backups/health`。快速健康摘要检查时效、文件存在性、大小和目录完整性结果，不替代SHA-256与SQLite完整性校验。
- 隔离EXE冒烟发现维护校验会为只读备份留下空`-wal`/`-shm`文件；现已在验证、隔离恢复和停机恢复关闭来源连接后清理辅助文件，并增加回归断言。
- 同步更新README、Web启动说明、操作历史说明、架构规范、后续开发约束、代码审计和新任务交接说明。

### 14.3 兼容性与安全边界

- 本阶段没有新增数据库表或迁移；现有`0004_database_restore_history`数据库可直接使用。没有安装或迁移SQL Server、MySQL或PostgreSQL。
- 没有改变物流结构版本11、Listing结构版本4及其快照、前置条件、检查和条件恢复动作；FBA、事件编号、MSKU、表头识别、人工列、公式列、货代兜底、店铺站点配置和用户隔离契约保持不变。
- 接管不是后台任务自动续跑：有效锁最长一小时内不能接管；接管后仍需人工触发只读核对或选择恢复。系统不会猜测或重放结果不确定的WPS写入。
- 备份健康摘要是快速告警，不保证文件正文此刻未变化；正式整库恢复仍必须停机并由维护程序重新校验来源哈希、完整性和独占访问。
- 超鸿、易通HTTP上游风险没有改变，继续要求可信网络、超时、有限重试和脱敏日志。
- 没有访问或修改正式`data/app.db`，没有写入真实店铺链接、Webhook、令牌、账号或业务数据；没有访问或修改桌面备用版。

### 14.4 测试、构建与Git

- 聚焦操作历史、选择恢复、接管、备份健康、辅助文件清理和HTTP路由契约：`24 passed`。
- 最终完整测试：`118 passed, 2 skipped`；仅保留既有Starlette TestClient/httpx弃用警告。Python`compileall`在隔离字节码目录通过，两份AirScript通过Node语法检查。仓库内既有`__pycache__`权限导致的首次编译写入失败没有覆盖或删除缓存，改用隔离目录后验证成功。
- `build_web_exe.ps1`双程序构建成功。`GTeamOpsHub.exe`大小24,431,183字节，SHA-256为`F5F9799D9B04EA017D5F3641EE3662658C5212A7F560BD2E38D3CAE3DA03CCF3`；`GTeamOpsMaintenance.exe`大小18,072,584字节，SHA-256为`CEF291E7A65376C7045B784E703907DFDF65C9283C910A0689389F9ACFCE9767`。
- 最终双EXE在隔离目录通过真实流程冒烟：Web`/health`和初始化页正常，自动备份1份；维护程序成功列出、预览并恢复，数据库为迁移版本`0004_database_restore_history`、完整性`ok`、恢复历史1条，并生成`pre_restore`保险备份。备份目录最终只有2份正式`.db`，辅助文件0个，8765端口无监听。
- 开发开始时分支为`main`，本地和`origin/main`均指向`c2ac55f`，工作区包含第一至第三阶段未提交成果；全程未覆盖这些改动。用户已明确要求本轮全部验证后提交并推送，本阶段提交与远端结果以Git历史为准。

### 14.5 人工操作与后续阶段

- 第一至第二阶段引入的物流版本11和Listing版本4 AirScript仍需用户在WPS中完整替换并测试连接；Webhook和令牌不变时无需重新生成，也不得写入文档或Git。
- 看到`running`或`rollback_running`时先确认原窗口、请求和程序已经停止；有效租约未过期的提示属于安全保护，不应手工删除锁或修改数据库绕过。
- 重要恢复前仍建议管理员手工备份并点击完整校验；整库恢复继续按维护程序的`list-backups`、`preview-restore`和显式确认流程执行。
- 第五阶段可在出现具体业务需求时建设持久化任务中心和新业务模块；第六阶段再评估公网多用户、正式数据库与服务器密钥管理；第七阶段为部署监控、集中日志、灾备演练和运维交接。当前不为未知公网需求提前增加复杂度。

## 15. 2026-08-15 本地轻量界面设计系统与工作台首页改版

### 15.1 需求与决策

- 用户希望参考个人Hexo博客的美感提升系统界面，但不完整照搬博客，并要求兼顾用户操作流畅与界面美观，首页至少具有花瓣飘落效果。
- 继续使用FastAPI、Jinja2和原生JavaScript/CSS，不为当前页面复杂度引入Vue或完整前端工程；先建立本地轻量设计系统，未来只有在复杂交互需求明确后才单独评估框架迁移。
- 不复制Butterfly博客主题，不使用其CDN脚本、远程字体或背景图片。正式EXE必须离线可用，装饰效果不得影响表单、表格、批量操作和移动端导航。
- 首页承担适量品牌氛围，数据密集业务页继续保持低干扰；所有既有路由、DOM业务钩子、CSRF、用户隔离、数据库、物流、Listing和AirScript契约保持不变。

### 15.2 实际修改

- 新增`static/theme.css`，集中定义浅色、深色主题的颜色、表面、边框、阴影和圆角变量；主题选择保存在当前浏览器本机，并在页面样式加载前应用，减少主题闪烁。
- 重构`static/app.css`，统一侧栏、导航、卡片、按钮、表单、表格、状态提示、对话框和登录页视觉；保留原有类名，使既有业务模板直接继承新设计。
- 重构基础模板：Unicode导航符号替换为本地内联SVG图标；增加移动端抽屉侧栏、遮罩、键盘Esc关闭、明暗主题按钮和动态效果按钮；新增本地`favicon.svg`。
- 重做工作台首页，增加中等高度品牌区、今日问候、快捷操作、现有能力概览和四个常用业务入口；没有伪造订单量、销售额等数据库尚未提供的指标。
- 新增`static/petals.js`。花瓣只在`/dashboard`加载，使用预渲染Canvas精灵和限制后的设备像素比；桌面最多32片、中等屏幕22片、移动端12片，页面后台暂停，不捕获鼠标事件，可由用户关闭，并强制遵循`prefers-reduced-motion`。
- `app.js`保留原`GTeam.api/showStatus/buttonBusy`公共接口，并新增主题、动态偏好、移动导航、日期和分时问候逻辑。
- 登录、初始化和修改密码页面接入同一主题变量和本地站点图标；README、Web启动说明、架构规范和后续开发约束同步记录本地资源及动效边界。
- 新增Web契约测试，固定主题资源、首页花瓣作用域、开关元素、本地静态文件可访问性和无外部页面依赖。

### 15.3 兼容性与安全边界

- 没有修改数据库模型、迁移、API请求或响应、表单字段名、CSRF、用户/店铺隔离、国家站点映射、货代客户端、共享表写入与恢复流程。
- 没有修改物流版本11或Listing版本4 AirScript；表头识别、人工列、公式列、货代兜底、审计、并发锁和恢复契约保持不变。
- 所有新增前端资源位于项目`g_team_ops/web/static`并随PyInstaller打包；实际浏览器渲染检查没有发现外部网络请求或控制台错误。
- 没有访问或修改正式`data/app.db`，没有写入真实账号、密码、店铺链接、Webhook或令牌；没有访问或修改桌面备用版。
- 花瓣Canvas覆盖首页业务内容，但低于侧栏、移动遮罩和浏览器顶层弹窗，并禁用指针事件。动态关闭偏好仅保存在当前浏览器，不写数据库，也不跨用户设备同步。

### 15.4 测试、视觉检查、构建与Git

- 新增前端契约测试后完整测试通过：`119 passed, 2 skipped`；仅保留既有Starlette TestClient/httpx弃用警告。
- Python`compileall`在隔离字节码目录通过；`app.js`、`petals.js`和两份AirScript均通过Node语法检查；`git diff --check`通过。
- 使用系统Edge和隔离临时数据库实际渲染桌面浅色、桌面深色、手机页面及手机侧栏；手机模拟“减少动态效果”时页面状态为静态，侧栏开关和关闭均正常，浏览器没有外部请求或控制台错误。
- `build_web_exe.ps1`双程序构建成功。全屏花瓣确认后的`GTeamOpsHub.exe`大小24,439,012字节，SHA-256为`9164E0622B14085291311F07DA9A2993DB1DEEB2A2090E51097CF702D6D04968`；`GTeamOpsMaintenance.exe`大小18,073,292字节，SHA-256为`8806678843CE4E28EB4930ABD18A27DE18F28A914008D98B7F906785228525B7`。
- 新Web EXE在隔离目录完成真实初始化、登录和工作台访问；主题、公共样式、公共脚本、花瓣脚本和SVG站点图标均返回HTTP 200。维护EXE成功列出隔离库的每日自动备份。测试进程和含测试账号的隔离目录已经清理。
- 本轮开始时分支为`main`，本地与`origin/main`均指向`5905f29`且工作区干净。结束时只有本轮前端、测试、文档及重新构建的双EXE改动；用户本轮没有要求提交或推送，因此保持未提交、未暂存状态。

### 15.5 人工操作与遗留事项

- 用户无需修改数据库、货代、店铺或AirScript配置；重新运行新`GTeamOpsHub.exe`即可使用。若浏览器仍显示旧缓存样式，可执行一次强制刷新。
- 侧栏“深色/浅色”和“动态/静态”按钮分别保存当前浏览器的显示偏好；操作系统启用“减少动态效果”时，花瓣保持关闭。
- 本轮统一了全部业务页的基础组件，但只重新组织了工作台首页的信息架构。后续新增具体业务模块时，应在不改变业务契约的前提下逐页优化高密度表单和表格，不一次性重写所有业务模板。
- 是否引入Vue等前端框架继续延后到出现复杂客户端状态、可复用交互组件或大规模页面维护成本时评估，并作为独立阶段执行。

### 15.6 2026-08-16 全屏花瓣覆盖确认调整

- 用户确认希望花瓣在首页全屏范围内更加明显。Canvas层级由业务内容下方调整到内容上方，同时继续低于侧栏、移动端遮罩和弹窗，`pointer-events: none`保持不变。
- 为兼顾观感与可读性，桌面密度由28片调整为32片，平板为22片，手机由14片降为12片；单片尺寸由8–16调整为7–14，透明度由0.28–0.62降低为0.22–0.52。
- 增加静态样式契约断言，固定全屏层级与鼠标穿透；花瓣仍只在工作台首页加载，其他业务页面、后台暂停、用户开关和减少动态效果行为不变。
- 使用系统Edge和隔离数据库实际渲染，确认Canvas为1440×1000、层级8、鼠标穿透且无控制台错误；隔离账号与数据库已清理。
- 完整测试再次通过：`119 passed, 2 skipped`；Python编译、前端脚本、两份AirScript和`git diff --check`通过。双EXE重新构建并通过隔离真实登录：首页、覆盖层样式及花瓣脚本均返回HTTP 200，维护程序正常列出隔离自动备份。
- 未修改正式数据库、业务API、AirScript、店铺配置或桌面备用版；本轮前端改动继续保持未提交、未暂存状态，没有推送GitHub。

## 16. 2026-08-16 沉浸式全屏壁纸首页与模块独立壁纸

### 16.1 需求与决策

- 用户确认采用接近个人博客氛围但适合运营系统的方案，并提供8张本机图片；由开发侧决定首页、登录页和各模块的图片分配，同时要求控制透明度、加载时间和操作干扰。
- 首页改为全屏壁纸、较丰富的花瓣和紧凑图标入口；入口仍保留短标签和可访问名称，避免纯图标降低识别效率。主题、动态、修改密码和退出固定在顶部工具栏。
- 业务模块不照搬博客首页：继续保留完整侧栏、表单、表格和状态结构，只在底层使用独立壁纸，并通过更强页面遮罩与高不透明内容表面保证操作优先。
- 继续使用FastAPI、Jinja2和原生JavaScript/CSS，不引入Vue、Hexo主题、CDN、远程字体或运行时外部图片依赖。

### 16.2 图片分配与性能处理

- 用户提供的8张原图只作为只读来源，没有改写本机原图；正式项目内生成语义化副本：`dashboard.webp`使用人物与猫、`tracking.webp`使用水上鸟居、`carriers.webp`使用深蓝云层、`shops.webp`使用日式街道、`inventory.webp`使用紫色城市、`operations.webp`使用大树秋千、`admin.webp`使用草地山丘、`auth.webp`使用樱花乡路。
- 所有副本保存在`g_team_ops/web/static/wallpapers`，最长边限制为2560像素，统一转换为WebP并移除来源元数据。8张原图合计24,488,575字节，项目副本合计1,722,882字节，约减少93%，单张均小于500,000字节。
- Windows本机MIME表可能把WebP返回为通用二进制；应用工厂现显式注册`.webp -> image/webp`，源码运行和PyInstaller程序行为一致。
- 页面只加载当前CSS命中的本地壁纸，不读取桌面路径；正式EXE继续离线运行。

### 16.3 实际修改

- `base.html`为工作台首页增加独立`portal-shell`：首页不再渲染常驻侧栏，改用玻璃顶部工具栏和全屏内容区；其他页面继续走原有`shell`与响应式侧栏。新增修改密码和退出图标，不改变原表单动作或CSRF字段。
- `dashboard.html`移除占用空间的概览卡片和大功能卡片，改为问候、日期、本机状态及物流、货代、店铺、库存、操作恢复、管理员账号六个紧凑入口；普通用户不会显示管理员入口。
- `app.css`增加页面到壁纸的本地映射、首页电影感渐变、玻璃工具栏和图标Dock；业务页浅色与深色分别使用可读遮罩。登录、初始化和修改密码页共用`auth.webp`，表单卡片保持实色可读。
- 移动端在390像素视口内完整显示管理员六个入口，不产生页面横向溢出；极窄环境仍保留Dock内部滚动保护。
- 花瓣桌面/中屏/移动数量调整为48/28/14，单片尺寸6–13、透明度0.18–0.43；Canvas继续限制设备像素比、后台暂停、支持用户关闭和`prefers-reduced-motion`，并保持`pointer-events: none`且低于功能入口。
- Web契约测试新增首页门户/侧栏作用域、认证页标识、八张WebP访问、MIME类型、体积上限、页面壁纸映射和花瓣性能策略断言；README、Web启动说明、架构规范和后续开发约束同步更新。

### 16.4 兼容性与安全边界

- 没有修改数据库模型、Alembic迁移、业务API、表单字段名、CSRF、用户/店铺隔离、国家站点映射、货代客户端、共享表写入、审计、并发保护或恢复逻辑。
- 没有修改物流结构版本11或Listing结构版本4 AirScript；表头识别、人工列、公式列、货代兜底和既有协议保持不变。
- 没有访问或修改正式`data/app.db`，没有写入真实账号、密码、店铺链接、Webhook、令牌或业务数据；没有访问或修改桌面备用版。
- 用户提供图片的授权与后续分发范围仍由用户负责；系统没有从博客仓库或互联网复制其他图片或主题资源。

### 16.5 测试、视觉检查、构建与Git

- 最终完整测试通过：`119 passed, 2 skipped`；仅保留既有Starlette TestClient/httpx弃用警告。Python`compileall`、`app.js`、`petals.js`、两份AirScript和`git diff --check`均通过。
- 使用系统Edge和隔离数据库实际检查登录页、1440×900桌面首页、深色业务页和390×844移动首页：没有控制台错误、外部网络请求或页面横向溢出；花瓣Canvas层级8、功能Dock层级15，Canvas不接收鼠标事件；移动Dock滚动宽度与可视宽度均为364像素。
- `build_web_exe.ps1`双程序构建成功。`GTeamOpsHub.exe`大小26,163,219字节，SHA-256为`D6229D29F2BF4A74B4F226D399AA543E30780A858A3B9103BEE57760DAAB11E1`；`GTeamOpsMaintenance.exe`大小18,072,403字节，SHA-256为`74672C7DD1F2DB8C028565C82616FFAB77B653A2B3321A6ADA5074290AD2F218`。
- 新Web EXE在隔离目录完成`/health`、初始化、登录和首页访问，门户与花瓣均存在且无外部链接；8张壁纸全部返回HTTP 200、`image/webp`和预期字节数。维护EXE正常列出隔离库每日自动备份；隔离进程、账号、数据库和日志已清理，8765与8878端口均无监听。
- 开发开始和结束时均在`main`，本地与`origin/main`指向`5905f29`。开始时已有上一轮未提交前端与EXE改动，本轮在其上增量修改且未覆盖；用户本轮没有要求提交或推送，因此全部修改保持未提交、未暂存状态。

### 16.6 人工操作与遗留事项

- 用户无需修改数据库、货代、店铺或AirScript配置；使用新`GTeamOpsHub.exe`即可看到效果。浏览器若保留旧CSS，可强制刷新一次。
- 首页顶部“动态/静态”继续控制花瓣；操作系统启用“减少动态效果”时保持静态。业务页壁纸不会增加外部请求，图片首次加载后由浏览器本地缓存。
- 后续若更换图片，只替换对应语义化WebP并维持尺寸/体积上限即可；不要修改CSS映射或在模板中写入桌面绝对路径。
- 仍不为未知需求提前迁移Vue或公网部署；出现复杂客户端状态或确定的公网多用户需求时再独立评估。

### 16.7 2026-08-16 模块壁纸清晰度与实色工作区调整

- 用户要求业务模块提高壁纸可见度，保持原图清晰，并让承载功能的框体完全不透明；工作台首页效果不变。
- 模块浅色遮罩调整为20%–30%，深色遮罩调整为32%–46%；删除模块背景上的模糊装饰色块和额外径向渐变，最终计算背景只有“薄明暗遮罩 + 当前模块原图”两层，不对壁纸应用模糊滤镜。
- 所有业务卡片改用`surface-solid`，取消卡片`backdrop-filter`；页面一级标题新增完全不透明标题容器，输入框、表格、状态区和侧栏继续保持实色。移动端标题容器同步缩小内边距。
- 增加Web样式契约断言，固定薄遮罩、实色标题、实色卡片和无卡片模糊。同步更新Web启动说明、架构规范与后续开发约束。
- 使用系统Edge和隔离数据库实际检查深色物流页：鸟居、天空与水面保持清晰，标题、状态卡、店铺选择、文本框与操作区均不透出壁纸；没有控制台错误、外部请求或横向溢出。
- 完整测试通过：`119 passed, 2 skipped`，仅保留既有TestClient弃用警告；`git diff --check`通过。
- 双EXE重新构建成功。`GTeamOpsHub.exe`大小26,163,727字节，SHA-256为`45B467A0575328040F49338684EE39B8EDA0A23ED837CB28FE24B85E8ED23BF2`；`GTeamOpsMaintenance.exe`大小18,071,943字节，SHA-256为`F620CA534F448079627860CDAECA919C6A7887BF4B3B6C0DB186298A45D13B04`。
- 新Web EXE在隔离目录通过`/health`和`app.css`检查，确认薄遮罩、实色卡片和无卡片模糊均已打包；隔离进程、数据库和日志已清理，8765与8878端口无监听。
- 未修改正式数据库、业务API、AirScript、店铺配置或桌面备用版；当前仍在`main`，修改保持未提交、未暂存，没有推送GitHub。

## 17. 2026-08-21 Listing库存销售共享表与补货规则版本5

### 17.1 需求与决策

- 用户要求基于领星Listing全字段导出升级库存销售共享表，同时保持现有公司口径：`预留=FBA待调仓+FBA预留`、`在途=FBA计划入库+FBA标发在途`，不引入FBA入库中、不可售、当前有效库存、已发在途、箱规等不适用字段。
- 评分与评论合并为`评分/评论数`及`上次评分/评论数`；保留昨日广告费和上次昨日广告费，增加7/14/30日广告费、销售额、30日广告费率和30日实际成交均价；覆盖月数改为覆盖天数。
- 销量预测采用互不重叠的M1/M2/M3窗口，并按30日销量分为低、中、高三档权重。30日销量只用于选档，不再把基础预测与30日销量二次混合，避免同一数据重复加权。
- 业务计算留在WPS公式中，Python只解析和写入原始数据。新增工作簿级`规则配置`工作表，集中维护规则版本、销量分界、目标覆盖天数、趋势阈值、高广告费率门槛和三档权重；改变策略无需修改系统代码。
- `最终补货月销`默认使用公式；人工输入静态数字后视为明确覆盖值，后续导入、公式补齐和规则初始化均不覆盖。促销、低评分、广告变化和异常趋势只影响状态、可信度和提示，不自动乘隐藏修正系数。

### 17.2 实际修改

- `g_team_ops/listing.py`把Listing安全协议提升到结构版本5；领星必需字段调整为20个实际使用表头，完整导出的已知非使用列静默忽略，未知新列继续提示。增加价格、周期广告费、周期销售额和销量窗口完整性检查；后端不再计算系统月销。周期指标空白会明确清除旧周期值并让公式显示数据异常，库存空白仍按既有FBM保护规则保留。
- 目标Listing子表改为45个必需表头和可选`优惠价`。评分/评论、昨日广告费、库存和销量继续按新日期滚动上次值；价格、周期广告费与销售额直接更新；人工基础列不写入。
- `airscripts/Listing库存销售自动回填.js`新增`规则配置`初始化、参数合法性校验、按实际表头生成公式和规则状态统计。M1、M2、M3分别使用最近7日、第8—14日和第15—30日速度；覆盖和建议补货通过最终月销折算日均需求。
- Listing快照现在保存公式文本而不是公式显示结果，条件恢复可正确还原公式。预览后规则版本变化会停止正式回填；规则版本写入操作摘要。原有MSKU唯一、ASIN冲突、旧日期跳过、同日修正、写前条件和部分恢复契约保持。
- 库存销售页面新增“初始化 / 检查规则配置”，预览增加价格/优惠价、评分评论、周期广告费、周期销量、周期销售额、数据窗口警告、规则版本、公式行和人工覆盖行统计。
- 新增和更新Listing解析、AirScript、Web接口及模块路由契约测试；同步更新README、Web启动说明、AirScript升级说明、操作恢复说明、架构规范、后续开发约束、代码审计报告、新任务交接说明和完整库存销售使用说明。

### 17.3 兼容性、安全与数据边界

- 没有新增数据库表或迁移，继续使用本机SQLite、现有用户隔离、店铺/国家映射和操作历史。没有安装或迁移SQL Server、MySQL或其他服务型数据库。
- 没有修改物流AirScript结构版本11、物流表头、货代查询、共享表物流回填或货代兜底协议；Listing与物流仍使用独立Webhook和令牌。
- 外部调用继续使用既有超时、有限重试和脱敏日志；没有在代码、测试或文档记录密码、令牌、Webhook、完整请求正文或真实店铺链接。
- 没有访问或修改正式`data/app.db`，没有修改桌面备用版。用户提供的领星文件只做只读解析验证，没有改写或复制进项目；识别出31个可导入唯一MSKU、8个重复MSKU、0个销量窗口异常，重复MSKU继续安全跳过且不记录具体业务值到文档。
- 规则初始化是用户显式触发的结构维护动作，首次执行前仍需WPS工作簿副本或历史版本；日常Listing正式导入继续具有系统操作历史、并发锁和条件恢复双重保险。

### 17.4 测试、构建与Git

- Listing聚焦测试：`11 passed`。最终完整测试：`123 passed`；仅保留既有Starlette TestClient/httpx弃用警告。
- Python在隔离字节码目录通过`compileall`；`app.js`、`petals.js`、物流版本11和Listing版本5两份AirScript均通过Node语法检查；`git diff --check`通过。
- `build_web_exe.ps1`双程序构建成功。`GTeamOpsHub.exe`大小26,167,274字节，SHA-256为`8D74862B1407CC277572661F1898331AD8E6B9812B657C96DBD5877B25746844`；`GTeamOpsMaintenance.exe`大小18,072,202字节，SHA-256为`63DD875C28CE8A071927F5C6CCD152E27D44F93B2B1A39025E86B052831B6C4B`。
- Web EXE在隔离目录通过`/health`、初始化页和本地`app.js`访问检查；维护EXE成功读取隔离数据库并列出自动备份。测试进程、隔离数据库和临时目录已清理，8765端口没有遗留监听。
- 本轮开始时`main`工作区干净，本地与`origin/main`均为`4d845fe`。用户明确要求完成后提交并推送GitHub；最终提交与远端结果以Git历史为准。

### 17.5 用户需要完成的WPS操作与遗留事项

- 先备份WPS工作簿，按`库存销售表格模块使用说明.md`把每个Listing子表迁移到版本5表头并核对旧人工状态；不要直接把旧`Listing状态`值当作新`补货状态`。
- 将项目内`airscripts/Listing库存销售自动回填.js`完整替换到WPS Listing脚本。Webhook和令牌未变化时无需重新生成；任何敏感连接值只在本机“我的店铺”保存。
- 在网页选定店铺和国家后点击“初始化 / 检查规则配置”，检查`规则配置`的默认参数并按公司策略调整；每次改变规则后同步升级规则版本号，再重新预览文件。
- 当前无法在本机测试环境执行真实WPS公式计算和真实共享表回填；首次在正式共享表启用时应先用副本或少量SKU验证公式、状态和恢复，再覆盖全部店铺。
- 用户文件中的8个重复MSKU需要在实际导入预览中人工核对店铺/站点/Listing口径；系统不会猜测合并。

## 18. 2026-08-22 Listing真实WPS回填排错与结构版本6

### 18.1 需求与只读核验结论

- 用户提供两份连续日期的领星Listing全字段导出和一份纯粹美国站WPS测试工作簿，要求逐SKU核对源数据与共享表，解释趋势、异常、月销、库存、广告和可信度规则，并把建议补货量调整为5的倍数。
- 三份桌面工作簿均只读分析，没有改写或复制进正式项目。两份源文件和目标Listing子表各有32个唯一MSKU，无重复、无缺失、无多余键。按MSKU和表头名称核对最新值及上次值后，库存、销量、广告费、销售额等原始字段不存在“源表非零、共享表写成0”的差异。
- 发现两个真实缺陷：32个SKU共有224个规则相关公式错误，集中在趋势差异率、销量状态、预测可信度、系统建议月销、建议补货量、库存状态和广告状态；根因是WPS无法正确计算中文规则工作表标签的跨表引用。另有2个SKU的本次及上次评分/评论文本被WPS自动解释成日期序列，共4个受影响单元格。
- 30日实际成交均价在31个有销量SKU中有26个与当前/优惠价相差不超过2%，但仍有3个偏差超过5%，最大偏差44.3%。该列可用于发现历史折扣、Coupon、组合装变化或销售额口径异常，因此保留为人工诊断指标，继续不参与补货公式和隐藏修正。
- “5的倍数”按用户原文采用向下取整：理论缺口13取10、5取5、1至4取0。为便于后续公司策略变化，倍数不写死为5，而是新增规则参数，默认值为5。

### 18.2 实际修改与业务契约

- Listing AirScript提升到结构版本6。规则工作表标签改为ASCII `ListingRules`，A1中文标题不变；点击“初始化 / 检查规则配置”时自动把版本5旧标签`规则配置`重命名并保留参数，然后重新安装全部标准公式，规避WPS `#NAME?`。
- `评分/评论数`和`上次评分/评论数`在批量写入及条件恢复前强制设为文本格式，避免`5/4`、`5/1`等内容被自动转成日期。
- `ListingRules!B14`新增“补货取整倍数”，默认5且必须是正整数。建议补货量先计算非负理论缺口，再通过`INT(缺口/倍数)×倍数`向下取整；清库存/停售仍为0，新品观察/暂缓补货仍不自动推荐。
- AirScript新增公式错误行统计，Python客户端会在预览、正式执行和规则初始化前拒绝存在`#NAME?`、`#REF!`等错误的共享表，不再只检查“单元格里是否有公式”。页面预览和规则提示同步显示公式错误行数。
- 45个必需Listing表头、可选优惠价、MSKU/ASIN保护、同日修正、上次值滚动、规则版本并发保护、人工最终月销保护、操作历史与条件恢复契约均保持不变。物流版本11、货代查询、物流回填和轨迹协议未修改。

### 18.3 公式说明补充

- 使用说明补全趋势差异率的M1/M3比较及边界、异常原因拼接条件、三档系统建议月销、最终月销优先级、库存状态、广告状态和三级预测可信度口径。
- 广告状态仍由WPS公式计算：30日广告费率达到默认25%为高广告费；否则比较最近7日与第8—30日广告日均，比例大于1.2为上涨、小于0.8为下降。
- 预测可信度是可解释规则等级而非统计概率；低可信度时最终补货月销默认留空，必须由运营结合补货状态、链接状态和异常原因人工判断。

### 18.4 测试、构建与兼容性

- Listing聚焦测试：`12 passed`。完整测试：`124 passed`；仅保留既有Starlette TestClient/httpx弃用警告。
- Python隔离字节码编译、Listing版本6与物流版本11 AirScript、前端公共脚本语法检查及`git diff --check`均通过。
- 首次构建因正式`GTeamOpsHub.exe`仍在运行而无法替换；核对占用进程路径确实位于正式项目后，只关闭两个正式Web程序进程，没有终止其他程序，也没有改写数据库。随后双EXE构建成功。
- `GTeamOpsHub.exe`大小26,167,918字节，SHA-256为`5AA52BDB6882010337EB7BE3CD4D3FBBC492215E8E8646E3BB00203981101428`；`GTeamOpsMaintenance.exe`大小18,071,645字节，SHA-256为`FCCFCE322249A57AB2DC620F82C11F108A2146B1818EF03C8E676186610E1C2D`。
- Web EXE在隔离目录通过健康页、初始化页和本地静态资源检查；维护EXE成功读取隔离数据目录。隔离进程和临时目录已清理，8765端口无遗留监听。
- 本轮开始时`main`与`origin/main`一致，基线提交为`7b9e722`。本轮改动尚未提交、尚未推送；用户未授权Git提交或GitHub推送。

### 18.5 用户需要完成的WPS操作与遗留事项

- 先创建WPS工作簿副本或确认历史版本可用，再完整替换项目内Listing版本6 AirScript。共享表表头不需要再次调整。
- 在网页选择店铺与国家，点击“初始化 / 检查规则配置”。该动作会把旧`规则配置`标签迁移为`ListingRules`并覆盖修复标准公式，但保留人工最终月销。
- 检查`ListingRules!B4:B14`及`B17:D19`；默认补货倍数为5。任何规则变化都需要同步升级B4规则版本，再重新预览。
- 再次导入最新领星文件以修正已经变成日期序列的评分/评论数。正式导入前应确认页面显示公式错误0行；若不是0行，系统会停止，不能绕过。
- 当前无法从本地测试环境直接执行真实WPS计算引擎。版本6修复依据真实工作簿XML、32个SKU全量对账和AirScript回归测试完成；用户首次升级后仍应在工作簿副本中抽查公式结果，再用于正式补货决策。

## 19. 2026-08-22 Listing规则交互与结构版本7

### 19.1 需求、只读核验与决策

- 用户提供更新后的店铺共享表，要求核对趋势差异率空白、完整说明异常原因规则、取消公式对人工链接状态的依赖，并调整最终补货月销、补货取整、可选列和规则配置维护方式。
- 工作簿只读核验发现32个SKU、236个公式且没有公式错误；10个趋势差异率空白均因30日销量低于规则表默认趋势判断门槛10，属于既定低销量保护，不是公式缺失或回填故障。
- `链接状态`改为完全人工记录，系统公式不再据此调整预测可信度、异常原因、系统建议月销、最终补货月销、覆盖天数或建议补货量；运营可自由记录高退标签、链接异常等业务信息，再通过人工补货状态或最终月销干预决策。
- 低可信度不再令最终补货月销留空；正常补货时高、中、低可信度均默认采用系统建议月销。中可信度对应的最终补货月销以矢车菊蓝主题浅色60%填充提醒复核，颜色只作提示，不改变公式结果。
- 人工覆盖最终补货月销只在当次表内立即影响覆盖和补货结果；下一次正式更新或再次初始化公式时会被最新公式替换，不沿用上一次判断。
- 建议补货量改为向上补足到规则倍数，默认5：理论缺口1取5、13取15、0仍为0。`运营备注`改为非必需可选列。

### 19.2 ListingRules与实际修改

- Listing AirScript提升到结构版本7。`ListingRules`增加公式字段说明区，记录趋势差异率、销量状态、异常原因、预测可信度、系统/最终月销、覆盖天数、库存状态、广告状态和建议补货量的输入、可能输出及计算策略。
- 日常正式回填只读取并验证工作簿现有规则参数，不覆盖运营修改；只有用户显式点击“初始化 / 检查规则配置”才清空并恢复系统默认参数、权重、说明、公式、样式和保护状态。初始化页面及确认提示明确告知该动作会重置自定义规则和人工最终月销。
- `ListingRules`初始化后使用无密码工作表保护，防止日常误改；需要修改时可在WPS中取消保护，调整B4:B14、B17:D19等参数并升级规则版本，必要时再手工保护。说明文字不是可执行规则，真正计算仍只读取参数单元格。
- AirScript为中可信度最终补货月销范围安装公式型条件格式，填充色为`#9DC3E6`；初始化和日常更新都会校验并补齐该提示规则。
- Python Listing协议提升到结构版本7，`运营备注`从必需表头移至可选表头；预览和规则初始化结果增加规则表保护及中可信度高亮状态。网页说明、使用说明、AirScript升级说明、README、架构规范、Web启动说明、操作历史说明、后续开发约束和审计报告同步更新。
- 原有MSKU唯一性、ASIN冲突、按表头识别、旧日期拒绝、同日修正、上次值滚动、规则版本并发保护、公式错误阻断、操作历史与条件恢复契约保持不变；物流版本11及货代、物流回填协议未修改。

### 19.3 兼容性、安全与数据边界

- 没有新增数据库表或迁移，没有更改本机SQLite、用户隔离、店铺配置、国家站点映射、Webhook或AirScript连接配置。
- 没有访问或修改正式`data/app.db`，没有记录账号、密码、令牌、Webhook、完整请求正文、店铺链接或具体SKU业务值；用户工作簿只做只读分析，没有写回或复制进项目。
- 没有修改桌面备用版，没有提交或推送GitHub。开始时工作区已有上一轮未提交修改，本轮仅在正式网页版项目上增量修改并完整保留。
- 工作簿中另有2个历史`上次评分/评论数`仍为旧版遗留的日期序列值；当前`评分/评论数`已经是正确文本。下一次跨日期导入会把当前文本滚入上次值并自然修正，也可在备份后提前手工修正。

### 19.4 测试、构建与Git

- Listing聚焦测试：`12 passed`。完整测试：`124 passed`；仅保留既有Starlette TestClient/httpx弃用警告。
- Listing版本7与物流版本11 AirScript、前端公共脚本、Python隔离字节码编译及`git diff --check`均通过。
- 构建前核对并关闭两个路径确属正式项目的旧Web进程，没有终止其他应用或改写数据库；随后双EXE构建成功。
- `GTeamOpsHub.exe`大小26,168,545字节，SHA-256为`FDBB263533749C778BF494884AEB20D10B83916D00C0745B2C162FFADA6FA9C7`；`GTeamOpsMaintenance.exe`大小18,072,819字节，SHA-256为`18AFD585B921DA4B2C2176C4C19C42AF66F56AEFA88B69500FE1B2C855F1AEAC`。
- Web EXE在隔离目录通过`/health`和初始化页检查；维护EXE在全新隔离数据库成功返回空备份列表。隔离进程、数据库和临时目录均已清理，8765端口没有遗留监听。
- 本轮仍在`main`，基线提交为`7b9e722`，远程地址保持正式GitHub仓库；修改未暂存、未提交、未推送。

### 19.5 用户需要完成的WPS操作与遗留事项

- 先创建WPS工作簿副本或确认历史版本可恢复，再把项目内Listing版本7 AirScript完整替换到WPS脚本；共享表不要求存在`运营备注`列。
- 使用新版`GTeamOpsHub.exe`启动系统，在网页选择店铺与国家后点击一次“初始化 / 检查规则配置”。该动作会重置现有自定义规则和人工最终月销，安装版本7公式、说明、保护及中可信度高亮。
- 若要自定义规则，先在WPS取消`ListingRules`保护，只修改规则参数和权重单元格，更新B4规则版本后可重新保护；日常正式数据更新会继续使用这些工作簿规则，不会恢复系统默认值。
- 下一次导入最新领星文件时，任何上次人工填写的最终补货月销都会被新版公式替换；运营需要按最新数据重新复核中、低可信度行，并用补货状态或最终补货月销表达当次判断。
- 当前本地环境仍不能直接运行真实WPS公式引擎；保护、条件格式和公式逻辑已通过AirScript测试与工作簿只读对账验证，首次正式使用仍应先在副本中抽查，再用于补货决策。

### 19.6 2026-08-22 ListingRules取消保护兼容修复

- 用户在真实WPS中点击“初始化 / 检查规则配置”时收到“无法取消ListingRules保护”错误。核对截图与代码后确认，旧版升级场景中的规则表可能尚未受保护，但部分WPS运行环境会在未保护工作表上调用`Unprotect()`时抛出异常；原错误提示又把所有异常统一解释为密码问题。
- Listing版本仍保持结构版本7，不改变协议或表头。新增规则表保护状态检测：只有`ProtectContents`、`ProtectDrawingObjects`或`ProtectScenarios`表明确实处于保护状态时才调用`Unprotect()`；未保护的旧规则表直接进入初始化。若用户自行设置了密码保护，脚本不会猜测或绕过密码，而是提示先在WPS手工取消。
- AirScript测试桩新增真实兼容场景：未保护工作表调用`Unprotect()`会抛错；初始化必须跳过该调用、恢复默认规则并重新施加无密码保护。同步补充AirScript升级说明和库存销售使用说明中的排错步骤。
- Listing聚焦测试最终为`12 passed`，完整测试为`124 passed`；仅保留既有Starlette TestClient/httpx弃用警告。Listing与物流AirScript语法、Python编译和差异检查继续执行最终验证。
- 构建前只关闭路径精确属于正式项目的两个旧Web程序进程，没有访问或修改正式数据库。双EXE重新构建成功：`GTeamOpsHub.exe`大小26,168,781字节，SHA-256为`357D1859D0224CA23875F4DC007B3443F58C233F8D2B63683EE58A19C97BD48B`；`GTeamOpsMaintenance.exe`大小18,072,918字节，SHA-256为`FB007E5E651BDAD0AB4872DB9C629084D58C154538CC22AB66023DD6D201E28F`。
- 新Web EXE在隔离目录通过健康页和初始化页检查；维护EXE在全新隔离数据库成功列出空备份目录。隔离进程、数据库和临时目录均已清理，正式程序保持停止。
- 本次兼容修复未修改物流协议、业务数据库、店铺配置、连接值或桌面备用版，仍未提交、未推送GitHub。用户必须把修复后的Listing版本7脚本再次完整替换到WPS；仅重启EXE不能更新WPS内已经保存的AirScript代码。

## 20. 2026-08-23 金山文档在线规则表恢复与Listing结构版本8

### 20.1 问题与决策

- 用户确认实际目标是金山文档在线共享表。该环境没有桌面WPS的“审阅/撤销工作表保护”入口，且真实运行中无法稳定通过单一`Unprotect()`调用解除系统此前施加的无密码保护。
- 自动保护规则表在金山文档在线场景会造成无法自助解锁，操作可靠性优先于防误改外观，因此Listing结构升级到版本8：新`ListingRules`保持未保护，不再自动调用`Protect()`。
- 防误改改由可见规则说明、B4规则版本号、预览后的规则版本并发校验、公式错误阻断和WPS历史版本共同承担。日常回填仍只读取规则，不重置参数；显式初始化仍会恢复系统默认规则和公式。

### 20.2 实际修改与恢复策略

- 取消保护依次兼容无参数、空字符串和对象密码参数三种在线接口签名；只有规则表确实受保护时才调用。
- 若系统生成的历史`ListingRules`仍无法解除保护，初始化不会删除它：先重命名为`ListingRules_旧保护`，重名时追加编号，再创建新的未保护`ListingRules`、恢复默认参数并重装所有标准公式。非系统标题的同名表不会被自动替换。
- `ListingRules`说明区扩展至A22:D38，明确记录在线规则表保持未保护以及修改参数后必须升级B4规则版本。初始化结果会报告是否保留了旧受保护规则表。
- Python端要求Listing结构版本8并解析旧规则表归档名称；库存销售页面和接口提示改为“规则表保持未保护”，不再指导在线用户寻找不存在的“审阅”菜单。
- README、Web启动说明、AirScript升级说明、库存销售使用说明、架构规范、操作恢复说明、后续开发约束和审计报告同步更新到Listing版本8。

### 20.3 测试、构建与边界

- AirScript测试新增两类兼容场景：在线接口只接受对象密码参数；所有取消保护签名均失败时保留旧表并新建未保护规则表。Listing聚焦测试`12 passed`，完整测试`124 passed`；仅保留既有Starlette TestClient/httpx弃用警告。
- Listing版本8、物流版本11和前端脚本语法检查、Python隔离编译及差异检查均纳入最终验证。
- 构建前只关闭路径精确属于正式项目的两个Web进程，没有访问或修改正式数据库。双EXE重新构建成功：`GTeamOpsHub.exe`大小26,168,091字节，SHA-256为`1F25D7B72A756321C12F9C11B948318E08D9422CBC65884BFA2381B3D41BB7E6`；`GTeamOpsMaintenance.exe`大小18,073,438字节，SHA-256为`99F7CB8CA442D70671D1816F54BA8C820AC6A499A8837D26BC272056C32A03B9`。
- 新Web EXE在隔离目录通过健康页和初始化页检查；维护EXE在全新隔离数据库成功列出空备份目录。隔离进程、数据库和临时目录均已清理，正式程序保持停止。
- 没有修改物流协议、正式数据库、用户/店铺/国家映射、Webhook、令牌或桌面备用版；没有把共享表链接或业务数据写入文档。当前修改仍未提交、未推送GitHub。

### 20.4 用户操作

- 必须把项目内Listing版本8脚本完整替换到金山文档AirScript并保存，再启动新版`GTeamOpsHub.exe`；仅更新EXE不会替换在线文档中保存的脚本。
- 先确认WPS历史版本或副本可用，再点击一次“初始化 / 检查规则配置”。若页面报告旧表已保留为`ListingRules_旧保护`，说明在线接口未能解锁旧表，但新规则表和标准公式已经恢复；旧表可暂时保留作为只读备份。
- 后续修改B4:B14或B17:D19后必须同步升级B4规则版本号并重新预览。不要手工重新保护在线`ListingRules`，否则可能再次失去在线解除入口。

### 20.5 2026-08-23 ListingRules保护与低可信度提示修正

- 用户在真实金山文档确认规则能正确写入，但新`ListingRules`没有设置保护；同时明确要求中可信度不着色，只有低可信度的`最终补货月销`使用矢车菊蓝浅色60%填充。补货状态优先级确认为：`清库存/停售`填0，`新品观察/暂缓补货`留空等待人工，其他状态默认使用系统建议月销。
- Listing AirScript升级到结构版本9。初始化时先按无参、空字符串和对象参数兼容旧保护解除，恢复默认规则后优先按金山文档官方对象参数施加无密码保护，再兼容无参和空字符串签名，并回读保护状态。保护调用返回但状态仍明确为未保护时会停止并报错，不误报成功。
- 规则表`A1:E40`默认锁定，`B4:B14`参数与`B17:D19`权重区解锁；同时尝试官方`AllowEditRanges`接口作在线兼容。因此网页版无需“审阅”菜单也能修改真正的规则输入，说明、标题和公式单元格仍防误改。`ListingRules!A38:D38`和页面提示同步更新。
- 条件格式安装函数改为只识别`预测可信度="低"`。若目标范围已存在版本8的“中可信度”系统条件，初始化或日常回填会将它修改为“低”；不能修改或删除旧条件时返回失败，不叠加两套蓝色规则。色值仍为`#9DC3E6`。
- Python端要求Listing结构版本9，并把回传字段与网页文案从“中可信度”改为“低可信度”。README、Web启动说明、AirScript升级说明、库存销售使用说明、架构规范、后续开发约束、操作恢复说明和代码审计报告同步更新。
- 自动化测试固定了四类补货状态公式、中转低提示色迁移、色值、规则表保护回读以及参数/权重区解锁。Listing聚焦测试`12 passed`；完整回归`124 passed`，仅保留既有Starlette TestClient/httpx弃用警告。Listing版本9 AirScript语法、差异空白和Python编译继续纳入最终检查。
- 首次构建时发现正式项目根目录的`GTeamOpsHub.exe`正由两个同路径进程占用；核对可执行路径后只停止这两个正式程序进程，未终止其他应用。双EXE重新构建成功：`GTeamOpsHub.exe`大小26,169,342字节，SHA-256为`28225B678A62148A1A6811496F42D198B91C870D7BC9DC706B2D6E721D384AEF`；`GTeamOpsMaintenance.exe`大小18,073,377字节，SHA-256为`C7996D7187A436849AE2F3922BAC7367CB91F0CB8D7C5592A5F3DB28F5B78954`。
- 新Web EXE在正式项目内的隔离目录通过`/health`与`/setup` 200检查；维护EXE成功读取同一隔离数据目录并列出自动备份。PyInstaller父子测试进程、隔离数据库、日志、备份和测试目录均已停止并清理，8765端口无监听。
- 本轮没有修改物流协议、正式数据库、用户/店铺/国家映射、Webhook、令牌或桌面备用版；没有记录敏感值。当前仍在`main`分支，修改未暂存、未提交、未推送。
- 用户必须把项目内版本9 `airscripts/Listing库存销售自动回填.js`完整替换到金山文档AirScript并保存，再使用新EXE点击一次“初始化 / 检查规则配置”。仅替换EXE不会自动替换在线文档已保存的AirScript。初始化后应抽查一行中可信度不着色、一行低可信度着蓝色，并确认参数区可编辑而说明区不可编辑。

## 21. 2026-08-25 Listing规则逐项说明、取消保护与趋势口径复核

### 21.1 需求、核对结果与决策

- 用户要求解释预测可信度为什么没有高、彻底取消无法稳定工作的规则表保护方案、把每一种可能结果的精确触发条件写入规则表，并核对趋势差异率示例。
- 对用户提供的测试工作簿只读核对；没有修改、复制或提交工作簿。工作簿现有预测可信度均为中或低，是因为每行至少命中了销量趋势、销量档位、广告状态、补货状态或FBA可售条件之一，不是“高”枚举失效。
- 预测可信度明确为优先级规则：低条件优先；未命中低且命中普通增长/下降、低于高销量分界、广告数据不足/高广告费/广告涨跌时为中；高必须同时满足高销量、销量稳定、正常或空补货状态、FBA可售大于0、广告正常或无广告。
- 复核确认趋势差异率现有`(M1-M3)/MAX(M3,1)`没有计算错误。M1与M3都换算为相同30天速度；直接比较7天总量和较早16天总量会产生周期偏差，稳定日销也会被误判为快速下降，因此不把现有等周期速度公式改成原始总量公式。
- 根据用户最终决定，取消规则表保护方案。系统不再设置工作表保护、单元格`Locked`或`AllowEditRanges`；旧受保护系统规则表仍会尝试安全解除，无法解除时保留为`ListingRules_旧保护`并创建未保护的新规则表。

### 21.2 实际修改与兼容性

- Listing AirScript升级到结构版本10，Python端同步拒绝版本10以下旧脚本。`ListingRules`初始化说明区扩展为A22:D71，按“一种结果一行”逐项列出趋势差异率、全部销量状态、预测可信度高/中/低、全部异常原因、三档系统建议月销、最终补货月销、覆盖天数、建议补货量、库存状态和广告状态的精确条件。
- 预测可信度公式把`广告状态=数据不足`明确归为中，确保广告数据不完整时不会误填高；高的公式结果只会在稳定高销量、FBA有货、补货状态正常且广告正常或无广告时出现。`链接状态`继续完全人工填写，不参与任何公式。
- 网页初始化说明和接口结果改为“规则表保持未保护，可直接编辑”；不再显示保护成功或要求查找审阅菜单。初始化仍会重置规则参数、说明、标准公式和人工最终月销，并保留低可信度最终月销的矢车菊蓝浅色60%提示。
- README、Web启动说明、AirScript升级说明、库存销售使用说明、架构规范、后续开发约束、操作恢复说明和代码审计报告同步更新为Listing版本10。物流版本11、货代连接、物流回填、用户隔离、店铺/国家映射和恢复协议未修改。
- 没有新增数据库迁移，没有访问或修改正式`data/app.db`，没有记录密码、令牌、Webhook、店铺链接或完整请求正文；桌面备用版没有访问或修改。

### 21.3 测试、构建与Git状态

- Listing聚焦测试`12 passed`；完整回归`124 passed`，仅保留既有Starlette TestClient/httpx弃用警告。
- Listing版本10与物流版本11 AirScript语法检查、Python隔离编译和`git diff --check`通过。
- 双EXE构建成功。`GTeamOpsHub.exe`大小26,167,893字节，SHA-256为`0BC1C10FC0C469E06C1B725553C0FFF92A00B9FFF8156703610B6DEF20EA091F`；`GTeamOpsMaintenance.exe`大小18,073,418字节，SHA-256为`3128A8296AC4FF2262A7D50160963E49B79D747FD5E6E37B6DF9CC51083555BA`。
- Web EXE在正式项目内的隔离目录通过`/health`和`/setup`检查，维护EXE成功读取同一隔离数据库的备份清单；隔离进程、数据库、日志、备份、Pytest目录、字节码和工作簿分析临时目录均已清理，8765端口无遗留监听。
- 当前分支为`main`，本轮开始基线为`7b9e722`，远程仍为正式GitHub仓库`origin`。用户已明确授权在本轮结束提交、推送并关机；提交前将再次检查差异、暂存内容和敏感文件，推送后核对远程分支再执行关机。

### 21.4 用户需要完成的WPS操作与遗留事项

- 先确认金山文档历史版本或工作簿副本可恢复，再把项目内版本10 `airscripts/Listing库存销售自动回填.js`完整替换到在线AirScript并保存；只更新EXE不会替换在线脚本。
- 使用新版`GTeamOpsHub.exe`启动网页，选择店铺和国家后点击一次“初始化 / 检查规则配置”。该动作会写入A22:D71详细说明、重装版本10公式、取消新规则表保护并重置旧人工最终月销。
- 初始化后核对规则参数和三档权重；如人工修改B4:B14或B17:D19，必须同步升级B4规则版本号并重新预览。低可信度最终月销应着蓝色，中可信度不着色。
- 现有测试工作簿不会被系统离线改写。用户替换在线脚本并初始化后，下一次正式导入才会把新规则和公式用于共享表；首次仍建议在副本中抽查高/中/低各一行再用于补货决策。
