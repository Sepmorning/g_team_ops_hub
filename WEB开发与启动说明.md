# WEB开发与启动说明

## 当前结构

本目录只保留网页版：

- `GTeamOpsHub.exe`：本机FastAPI后端，启动后自动打开浏览器。
- `g_team_ops/web/factory.py`：应用工厂，只装配依赖和注册模块；
- `g_team_ops/web/context.py`：当前用户、数据库、模板和CSRF公共依赖；
- `g_team_ops/modules/`：账号、货代连接、店铺、物流和库存销售功能路由；
- `g_team_ops/web/app.py`：兼容入口，不再放置业务路由。

桌面验证版已分离到 `D:\CodexProject\FBA_Tracker_Desktop`。两套项目的源码、依赖、数据库和EXE互相独立。

新增模块必须遵守[架构与模块开发规范.md](架构与模块开发规范.md)：在
`g_team_ops/modules/<module_name>`内开发，并配套独立业务测试和Web接口测试。
禁止将新功能重新堆入应用工厂、其他模块路由或单个超大模板脚本。

## 本机启动

最简单的方式是双击：

```text
GTeamOpsHub.exe
```

启动后浏览器会自动打开：

```text
http://127.0.0.1:8765
```

运行期间请保持后端控制台窗口开启。关闭控制台即停止后端，浏览器页面随后无法继续查询。

源码开发方式：

```powershell
.\start_web.ps1
```

## 数据

网页版只使用：

```text
data/app.db
```

桌面版使用 `D:\CodexProject\FBA_Tracker_Desktop\data\app.db`。分离时复制了一份数据快照，之后两边不会自动同步。网页版还会生成：

```text
data/web_session.key
```

该文件保存的是Windows DPAPI密文，不是明文会话密钥。不要删除正在使用的 `data` 文件夹。

## 当前安全边界

当前Web服务只监听 `127.0.0.1`，只能从本机访问。不要通过端口转发、关闭防火墙或把监听地址改成 `0.0.0.0` 来直接给其他人使用，因为当前本机开发模式尚未配置HTTPS、反向代理、服务器备份和正式密钥管理。

## 后续正式给组员使用

正式多人版本建议：

1. 部署到公司所有或明确授权的Windows/Linux服务器；
2. 使用PostgreSQL替换SQLite；
3. 配置域名、HTTPS和反向代理；
4. 将DPAPI业务密钥迁移为服务器主密钥或密钥管理服务；
5. 增加登录失败锁定、数据库定时备份和集中审计；
6. 设置至少两个公司掌握的超级管理员；
7. 完成数据导出和管理员交接流程。

## 页面

- `/setup`：首次管理员初始化；
- `/login`：登录；
- `/dashboard`：工作台；
- `/tracking`：三家货代查询和共享表同步；
- `/carriers`：当前用户自己的货代账号和验证码登录；
- `/shops`：当前用户的多个店铺、两套AirScript连接和国家站点三子表映射；
- `/inventory`：领星Listing库存销售文件预览与共享表更新；
- `/admin`：管理员账号管理。
- `/operations`：当前用户操作历史、逐项选择恢复、中断任务安全接管和冲突保护；管理员还可查看备份健康状态、创建并验证本机数据库备份。

## 界面与本地资源

- 当前继续使用FastAPI、Jinja2和原生JavaScript/CSS，不需要安装Node.js或运行前端构建服务。
- `theme.css`集中维护浅色、深色主题变量，`app.css`维护全局布局和业务组件；明暗选择保存在当前浏览器本机。
- 导航图标和站点图标均为项目内SVG，不引用图标字体、CDN、博客主题文件或远程图片；壁纸统一保存在`static/wallpapers`并转换为受控尺寸的WebP。
- `/dashboard`使用全屏壁纸和紧凑玻璃图标入口，不显示常驻侧栏；主题、动态、修改密码和退出仍固定可达。进入业务模块后恢复完整侧栏，避免沉浸式首页降低操作效率。
- 花瓣效果仅在`/dashboard`加载，以全视口Canvas呈现较丰富但低透明度的花瓣，不接收鼠标事件且低于功能入口；页面失去焦点时暂停，用户可在首页顶部关闭，并自动遵循操作系统“减少动态效果”设置。
- 当前壁纸分配：工作台`dashboard.webp`、物流`tracking.webp`、货代`carriers.webp`、店铺`shops.webp`、库存`inventory.webp`、操作历史`operations.webp`、账号`admin.webp`、登录与初始化`auth.webp`。模块壁纸保持原图清晰且只覆盖薄明暗遮罩，不使用模糊滤镜；标题、卡片、表单、表格和状态区使用完全不透明表面，始终优先可读。
- 新增页面应复用现有变量、组件和壁纸遮罩策略，不得为单页复制相互冲突的样式，也不得直接加载用户桌面原图或超大未压缩资源。

新版一键查询前必须按照 [AirScript升级说明.md](AirScript升级说明.md) 替换WPS中的文档共享脚本。
每个启用物流的站点必须配置对应的 `国家代码-FBA` 主表和
`国家代码-轨迹明细` 子表，并按升级说明配置标准表头。
表头顺序不限，可与店铺、备注等其他列混排；程序按名称识别，不按列字母识别。
`US-FBA` 第一行必须有唯一的“货代”表头；自动任务会优先查询对应货代。只有
指定货代明确返回“未找到”时，才会查询另外两家兜底；登录失败、网络失败等
“查询失败”不会触发兜底。若仅一家兜底货代命中且另一家明确“未找到”，则正常
回填；若另一家查询失败，则报告“部分查询失败”并跳过回填，因为尚未排除货代冲突。
若多家同时命中则报告货代冲突并跳过回填。系统不会自动改写共享表的“货代”列。

一键任务会获取三家货代的完整轨迹：主表保存当前物流快照，明细表保存当前活跃FBA的预计、
实际、变更、异常、恢复和POD事件。提货、实际出发、实际到达和签收时间只在目标
单元格为空时填写；若已有人工值且不同则保留人工值并报告冲突。“提取派送”取最近
一次实际“派送中”的日期，发生重新派送时会更新。完整详情缓存在网页版本机数据库中，官网最新轨迹
未变化时复用缓存，避免每天两次更新都重复请求相同详情。

`运输方式`、`运输线路/渠道`、`目的仓`、`末端追踪号` 不由物流模块维护。
明细表不再要求 `地点` 和 `附件/POD`。AirScript会把日期列设为日期格式，并把
事件编号、FBA号、货代订单号设为文本格式。每次同步成功后，只清理主表“是否完成”
非空或已经移除FBA的明细行；仍为空的活跃FBA查询失败时保留原轨迹，不会整表清空。

进入 `/tracking` 时只读取本机配置和最近状态，不自动登录或访问货代网站。手动查询
和店铺一键更新优先复用当前用户最近 10 分钟内的连接结果，只对缺失或过期状态联网
检查；需要绕过缓存时由用户点击“立即重新检查”。进入 `/carriers` 时同样只加载当前
用户自己的本机加密账号密码和最近状态。缓存超时、程序重启或凭据变更后恢复为“未检查”。
保存或删除某一家货代账号时，只重置该货代的状态；其他货代的最近检查结果保留。
货代账号输入框禁用浏览器登录密码自动填充，并以当前用户本机加密保存的数据为准。

## Listing库存销售模块

Listing模块使用另一份独立AirScript，不复用物流Webhook和令牌。两个模块连接都在
“我的店铺”中集中保存一次；库存销售页面只选择店铺和国家。最终共享表表头、公式、
国家子表映射、同日重试规则及首次启用步骤见
[库存销售表格模块使用说明.md](库存销售表格模块使用说明.md)。

源码更新后，现有`GTeamOpsHub.exe`不会自动变化。如需使用EXE运行本模块，应在完整测试
通过后执行：

```powershell
.\build_web_exe.ps1
```

每次点击手动物流查询都会优先读取 10 分钟连接缓存；缓存不存在或过期时才并行检查
对应货代，然后只调度最近检查成功的货代。某家未连接不会终止任务，也不会把其他货代
查到的结果标成部分失败。安达会复用同次检查建立的短时内存会话，避免检查后重复登录。易通令牌失效时，账号密码仍会从本机
加载，用户只需刷新并输入验证码重新登录。

## 数据库迁移、备份与恢复

应用继续使用本机`data/app.db`，但数据库结构现由Alembic版本化管理。旧数据库首次启动
新版程序时会先在`data/backups`中生成SQLite一致性备份并完成完整性检查，再执行迁移。
所有数据库连接统一启用WAL、外键、10秒写等待和`synchronous=FULL`。

管理员可以在“操作历史与恢复”页面手工创建整库备份并重新验证。程序每天首次启动还会
自动生成一份一致性备份，只保留最近14份`scheduled_daily`自动备份；手工、迁移前、
恢复前备份不会被自动轮换删除。

正式整库恢复通过独立`GTeamOpsMaintenance.exe`或`restore_database.ps1`执行。维护工具会
验证SHA-256和数据库完整性、创建`pre_restore`保险备份、确认数据库可独占访问并使用
SQLite在线备份API恢复；失败时从保险备份恢复原库。Web服务运行或维护锁存在时禁止恢复。
恢复记录写入`database_restores`。具体命令见[操作历史与恢复说明.md](操作历史与恢复说明.md)。

物流版本11与Listing版本4 AirScript已接入共享表写前快照、前置条件校验、恢复预览和
条件恢复。每次写入前快照会先持久化到`operation_snapshots`；完成后记录实际变化。
“恢复上一次操作”仅处理当前用户、店铺和模块最近一个可恢复批次，冲突项不会覆盖，
具体规则见[操作历史与恢复说明.md](操作历史与恢复说明.md)。

状态为`uncertain`且保存了写前快照的共享表批次，可在操作历史页面执行只读核对。核对
只读取WPS当前值并补记实际差异，不修改共享表；有差异时批次转为`reconciled`并可按正常
冲突保护流程恢复，无差异时转为`reconciled_no_change`。

恢复预览会为每项实际差异返回数据库中的稳定变更ID，页面只把当前值仍等于原写入值的
项目列为可选项。部分恢复完成后会再次只读检查原批次全部变更；仍有待恢复、冲突或失败
时保留`rollback_partial`，后续可继续选择，其余项目不会被重复覆盖。

若程序退出后批次仍显示`running`或`rollback_running`，只能在确认原进程或请求已经停止、
且一小时租约锁已经过期后执行“接管中断任务”。普通写入有写前快照时转入`uncertain`并先
只读核对；恢复任务转入`rollback_partial`并从原操作继续预览。该能力不是后台任务队列，
不会自动重放货代查询或WPS写入。

管理员页面的备份健康提示快速检查最近每日备份的时效、文件存在性、大小和目录中的完整性
结果；“完整校验”按钮才会重新计算SHA-256并运行SQLite完整性检查。正式恢复前仍必须使用
维护程序的预览和校验流程。
